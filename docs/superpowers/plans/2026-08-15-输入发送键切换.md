# 输入发送键切换 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 输入框支持「Enter 发送 / Ctrl+Enter 发送」两种模式切换，基础设置页新增选项，勾选立即生效。

**Architecture:** 配置键 `input.enterSends`（默认 false）由 Config 读写；InputView 按键过滤器每次按键时读 `config.enterSends()` 分流键位，勾选即写回 Config（内存+落盘）→ 下一次按键自动生效，无回调机制。键位判定与发送键文案提为纯静态方法，可脱离 JavaFX 单测。

**Tech Stack:** JDK 8 + JavaFX 8、JUnit4、Maven（`JAVA_HOME="E:/javame/jdk8" mvn test`）。

## Global Constraints

- **JDK 8**：无新依赖；所有代码 JDK 8 语法（无 var、无 diamond 推断遗漏）。
- **零回归**：默认模式（enterSends=false）下按键行为与现状完全一致——Ctrl+Enter 发送（弹层开着也发送），Enter 换行，弹层打开时 Enter/Tab 确认。
- **非 ASCII 命令行参数**：本机 Git Bash wrapper 遇中文命令参数崩溃（exit 127）。git add 只写 ASCII 路径（本项目所有源文件路径均为 ASCII）；commit 消息用 Write 工具写入 `.git/COMMIT_MSG.tmp` 后 `git commit -F`，提交后删除。
- **并发仓库**：仓库可能有并行 agent 的未提交文件（如 theme.css）；git add 只添加本任务列出的文件，不做 `git add -A`。
- 文档、注释、commit 均用中文（conventional 格式）。

---

### Task 1: Config 配置项 `input.enterSends`

**Files:**
- Modify: `src/resource/config.properties`
- Modify: `src/main/java/com/minion/core/config/Config.java`（`confirmSkip()` 后新增 getter）
- Test: `src/test/java/com/minion/core/config/ConfigTest.java`

**Interfaces:**
- Produces: `Config.enterSends()` → `boolean`（默认 false；外部文件可覆盖为 true）。Task 2/3 依赖。

- [ ] **Step 1: 写失败测试**（ConfigTest 追加两个测试，风格对齐 `readAllowOutside_defaultsFalseAndOverridable`）

```java
/** 输入发送键：默认 false（Ctrl+Enter 发送） */
@Test
public void enterSends_defaultsFalse() throws IOException {
    Config c = Config.load(tmp.getRoot().toPath(), TEST_DEFAULTS);
    assertFalse(c.enterSends());
}

/** 输入发送键：外部文件可覆盖为 true，重载后保持 */
@Test
public void enterSends_externalOverridePersists() throws IOException {
    Path root = tmp.getRoot().toPath();
    Config c1 = Config.load(root, TEST_DEFAULTS);
    Files.write(c1.externalFile(), "\ninput.enterSends=true\n".getBytes(StandardCharsets.UTF_8),
            java.nio.file.StandardOpenOption.APPEND);
    Config c2 = Config.load(root, TEST_DEFAULTS);
    assertTrue(c2.enterSends());
}
```

- [ ] **Step 2: 运行确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ConfigTest`
Expected: `enterSends_defaultsFalse` 编译失败（`enterSends()` 不存在）。

- [ ] **Step 3: 实现**

`src/resource/config.properties` 的「高危操作确认」块后新增：

```properties
# ===== 输入框 =====
# 输入框发送键：false=Ctrl+Enter 发送（Enter 换行）；true=Enter 发送（Ctrl+Enter 换行）
input.enterSends=false
```

`Config.java` 在 `confirmSkip()` 后新增：

```java
/** 输入框发送键：true=Enter 发送（Ctrl+Enter 换行）；false（默认）=Ctrl+Enter 发送（Enter 换行） */
public boolean enterSends() { return Boolean.parseBoolean(get("input.enterSends", "false")); }
```

- [ ] **Step 4: 运行确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ConfigTest`
Expected: 全部 PASS（含既有 7 个测试）。

- [ ] **Step 5: Commit**

用 Write 工具写 `.git/COMMIT_MSG.tmp`，内容：`feat: 配置项 input.enterSends（发送键切换）`，然后：

```bash
git add src/resource/config.properties src/main/java/com/minion/core/config/Config.java src/test/java/com/minion/core/config/ConfigTest.java
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

---

### Task 2: InputView 键位逻辑 + 提示文案动态化

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`
- Modify: `src/main/java/com/minion/gui/MainWindow.java:150`
- Create: `src/test/java/com/minion/gui/input/InputViewKeyTest.java`

**Interfaces:**
- Consumes: `Config.enterSends()`（Task 1）。
- Produces:
  - `static boolean InputView.isSendKey(KeyCode code, boolean ctrl, boolean shift, boolean alt, boolean meta, boolean enterSends)` — 该按键事件是否为「发送」。默认模式 = ENTER 且仅 Ctrl 按下（精确修饰键语义，与 KeyCodeCombination(ENTER, CONTROL_DOWN) 一致）；Enter 发送模式 = ENTER 且无任何修饰键。
  - `static String InputView.sendKeyLabel(boolean enterSends)` — "Enter" / "Ctrl+Enter"。
  - `InputView(SessionManager manager, Config config)`（构造器签名变更）。

- [ ] **Step 1: 写失败测试**（新建 `src/test/java/com/minion/gui/input/InputViewKeyTest.java`，纯静态逻辑，参考 SuggestionPopupTest 风格；`KeyCode` 是枚举，无需 JavaFX Toolkit）

```java
package com.minion.gui.input;

import javafx.scene.input.KeyCode;
import org.junit.Test;

import static org.junit.Assert.*;

/** 发送键判定与文案：纯静态逻辑，可脱离 JavaFX 单测（KeyCode 为枚举，无需 Toolkit） */
public class InputViewKeyTest {

    // ===== isSendKey：默认模式（Ctrl+Enter 发送） =====

    @Test public void defaultMode_ctrlEnter_sends() {
        assertTrue(InputView.isSendKey(KeyCode.ENTER, true, false, false, false, false));
    }

    @Test public void defaultMode_plainEnter_notSend() {
        assertFalse(InputView.isSendKey(KeyCode.ENTER, false, false, false, false, false));
    }

    @Test public void defaultMode_ctrlShiftEnter_notSend() {
        // 与 KeyCodeCombination(ENTER, CONTROL_DOWN) 精确修饰键语义一致：Ctrl 按下时 Shift 须未按下
        assertFalse(InputView.isSendKey(KeyCode.ENTER, true, true, false, false, false));
    }

    @Test public void defaultMode_nonEnterKey_notSend() {
        assertFalse(InputView.isSendKey(KeyCode.A, true, false, false, false, false));
    }

    // ===== isSendKey：Enter 发送模式 =====

    @Test public void enterSendsMode_plainEnter_sends() {
        assertTrue(InputView.isSendKey(KeyCode.ENTER, false, false, false, false, true));
    }

    @Test public void enterSendsMode_ctrlEnter_notSend() {
        assertFalse(InputView.isSendKey(KeyCode.ENTER, true, false, false, false, true));
    }

    @Test public void enterSendsMode_shiftEnter_notSend() {
        assertFalse(InputView.isSendKey(KeyCode.ENTER, false, true, false, false, true));
    }

    @Test public void enterSendsMode_nonEnterKey_notSend() {
        assertFalse(InputView.isSendKey(KeyCode.A, false, false, false, false, true));
    }

    // ===== sendKeyLabel =====

    @Test public void sendKeyLabel_defaultMode() {
        assertEquals("Ctrl+Enter", InputView.sendKeyLabel(false));
    }

    @Test public void sendKeyLabel_enterSendsMode() {
        assertEquals("Enter", InputView.sendKeyLabel(true));
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputViewKeyTest`
Expected: 编译失败（`isSendKey`/`sendKeyLabel` 不存在）。

- [ ] **Step 3: 实现 InputView 静态方法与构造器**

3a. 构造器签名与字段：

```java
import com.minion.core.config.Config;
// ...
public class InputView extends VBox {
    // ...
    private final Config config;
    // 构造器：
    public InputView(final SessionManager manager, final Config config) {
        this.manager = manager;
        this.config = config;
        // ...（其余不变）
```

3b. 删除不再使用的 import：`javafx.scene.input.KeyCodeCombination`、`javafx.scene.input.KeyCombination`（旧第 109-110 行的 KeyCodeCombination 判断被 isSendKey 取代）。

3c. 新增静态方法（放在 `buttonMode` 附近，风格同 `buttonStyleClass`）：

```java
/** 当前发送键模式下该按键事件是否为「发送」：默认=Ctrl+Enter（精确修饰键语义，同 KeyCodeCombination）；
 *  Enter 发送模式=纯 Enter。弹层打开时由事件过滤器分流，不算发送 */
static boolean isSendKey(KeyCode code, boolean ctrl, boolean shift, boolean alt, boolean meta, boolean enterSends) {
    if (code != KeyCode.ENTER) return false;
    if (enterSends) return !ctrl && !shift && !alt && !meta;
    return ctrl && !shift && !alt && !meta;
}

/** 发送键显示名（按钮 tooltip / 输入框占位提示用） */
static String sendKeyLabel(boolean enterSends) {
    return enterSends ? "Enter" : "Ctrl+Enter";
}
```

- [ ] **Step 4: 改造按键过滤器**

把旧过滤器开头（原第 108-114 行：`input.addEventFilter(...)` 起、含 KeyCodeCombination 判断与 `if (popup.isShowing())` 前的位置）替换为：

```java
input.addEventFilter(KeyEvent.KEY_PRESSED, e -> {
    boolean ctrl = e.isControlDown(), shift = e.isShiftDown(), alt = e.isAltDown(), meta = e.isMetaDown();
    boolean enterSends = config.enterSends();
    // 发送键：默认模式优先于弹层（同现状：弹层开着 Ctrl+Enter 也发送）；
    // Enter 发送模式弹层打开时不发送，Enter/Ctrl+Enter 均走弹层确认
    if (isSendKey(e.getCode(), ctrl, shift, alt, meta, enterSends) && !(enterSends && popup.isShowing())) {
        e.consume();
        onAction();
        return;
    }
    // Enter 发送模式：弹层关闭时 Ctrl+Enter 显式插入换行（JavaFX TextArea 对 Ctrl+Enter 无默认换行绑定，须 replaceSelection）
    if (enterSends && !popup.isShowing() && e.getCode() == KeyCode.ENTER && ctrl && !shift && !alt && !meta) {
        e.consume();
        input.replaceSelection("\n");
        return;
    }
    // 长文本粘贴（Ctrl+V / Shift+Insert）→ 变块；短文本不拦截走默认粘贴
    if ((e.isControlDown() && e.getCode() == KeyCode.V)
            || (e.isShiftDown() && e.getCode() == KeyCode.INSERT)) {
```

过滤器其余部分（粘贴、弹层分支、Backspace 删块、Esc 终止）保持不变——弹层分支的 `case ENTER: confirmPopup()` 天然覆盖 Enter 发送模式下 Ctrl+Enter 确认（KeyCode 同为 ENTER）。

- [ ] **Step 5: 提示文案动态化**

5a. 构造器中删除硬编码占位提示（原第 71 行 `input.setPromptText("输入消息…  (@ 引用文件  / 命令  Ctrl+Enter 发送)")`），在构造器末尾（`getChildren().add(root)` 之后）调用 `updatePrompt();`（askPending 默认 false，显示正常提示）。

5b. `updatePrompt()` 非 askPending 分支改为：

```java
input.setPromptText("输入消息…  (@ 引用文件  / 命令  " + sendKeyLabel(config.enterSends()) + " 发送)");
```

5c. `updateButton()` 五个 tooltip 的 `"(Ctrl+Enter)"` 改为 `"(" + sendKeyLabel(config.enterSends()) + ")"`（SEND/SEND_DIM/SUPPLEMENT/ANSWER/ANSWER_DIM 共 5 处）。

5d. `focusedProperty` 监听（原第 77-79 行）补焦点进入时刷新文案——用户勾选设置后点回输入框，提示与 tooltip 立即正确：

```java
input.focusedProperty().addListener((obs, ov, nv) -> {
    if (!nv) { popup.hide(); lastToken = null; }
    else { updateButton(); updatePrompt(); }
});
```

- [ ] **Step 6: MainWindow 接线**

`src/main/java/com/minion/gui/MainWindow.java:150`：

```java
inputView = new InputView(manager, MinionApp.config());
```

（MinionApp 与 MainWindow 同包 `com.minion.gui`，无需 import。）

- [ ] **Step 7: 运行确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputViewKeyTest,ConfigTest`
Expected: 全部 PASS（10 个新测试 + 既有 ConfigTest）。

- [ ] **Step 8: Commit**

用 Write 工具写 `.git/COMMIT_MSG.tmp`，内容：`feat: 输入框发送键可切换（Enter 发送 / Ctrl+Enter 发送）`，然后：

```bash
git add src/main/java/com/minion/gui/input/InputView.java src/main/java/com/minion/gui/MainWindow.java src/test/java/com/minion/gui/input/InputViewKeyTest.java
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

---

### Task 3: 设置页选项（实时生效）+ README 同步

**Files:**
- Modify: `src/main/java/com/minion/gui/dialog/SettingsDialog.java`（BasicPane）
- Modify: `README.md:28`

**Interfaces:**
- Consumes: `Config.enterSends()`、`Config.set(String, String)`、`input.enterSends` 键名（Task 1）。

- [ ] **Step 1: BasicPane 新增 CheckBox**

`SettingsDialog.java` BasicPane 字段区（`skipConfirm` 后）新增：

```java
private final CheckBox enterSends;
```

构造器（`skipConfirm.setSelected(config.confirmSkip());` 后）：

```java
enterSends = new CheckBox("Enter 发送消息（Ctrl+Enter 换行）");
enterSends.setSelected(config.enterSends());
// 勾选立即生效：直接写回 Config（内存+落盘），InputView 按键时读取 → 下次按键即新键位；无需点「应用」
enterSends.selectedProperty().addListener((obs, ov, nv) ->
        config.set("input.enterSends", String.valueOf(nv)));
```

`rows` 列表在 `row("确认开关:", skipConfirm)` 后插入：

```java
row("发送键:", enterSends),
```

`apply()` 不写该键（实时已写，注释说明）。

- [ ] **Step 2: README 同步**

`README.md:28` 改为：

```markdown
- 发送键两种模式（基础设置「发送键」勾选即生效）：默认 Ctrl+Enter 发送、Enter 换行；勾选后 Enter 发送、Ctrl+Enter 换行；Esc 关闭补全弹层 / 终止当前运行
```

- [ ] **Step 3: 编译 + 全量测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn clean package`
Expected: BUILD SUCCESS（编译 + 全部既有测试通过，含 SettingsDialogTest/InputViewKeyTest）。

- [ ] **Step 4: 手动验证（GUI）**

Run: `minion.bat`
- 默认模式：输入文本按 Enter → 换行不发送；Ctrl+Enter → 发送；按钮 tooltip 显示「发送 (Ctrl+Enter)」。
- 设置（⚙ → 基础设置 → 发送键）勾选 → 点回输入框 → prompt 变「…Enter 发送」；输入文本按 Enter → 发送；Ctrl+Enter → 插入换行；按钮 tooltip 显示「发送 (Enter)」。
- 取消勾选 → 恢复默认键位。
- 重启 minion.bat → 勾选状态保持，键位按持久化配置生效。

- [ ] **Step 5: Commit**

用 Write 工具写 `.git/COMMIT_MSG.tmp`，内容：`feat: 基础设置新增发送键切换选项（实时生效），README 同步`，然后：

```bash
git add src/main/java/com/minion/gui/dialog/SettingsDialog.java README.md
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

---

## 自审记录

- **规格覆盖**：配置项（Task 1）✓；InputView 键位表全部四格行为（Task 2 Step 4）✓；设置页实时生效（Task 3 Step 1）✓；提示文案动态化 + 焦点刷新（Task 2 Step 5）✓；测试与文档（各任务）✓。
- **占位符**：无 TBD/TODO；每步含可执行代码。
- **类型一致**：`isSendKey(KeyCode, boolean×4, boolean)`、`sendKeyLabel(boolean)`、`Config.enterSends()`、`InputView(manager, config)` 各任务间签名一致。
