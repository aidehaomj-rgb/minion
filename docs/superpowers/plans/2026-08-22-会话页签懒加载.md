# 会话页签懒加载与关闭语义改造 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 页签改为「启动懒加载 + 打开过才有页签 + × 关闭不删除（运行中弹窗确认中断）+ 与工作空间解耦」

**Architecture:** MainWindow 用 `openedIds` 集合作为页签存在性的唯一权威（切工作空间保留）；激活会话即建页签、关闭页签即卸载视图缓存；SessionManager 补 `deactivateSession`（取消激活态，防幂等守卫挡住再次激活）、`findSession`（跨空间查会话）与 `onSessionDeleted` 通知（删除会话/工作空间时统一清理死页签）。

**Tech Stack:** JDK 8、JavaFX 8、JUnit 4、Maven（构建命令 `JAVA_HOME="D:/javame/jdk1.8" mvn ...`）

## Global Constraints

- JDK 8 兼容，不用任何 Java 9+ 语法/API
- 文档、注释、commit 均用中文；commit 用 conventional 格式（feat:/fix:/docs:）
- 代码风格：4 空格缩进、大括号换行（K&R 同现有代码）、显式 import
- 不新增依赖；不修改 core 层（AgentLoop/工具等）
- 每次任务结束必须 `mvn compile`（或 test）通过后再 commit

---

### Task 1: SessionManager — findSession + deactivateSession + onSessionDeleted 通知

**Files:**
- Modify: `src/main/java/com/minion/gui/session/SessionManager.java`
- Test: `src/test/java/com/minion/gui/session/SessionManagerTest.java`

**Interfaces:**
- Consumes: 现有 `Listener` 接口、`SessionHandle`（public 字段：`id`/`workspaceName`/`title`/`deleted`）、`WorkspaceCtx`（private 内部类，`ctxByName`/`sessions`）
- Produces:
  - `public void deactivateSession(SessionHandle h)` — 取消激活态：若 `currentSession == h` 则事件流 `setActive(false, null)` 且 `currentSession = null`
  - `public SessionHandle findSession(String id)` — 跨所有工作空间按 id 查会话，未找到返回 null
  - `Listener.onSessionDeleted(SessionHandle h)`（default 方法）— 删除通知；`deleteSession` 与 `deleteWorkspace` 均须发出

- [ ] **Step 1: 写失败测试**（追加到 `SessionManagerTest.java` 类尾部、`SpyManager` 之前）

```java
/** 页签关闭语义：deactivateSession 取消激活（currentSession 置空），再次 activate 可重新激活 */
@Test
public void deactivateSession_clearsCurrentAndAllowsReactivate() throws Exception {
    SessionManager m = newManager();
    final List<SessionHandle> activated = new ArrayList<SessionHandle>();
    m.addListener(new SessionManager.Listener() {
        @Override public void onSessionTitleChanged(SessionHandle h) { }
        @Override public void onSessionRunningChanged(SessionHandle h, boolean running) { }
        @Override public void onSessionActivated(SessionHandle h) { activated.add(h); }
        @Override public void onWorkspaceChanged() { }
        @Override public void onError(String message) { fail("不应有错误: " + message); }
    });
    SessionHandle h = m.createSession(null);
    m.activateSession(h);
    assertEquals(h, m.currentSession());

    m.deactivateSession(h);

    assertNull("取消激活后 currentSession 应置空", m.currentSession());
    m.activateSession(h); // 幂等守卫不再拦截：UI 关闭页签后左侧可重新打开
    assertEquals(h, m.currentSession());
    assertEquals(2, activated.size());
}

/** deactivateSession 只取消目标会话：非当前会话调用不误伤 */
@Test
public void deactivateSession_nonCurrentNoop() throws Exception {
    SessionManager m = newManager();
    SessionHandle h1 = m.createSession(null);
    SessionHandle h2 = m.createSession(null);
    m.activateSession(h1);

    m.deactivateSession(h2);

    assertEquals(h1, m.currentSession());
}

/** deactivateSession 幂等：连续调用无异常、状态不变 */
@Test
public void deactivateSession_idempotent() throws Exception {
    SessionManager m = newManager();
    SessionHandle h = m.createSession(null);
    m.activateSession(h);
    m.deactivateSession(h);
    m.deactivateSession(h); // 二次调用不抛异常
    assertNull(m.currentSession());
}

/** 删除会话：发 onSessionDeleted 通知（UI 清理页签/缓存） */
@Test
public void deleteSession_notifiesDeleted() throws Exception {
    SessionManager m = newManager();
    final List<SessionHandle> deleted = new ArrayList<SessionHandle>();
    m.addListener(new SessionManager.Listener() {
        @Override public void onSessionTitleChanged(SessionHandle h) { }
        @Override public void onSessionRunningChanged(SessionHandle h, boolean running) { }
        @Override public void onSessionActivated(SessionHandle h) { }
        @Override public void onWorkspaceChanged() { }
        @Override public void onError(String message) { fail("不应有错误: " + message); }
        @Override public void onSessionDeleted(SessionHandle h) { deleted.add(h); }
    });
    SessionHandle h = m.createSession(null);
    m.deleteSession(h);
    assertEquals(1, deleted.size());
    assertEquals(h, deleted.get(0));
}

/** 删除非当前工作空间：同样发 onSessionDeleted 通知（跨空间死页签清理依赖） */
@Test
public void deleteWorkspace_nonCurrent_notifiesDeletedForAll() throws Exception {
    Path jar = tmp.newFolder("jar").toPath();
    Config config = Config.load(jar);
    WorkspaceManager ws = WorkspaceManager.load(jar);
    ws.add("projA", tmp.newFolder("a").getPath(), "");
    ws.add("projB", tmp.newFolder("b").getPath(), "");
    ModelManager models = ModelManager.load(jar);
    SessionManager m = new SessionManager(FAKE_UI, config, jar, ws, models,
            new ArrayList<Skill>(), null, null);
    m.switchWorkspace("projA");
    SessionHandle h = m.createSession(null);
    m.switchWorkspace("projB"); // 当前空间切走：projA 会话处于「页签保留」状态
    final List<SessionHandle> deleted = new ArrayList<SessionHandle>();
    m.addListener(new SessionManager.Listener() {
        @Override public void onSessionTitleChanged(SessionHandle h) { }
        @Override public void onSessionRunningChanged(SessionHandle h, boolean running) { }
        @Override public void onSessionActivated(SessionHandle h) { }
        @Override public void onWorkspaceChanged() { }
        @Override public void onError(String message) { fail("不应有错误: " + message); }
        @Override public void onSessionDeleted(SessionHandle h) { deleted.add(h); }
    });

    assertTrue(m.deleteWorkspace("projA")); // 非当前空间删除：无 onWorkspaceChanged，须靠通知清页签

    assertEquals(1, deleted.size());
    assertEquals(h, deleted.get(0));
    assertNull(ws.get("projA"));
}

/** findSession：跨工作空间按 id 查找（页签点击路径——页签与工作空间无关） */
@Test
public void findSession_acrossWorkspaces() throws Exception {
    Path jar = tmp.newFolder("jar").toPath();
    Config config = Config.load(jar);
    WorkspaceManager ws = WorkspaceManager.load(jar);
    ws.add("projA", tmp.newFolder("a").getPath(), "");
    ModelManager models = ModelManager.load(jar);
    SessionManager m = new SessionManager(FAKE_UI, config, jar, ws, models,
            new ArrayList<Skill>(), null, null);
    m.switchWorkspace("projA");
    SessionHandle h = m.createSession(null);
    m.switchWorkspace("default");

    assertNull("查不到应返回 null", m.findSession("不存在"));
    assertEquals(h, m.findSession(h.id)); // 跨空间可查
}
```

- [ ] **Step 2: 跑测试确认失败（编译失败即预期）**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=SessionManagerTest`
Expected: 编译失败，报 `deactivateSession`/`findSession`/`onSessionDeleted` 不存在

- [ ] **Step 3: 实现**（`SessionManager.java` 三处改动）

改动 a — Listener 接口新增 default 方法（在 `onSessionAskChanged` 之后）：

```java
/** 会话被删除（deleteSession / deleteWorkspace 均通知，含非当前空间）：UI 清理页签与缓存 */
default void onSessionDeleted(SessionHandle h) { }
```

改动 b — 通知方法（放在 `notifyAskChanged` 之后）：

```java
private void notifySessionDeleted(SessionHandle h) {
    for (Listener l : listeners) l.onSessionDeleted(h);
}
```

改动 c — 新增两个公开方法（`deactivateSession` 放 `activateSession` 之后；`findSession` 放 `sessions()` 之后）：

```java
/** 取消激活态（关闭激活中会话的页签时调用）：
 *  UI 已卸载该会话视图但 currentSession 仍指向它——activateSession 的幂等守卫
 *  （currentSession == h 直接 return）会挡住用户从左侧再次打开，故须置空。 */
public void deactivateSession(SessionHandle h) {
    if (currentSession == h) {
        currentSession.controller.eventList().setActive(false, null);
        currentSession = null;
    }
}

/** 按 id 查找会话（跨所有工作空间）：页签点击路径——页签与工作空间无关 */
public SessionHandle findSession(String id) {
    for (WorkspaceCtx ctx : ctxByName.values()) {
        for (SessionHandle h : ctx.sessions) {
            if (h.id.equals(id)) return h;
        }
    }
    return null;
}
```

改动 d — `deleteSession` 中 `h.deleted = true;` 之后加：

```java
notifySessionDeleted(h);
```

改动 e — `deleteWorkspace` 的 for 循环内、`h.deleted = true;` 之后加（循环内每个会话都通知）：

```java
notifySessionDeleted(h);
```

- [ ] **Step 4: 跑测试确认通过**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=SessionManagerTest`
Expected: 全部 PASS（含既有用例，共 25+）

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/minion/gui/session/SessionManager.java src/test/java/com/minion/gui/session/SessionManagerTest.java
git commit -m "feat: 会话页签关闭语义支撑（deactivateSession/findSession/onSessionDeleted 通知）"
```

---

### Task 2: MainWindow — 页签懒加载、× 关闭、切空间页签保留、跨空间自动切

**Files:**
- Modify: `src/main/java/com/minion/gui/MainWindow.java`

**Interfaces:**
- Consumes: Task 1 产出的 `manager.deactivateSession(SessionHandle)`、`manager.findSession(String)`；现有 `manager.switchWorkspace(String)`、`manager.activateSession(SessionHandle)`、`manager.stop(SessionHandle)`、`manager.sessions()`、`manager.currentSession()`；`SessionHandle.title/running/workspaceName`（public）
- Produces: 无对外接口（GUI 行为）

说明：JavaFX 界面代码无法单测，本任务以 `mvn compile` + 行为自查为验证；`tabTitle` 静态方法及其测试（`MainWindowTabTitleTest`）不受影响。

- [ ] **Step 1: 字段与 import**

在 `MainWindow.java` 字段区（`suppressingTabSelect` 声明处）：

```java
/** 已打开会话 id 集合：页签存在性的唯一权威（页签 ⇔ openedIds 含 id）；切工作空间保留 */
private final Set<String> openedIds = new HashSet<String>();
```

删除字段 `private boolean suppressingTabSelect; // rebuildTabs 期间不触发页签选中激活`。

import 区（`java.util.HashMap` 附近）补：

```java
import java.util.HashSet;
import java.util.Set;
```

- [ ] **Step 2: 删除启动重建**

删除 `show()` 末尾（`stage.show();` 之前）的：

```java
rebuildTabs(); // 启动补齐历史会话页签（需求 2：启动后历史会话即有页签）
```

删除 `rebuildTabs()` 方法整体（文件末尾，`addTab`/`removeTabById`/`selectTab` 之后的最后一段）：

```java
private void rebuildTabs() {
    for (Tab t : tabs.getTabs()) StatusDot.stopPulseIn(t.getGraphic()); // 回收呼吸动画
    tabs.getTabs().clear();
    suppressingTabSelect = true; // 启动/切空间补齐不触发激活；结束后恢复当前会话选中
    for (SessionHandle h : manager.sessions()) {
        if (h.title != null) addTab(h);
    }
    suppressingTabSelect = false;
    if (manager.currentSession() != null) selectTab(manager.currentSession());
}
```

`addTab` 末尾的选中改为无条件（不再有 suppressingTabSelect 场景）：

```java
tabs.getSelectionModel().select(t);
```

- [ ] **Step 3: 激活即建页签 + updateTab 防复活**

`onSessionActivated` 的 `Platform.runLater` 块开头（`selectTab(h)` 之前）插入：

```java
if (!openedIds.contains(h.id)) {
    openedIds.add(h.id); // 打开过的会话才有页签（懒加载）；titlePending 等标题生成后由 updateTab 补建
    addTab(h); // addTab 内部有 title 非空守卫
}
```

`updateTab` 末尾的 addTab 分支改为（防运行中会话关闭页签后被 running 通知复活）：

```java
if (h.title != null && openedIds.contains(h.id)) addTab(h); // 标题生成后才建页签；已关闭页签的不复活
```

- [ ] **Step 4: 页签 × = 关闭（运行中弹窗确认中断）**

重写 `addTab` 中的 `t.setOnCloseRequest(...)` 整个 lambda（当前是删除会话弹窗），替换为：

```java
t.setOnCloseRequest(e -> {
    e.consume(); // 不触发 TabPane 默认移除，由本流程显式移除
    if (h.running) {
        // 运行中：确认才关（关闭 = 中断运行，会话不删除）
        Alert a = new Alert(Alert.AlertType.CONFIRMATION,
                "会话「" + h.title + "」正在运行，关闭将中断运行，确认关闭？",
                ButtonType.OK, ButtonType.CANCEL);
        a.setHeaderText(null);                 // 去掉左边的"确认"文字
        a.getDialogPane().setGraphic(null);    // 去掉叹号圆圈图标
        a.getDialogPane().getStyleClass().add("dialog-exit"); // 正文居中
        Theme.style(a);
        a.setTitle("关闭会话");
        a.showAndWait();
        if (a.getResult() != ButtonType.OK) return; // 取消：页签保留、运行继续
        manager.stop(h); // 中断运行（会话不删除，保留在列表）
    }
    // 关闭流程：卸载视图缓存（再次从左侧打开时重新加载）+ 移除页签
    openedIds.remove(h.id);
    viewCache.remove(h.id);
    tabs.getTabs().remove(t);
    if (chatView != null && chatView.handle() == h) {
        manager.deactivateSession(h); // 幂等守卫不再拦截再次激活
        clearChatPane();
    }
});
```

- [ ] **Step 5: 切工作空间页签保留 + onSessionDeleted 死页签清理**

`onWorkspaceChanged` 的 `Platform.runLater` 块改为（去掉 `rebuildTabs()`；`openedIds` 保留）：

```java
@Override public void onWorkspaceChanged() {
    Platform.runLater(() -> {
        clearChatPane();
        viewCache.clear(); // 工作空间切换：不跨空间残留缓存视图；openedIds 保留（页签与空间无关）
        wsList.refresh();
        sessionList.refresh();
    });
}
```

`manager.addListener(...)` 中新增 `onSessionDeleted` 监听（与其它 override 平级）：

```java
@Override public void onSessionDeleted(SessionHandle h) {
    Platform.runLater(() -> {
        removeTabById(h.id);
        openedIds.remove(h.id);
        viewCache.remove(h.id); // 死页签清理（幂等：与 SessionListView.onDeleted 回调共存）
        if (chatView != null && chatView.handle() == h) clearChatPane();
    });
}
```

`SessionListView` 的 `onDeleted` 回调（构造处）补 `openedIds.remove(h.id);`（放在 `removeTabById(h.id)` 之后）：

```java
h -> {
    removeTabById(h.id);
    openedIds.remove(h.id); // 删除会话：打开标记一并清除
    viewCache.remove(h.id); // 删除会话：缓存视图一并释放
    if (chatView != null && chatView.handle() == h) clearChatPane();
    sessionList.refresh();
});
```

- [ ] **Step 6: 跨空间页签点击自动切空间**

重写 `show()` 中页签选中监听（当前用 `manager.sessions()` 只在当前空间找，跨空间找不到）：

```java
// 需求：标题栏页签点击激活对应会话（userData 存会话 id）；跨工作空间的页签点击先自动切回该空间
tabs.getSelectionModel().selectedItemProperty().addListener((obs, ov, nv) -> {
    if (nv == null) return;
    Object id = nv.getUserData();
    if (id == null) return;
    SessionHandle h = manager.findSession((String) id); // 跨所有工作空间查找
    if (h == null) return; // 已删会话的残留页签（理论上 onSessionDeleted 已清理）：忽略
    if (!manager.workspaces().currentName().equals(h.workspaceName)) {
        manager.switchWorkspace(h.workspaceName); // 页签与工作空间无关：自动切回所属空间
    }
    manager.activateSession(h); // deleted/空间守卫由 activateSession 内部处理
});
```

- [ ] **Step 7: 编译验证**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn compile`
Expected: BUILD SUCCESS（无警告级错误；`Set`/`HashSet` import 已就位）

- [ ] **Step 8: Commit**

```bash
git add src/main/java/com/minion/gui/MainWindow.java
git commit -m "feat: 会话页签懒加载（打开过才有页签、×关闭不删除、与工作空间解耦）"
```

---

### Task 3: README 同步 + 全量回归

**Files:**
- Modify: `README.md`（两行行为描述）

- [ ] **Step 1: 更新 README 旧行为描述**（第 44、46 行附近，精确替换）

原文：

```
- 关闭会话页签 = 删除会话（有确认）；删除会话/切换工作空间后右侧自动清空
```

替换为：

```
- 关闭会话页签 = 仅关闭页签不删除会话（运行中弹确认、确认后中断运行）；关闭后再从左侧点击会话会重新加载；删除会话/切换工作空间后右侧自动清空
```

原文：

```
- 启动后自动激活最近会话：右侧直接显示其历史消息，直接输入会继续该会话；切换工作空间后自动激活新空间首个会话（JavaFX 页签自动选中行为，已确认接受）
```

替换为：

```
- 启动懒加载：不自动打开任何会话，右侧空白占位；点击左侧会话（或新建/发送）才加载并出现页签；页签与工作空间无关，切换工作空间页签保持不变，点击旧空间页签自动切回该空间
```

- [ ] **Step 2: 全量测试**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn test`
Expected: 全部 PASS（含 SessionManagerTest 新增 6 例、MainWindowTabTitleTest 既有 4 例）

- [ ] **Step 3: 手动验证清单**（启动 GUI `java -jar target/minion-0.1.0.jar`，逐项确认后勾选）

- [ ] 启动：无任何页签，右侧空白占位，输入框可发送（自动新建会话并出现页签）
- [ ] 左侧点击历史会话：右侧加载历史消息，页签出现
- [ ] 页签 ×（会话空闲）：直接关闭无弹窗，右侧清空（若是当前会话）；会话仍在左侧列表，再点击可重新加载
- [ ] 页签 ×（会话运行中）：弹确认框；取消无变化；确认后会话中断、页签关闭、会话保留
- [ ] 切工作空间：右侧清空、页签栏保持不变
- [ ] 点击旧空间页签：自动切回该空间并打开会话
- [ ] 左侧 ✕ 删除会话：页签同时移除（若有）
- [ ] 删除非当前工作空间：其残留页签被清理
- [ ] 运行中会话关页签后运行结束：页签不自动复活

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: 同步会话页签懒加载与关闭语义说明"
```

---

## 自查记录

- **Spec 覆盖**：设计文档 8 个改动点 → Task 1（SessionManager 3 项）、Task 2（MainWindow 8 项）、Task 3（README + 回归 + 手动清单）全覆盖；行为矩阵 10 行全部落在手动验证清单
- **类型一致性**：`deactivateSession(SessionHandle)`、`findSession(String)→SessionHandle`、`onSessionDeleted(SessionHandle)` 在 Task 1 定义、Task 2 消费，签名一致；`openedIds` 为 `Set<String>`，操作均为 `contains/add/remove`
- **无占位符**：全部步骤含实际代码或精确命令
