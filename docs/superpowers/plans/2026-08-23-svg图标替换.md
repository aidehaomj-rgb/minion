# SVG 图标替换（Win7 方块修复）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将界面 15 处 Unicode 字符图标（Win7 显示方块）替换为 Material Symbols Outlined 风格的 SVGPath 矢量图标，新建 IconFactory 集中管理。

**Architecture:** 新建 `com.minion.gui.icon.IconFactory` 静态工厂（24×24 Material path 常量 + 语义方法 + CSS 样式类 + size() 缩放 helper）；标题栏/侧栏/设置窗/正文/输入框逐处 setGraphic 替换；正文摘要行 String → Node（HBox[图标, 文本]），折叠箭头图形化；core 层 StatsLine 不动（GUI 渲染层剥离 "⏱ " 前缀）。

**Tech Stack:** JDK 8、JavaFX 8（SVGPath）、JUnit4、Git Bash（Windows，命令必须纯 ASCII——wrapper 对中文命令崩溃）。

## Global Constraints

- 设计文档：`docs/superpowers/specs/2026-08-23-svg图标替换-design.md`（已确认）
- 风格：Material Symbols Outlined 24×24 viewport；path 数据来源 https://github.com/google/material-design-icons （Apache-2.0），图标名写进注释
- 颜色一律 CSS `-fx-fill` 控制，不内联 setFill；唯一例外：MCP 状态点 4 态色（灰/绿/橙/红）内联 `setStyle("-fx-fill: ...")`，与现状 `setStyle("-fx-text-fill: ...")` 同款方式
- StatsLine（core 层）与 `StatsLineTest`/`AgentLoopTest` 中 `⏱ ` 字符串断言**一律不动**
- 纯展示层改动：不触碰消息历史/事件流/存储格式
- 语义变化仅一处：行内删除按钮 ✕ → 垃圾桶（delete 图标）
- 注释/commit 中文（conventional 格式）；**bash 命令内禁止出现任何中文**（含中文路径——git add 用 ASCII 路径或目录级）；commit 消息用 Write 工具写 `C:\Users\Administrator\AppData\Local\Temp\minion_commit_msg.txt`（该文件本会话已 Read，可直接覆盖），再 `git commit -F /tmp/minion_commit_msg.txt`
- 回归：`JAVA_HOME="E:/javame/jdk8" mvn compile` + 相关测试通过

---

### Task 1: IconFactory 图标工厂 + 测试

**Files:**
- Create: `src/main/java/com/minion/gui/icon/IconFactory.java`（新包 `com.minion.gui.icon`）
- Test: `src/test/java/com/minion/gui/icon/IconFactoryTest.java`

**Interfaces:**
- Produces（后续任务全部依赖，签名锁定）：
  - `public static void size(SVGPath icon, double px)` — 按 24×24 viewport 缩放到目标像素（scale = px/24）
  - 工厂方法（每方法返回已设样式类的 SVGPath）：`settings()`(.icon-settings)、`gear()`(.running-indicator-gear)、`remove()`(.icon-min)、`cropSquare()`(.icon-max)、`filterNone()`(.icon-restore)、`close()`(.icon-close)、`edit()`(.icon-edit)、`delete()`(.icon-delete)、`dot()`(.icon-dot)、`help()`(.icon-help)、`success()`(.icon-success)、`error()`(.icon-error)、`play()`(.icon-play)、`check()`(.icon-check)、`build()`(.icon-build)、`chevronDown()`(.icon-chevron)、`chevronRight()`(.icon-chevron)、`timer()`(.icon-timer)、`send()`(.icon-send)、`stop()`(.icon-stop)、`attachFile()`(.icon-upload)
  - 常量（供 CollapsibleText/ChatView 等组合场景直接取 path）：`CHEVRON_DOWN_PATH`、`CHEVRON_RIGHT_PATH`

- [ ] **Step 1: 写失败测试** `IconFactoryTest.java`

```java
package com.minion.gui.icon;

import javafx.scene.shape.SVGPath;
import org.junit.Test;

import static org.junit.Assert.*;

/** 图标工厂：全部方法返回非空可构造的 SVGPath 且带正确样式类；size() 缩放换算 */
public class IconFactoryTest {

    private void assertIcon(SVGPath icon, String styleClass) {
        assertNotNull(icon);
        assertNotNull(icon.getContent());
        assertFalse("path 非空: " + styleClass, icon.getContent().trim().isEmpty());
        assertTrue("样式类 " + styleClass, icon.getStyleClass().contains(styleClass));
    }

    @Test
    public void allIcons_validAndStyled() {
        assertIcon(IconFactory.settings(), "icon-settings");
        assertIcon(IconFactory.gear(), "running-indicator-gear");
        assertIcon(IconFactory.remove(), "icon-min");
        assertIcon(IconFactory.cropSquare(), "icon-max");
        assertIcon(IconFactory.filterNone(), "icon-restore");
        assertIcon(IconFactory.close(), "icon-close");
        assertIcon(IconFactory.edit(), "icon-edit");
        assertIcon(IconFactory.delete(), "icon-delete");
        assertIcon(IconFactory.dot(), "icon-dot");
        assertIcon(IconFactory.help(), "icon-help");
        assertIcon(IconFactory.success(), "icon-success");
        assertIcon(IconFactory.error(), "icon-error");
        assertIcon(IconFactory.play(), "icon-play");
        assertIcon(IconFactory.check(), "icon-check");
        assertIcon(IconFactory.build(), "icon-build");
        assertIcon(IconFactory.chevronDown(), "icon-chevron");
        assertIcon(IconFactory.chevronRight(), "icon-chevron");
        assertIcon(IconFactory.timer(), "icon-timer");
        assertIcon(IconFactory.send(), "icon-send");
        assertIcon(IconFactory.stop(), "icon-stop");
        assertIcon(IconFactory.attachFile(), "icon-upload");
    }

    @Test
    public void size_scalesToTargetPx() {
        SVGPath icon = IconFactory.success();
        IconFactory.size(icon, 12);
        assertEquals(12.0 / 24.0, icon.getScaleX(), 0.001);
        assertEquals(12.0 / 24.0, icon.getScaleY(), 0.001);
    }

    @Test
    public void chevronPaths_differ() {
        assertNotEquals(IconFactory.CHEVRON_DOWN_PATH, IconFactory.CHEVRON_RIGHT_PATH);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=IconFactoryTest`
Expected: 编译失败（IconFactory 不存在）

- [ ] **Step 3: 写 IconFactory.java**

```java
package com.minion.gui.icon;

import javafx.scene.shape.SVGPath;

/**
 * 图标工厂：集中管理全部界面矢量图标（Material Symbols Outlined 24×24 viewport，Apache-2.0，
 * 来源 https://github.com/google/material-design-icons ，注释标注图标名）。
 * 颜色一律由 CSS 样式类控制（-fx-fill），本类不设颜色；
 * 显示尺寸用 size() 按 24 viewport 缩放（默认 scale=1.0 即 24px）。
 * 动机：Win7 字体回退链缺这些 Unicode 字形（⚙✕✅❌❓▶✓⛭▾▸⏱❐□ 等显示方块），SVGPath 不依赖字体。
 */
public final class IconFactory {

    private IconFactory() { }

    // ---- 24×24 path 常量（Material Symbols / Material Icons，图标名见注释） ----

    /** settings（齿轮；自 RunningIndicator 迁移） */
    public static final String SETTINGS_PATH = "M19.14,12.94c0.04,-0.3 0.06,-0.61 0.06,-0.94c0,-0.32 -0.02,-0.64 -0.07,-0.94"
            + "l2.03,-1.58c0.18,-0.14 0.23,-0.41 0.12,-0.61l-1.92,-3.32c-0.12,-0.22 -0.37,-0.29 -0.59,-0.22"
            + "l-2.39,0.96c-0.5,-0.38 -1.03,-0.7 -1.62,-0.94L14.4,2.81c-0.04,-0.24 -0.24,-0.41 -0.48,-0.41"
            + "h-3.84c-0.24,0 -0.43,0.17 -0.47,0.41L9.25,5.35C8.66,5.59 8.12,5.92 7.63,6.29L5.24,5.33"
            + "c-0.22,-0.08 -0.47,0 -0.59,0.22L2.74,8.87C2.62,9.08 2.66,9.34 2.86,9.48l2.03,1.58"
            + "C4.84,11.36 4.8,11.69 4.8,12s0.02,0.64 0.07,0.94l-2.03,1.58c-0.18,0.14 -0.23,0.41 -0.12,0.61"
            + "l1.92,3.32c0.12,0.22 0.37,0.29 0.59,0.22l2.39,-0.96c0.5,0.38 1.03,0.7 1.62,0.94l0.36,2.54"
            + "c0.05,0.24 0.24,0.41 0.48,0.41h3.84c0.24,0 0.44,-0.17 0.47,-0.41l0.36,-2.54"
            + "c0.59,-0.24 1.13,-0.56 1.62,-0.94l2.39,0.96c0.22,0.08 0.47,0 0.59,-0.22l1.92,-3.32"
            + "c0.12,-0.22 0.07,-0.47 -0.12,-0.61L19.14,12.94zM12,15.6c-1.98,0 -3.6,-1.62 -3.6,-3.6"
            + "s1.62,-3.6 3.6,-3.6s3.6,1.62 3.6,3.6S13.98,15.6 12,15.6z";
    /** remove（最小化：短横线） */
    public static final String REMOVE_PATH = "M19 13H5v-2h14v2z";
    /** crop_square（最大化：圆角方框） */
    public static final String CROP_SQUARE_PATH = "M18 4H6c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 14H6V6h12v12z";
    /** filter_none（还原：双框） */
    public static final String FILTER_NONE_PATH = "M3 5H1v16c0 1.1.9 2 2 2h16v-2H3V5zm18-4H7c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V3c0-1.1-.9-2-2-2zm0 16H7V3h14v14z";
    /** close（叉） */
    public static final String CLOSE_PATH = "M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z";
    /** edit（铅笔） */
    public static final String EDIT_PATH = "M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04c.39-.39.39-1.02 0-1.41l-2.34-2.34c-.39-.39-1.02-.39-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z";
    /** delete（垃圾桶） */
    public static final String DELETE_PATH = "M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z";
    /** lens（实心圆点） */
    public static final String DOT_PATH = "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z";
    /** help_outline（问号圈） */
    public static final String HELP_PATH = "M11 18h2v-2h-2v2zm1-16C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm0-14c-2.21 0-4 1.79-4 4h2c0-1.1.9-2 2-2s2 .9 2 2c0 2-3 1.75-3 5h2c0-2.25 3-2.5 3-5 0-2.21-1.79-4-4-4z";
    /** check_circle（成功圈勾） */
    public static final String SUCCESS_PATH = "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z";
    /** error（失败圈叹号） */
    public static final String ERROR_PATH = "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z";
    /** play_arrow（子任务开始） */
    public static final String PLAY_PATH = "M8 5v14l11-7z";
    /** check（子任务完成） */
    public static final String CHECK_PATH = "M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z";
    /** build（扳手：工具摘要） */
    public static final String BUILD_PATH = "M22.7 19l-9.1-9.1c.9-2.3.4-5-1.5-6.9-2-2-5-2.4-7.4-1.3L9 6 6 9 1.6 4.7C.4 7.1.9 10.1 2.9 12.1c1.9 1.9 4.6 2.4 6.9 1.5l9.1 9.1c.4.4 1 .4 1.4 0l2.3-2.3c.5-.4.5-1.1.1-1.4z";
    /** expand_more（收起） */
    public static final String CHEVRON_DOWN_PATH = "M16.59 8.59L12 13.17 7.41 8.59 6 10l6 6 6-6z";
    /** chevron_right（展开） */
    public static final String CHEVRON_RIGHT_PATH = "M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z";
    /** timer（统计行耗时） */
    public static final String TIMER_PATH = "M15 1H9v2h6V1zm-4 13h2V8h-2v6zm8.03-6.61l1.42-1.42c-.43-.51-.9-.99-1.41-1.41l-1.42 1.42C16.07 4.74 14.12 4 12 4c-4.97 0-9 4.03-9 9s4.02 9 9 9 9-4.03 9-9c0-2.12-.74-4.07-1.97-5.61zM12 20c-3.87 0-7-3.13-7-7s3.13-7 7-7 7 3.13 7 7-3.13 7-7 7z";
    /** attach_file（回形针：上传，outlined 填充版） */
    public static final String ATTACH_FILE_PATH = "M16.5 6v11.5c0 2.21-1.79 4-4 4s-4-1.79-4-4V5c0-1.38 1.12-2.5 2.5-2.5s2.5 1.12 2.5 2.5v10.5c0 .55-.45 1-1 1s-1-.45-1-1V6H10v9.5c0 1.38 1.12 2.5 2.5 2.5s2.5-1.12 2.5-2.5V5c0-2.21-1.79-4-4-4S7 2.79 7 5v12.5c0 3.04 2.46 5.5 5.5 5.5s5.5-2.46 5.5-5.5V6h-1.5z";
    /** 发送上箭头（自绘 path，InputView 迁移） */
    public static final String SEND_PATH = "M12 4 L20 13 L15 13 L15 21 L9 21 L9 13 L4 13 Z";
    /** 停止方块（自绘 path，InputView 迁移） */
    public static final String STOP_PATH = "M7 7 L17 7 L17 17 L7 17 Z";

    // ---- 工厂方法（样式类在 theme.css 定义颜色） ----

    private static SVGPath create(String path, String styleClass) {
        SVGPath icon = new SVGPath();
        icon.setContent(path);
        icon.getStyleClass().add(styleClass);
        return icon;
    }

    public static SVGPath settings() { return create(SETTINGS_PATH, "icon-settings"); }
    public static SVGPath gear() { return create(SETTINGS_PATH, "running-indicator-gear"); }
    public static SVGPath remove() { return create(REMOVE_PATH, "icon-min"); }
    public static SVGPath cropSquare() { return create(CROP_SQUARE_PATH, "icon-max"); }
    public static SVGPath filterNone() { return create(FILTER_NONE_PATH, "icon-restore"); }
    public static SVGPath close() { return create(CLOSE_PATH, "icon-close"); }
    public static SVGPath edit() { return create(EDIT_PATH, "icon-edit"); }
    public static SVGPath delete() { return create(DELETE_PATH, "icon-delete"); }
    public static SVGPath dot() { return create(DOT_PATH, "icon-dot"); }
    public static SVGPath help() { return create(HELP_PATH, "icon-help"); }
    public static SVGPath success() { return create(SUCCESS_PATH, "icon-success"); }
    public static SVGPath error() { return create(ERROR_PATH, "icon-error"); }
    public static SVGPath play() { return create(PLAY_PATH, "icon-play"); }
    public static SVGPath check() { return create(CHECK_PATH, "icon-check"); }
    public static SVGPath build() { return create(BUILD_PATH, "icon-build"); }
    public static SVGPath chevronDown() { return create(CHEVRON_DOWN_PATH, "icon-chevron"); }
    public static SVGPath chevronRight() { return create(CHEVRON_RIGHT_PATH, "icon-chevron"); }
    public static SVGPath timer() { return create(TIMER_PATH, "icon-timer"); }
    public static SVGPath send() { return create(SEND_PATH, "icon-send"); }
    public static SVGPath stop() { return create(STOP_PATH, "icon-stop"); }
    public static SVGPath attachFile() { return create(ATTACH_FILE_PATH, "icon-upload"); }

    /** 按 24×24 viewport 缩放到目标像素（scale = px/24） */
    public static void size(SVGPath icon, double px) {
        double s = px / 24.0;
        icon.setScaleX(s);
        icon.setScaleY(s);
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=IconFactoryTest`
Expected: PASS（全部图标合法 + size 换算正确）

- [ ] **Step 5: Commit**

1) Write 工具覆盖 `C:\Users\Administrator\AppData\Local\Temp\minion_commit_msg.txt`：
```
feat: 新增 IconFactory 图标工厂与测试（Material 24x24 SVGPath 集中管理）

Co-Authored-By: Claude <noreply@anthropic.com>
```
2) Bash（纯 ASCII 命令）：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/icon/IconFactory.java src/test/java/com/minion/gui/icon/IconFactoryTest.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 2: 标题栏窗口按钮 SVG 化

**Files:**
- Modify: `src/main/java/com/minion/gui/TitleBar.java:40-54`（gear/min/max/close 按钮）、`:97`（toggleMaximize 换图标）

**Interfaces:**
- Consumes: `IconFactory.settings()/remove()/cropSquare()/filterNone()/close()`、`IconFactory.size(icon, px)`

- [ ] **Step 1: 替换按钮为 setGraphic**

`TitleBar.java` 构造器中 4 处：

```java
Button gear = new Button();
gear.setGraphic(icon("⚙"));   // 见下方 helper
gear.getStyleClass().add("btn-ghost");
gear.setOnAction(e -> openSettings.run());

Button min = new Button();
min.setGraphic(minimizeIcon());
min.getStyleClass().add("btn-ghost");
min.setOnAction(e -> stage.setIconified(true));

max = new Button();
max.setGraphic(maximizeIcon(true));
max.getStyleClass().add("btn-ghost");
max.setOnAction(e -> toggleMaximize()); // 手动最大化（不挡任务栏）

Button close = new Button();
close.setGraphic(closeIcon());
close.getStyleClass().add("btn-close");
close.setOnAction(e -> confirmClose.run());
```

私有静态 helper（放类内，替换原 `maxBounds` 前后均可）：

```java
/** 标题栏图标：24 viewport 缩放到 14px 显示 */
private static SVGPath sized(SVGPath icon) {
    IconFactory.size(icon, 14);
    return icon;
}
private static Node minimizeIcon() { return sized(IconFactory.remove()); }
private static Node maximizeIcon(boolean maxed) {
    return sized(maxed ? IconFactory.filterNone() : IconFactory.cropSquare());
}
private static Node closeIcon() { return sized(IconFactory.close()); }
```

- [ ] **Step 2: 修改 toggleMaximize 图标切换**

`:97` `max.setText(maxed ? "❐" : "□")` 改为：

```java
max.setGraphic(maximizeIcon(maxed));
```

- [ ] **Step 3: 补 import 并编译**

```java
import javafx.scene.Node;
import javafx.scene.shape.SVGPath;
import com.minion.gui.icon.IconFactory;
```

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile`
Expected: BUILD SUCCESS

- [ ] **Step 4: 回归 TitleBar 测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=TitleBarTest`
Expected: PASS（maxBounds 逻辑未动）

- [ ] **Step 5: Commit**

消息文件（Write 覆盖）：
```
feat: 标题栏窗口按钮改 SVG 图标（设置/最小化/最大化/还原/关闭）

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/TitleBar.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 3: 侧栏会话/工作空间按钮与当前标记 SVG 化

**Files:**
- Modify: `src/main/java/com/minion/gui/sidebar/SessionListView.java:94-101`
- Modify: `src/main/java/com/minion/gui/sidebar/WorkspaceListView.java:73,82-89`

**Interfaces:**
- Consumes: `IconFactory.edit()/delete()/dot()`、`IconFactory.size(icon, px)`

- [ ] **Step 1: SessionListView 重命名/删除按钮**

`:94-101`：

```java
Button renameBtn = new Button();
renameBtn.setGraphic(IconFactory.edit());
IconFactory.size(renameBtn.getGraphic(), 13); // getGraphic() 返回 Node；直接对 SVGPath 缩放的写法见下
renameBtn.getStyleClass().add("btn-cell");
renameBtn.setTooltip(new Tooltip("重命名"));
renameBtn.setOnAction(e -> doRename(h));
Button delBtn = new Button();
delBtn.setGraphic(IconFactory.delete());
delBtn.getStyleClass().add("btn-cell");
delBtn.setTooltip(new Tooltip("删除"));
delBtn.setOnAction(e -> doDelete(h));
```

缩放写法（避免 getGraphic 强转）：改为先取局部变量——

```java
SVGPath renameIcon = IconFactory.edit();
IconFactory.size(renameIcon, 13);
Button renameBtn = new Button();
renameBtn.setGraphic(renameIcon);
```

（同上模式处理 delBtn：`SVGPath delIcon = IconFactory.delete(); IconFactory.size(delIcon, 13);`）

- [ ] **Step 2: WorkspaceListView 修改/删除按钮**

`:82-89` 同模式：

```java
SVGPath editIcon = IconFactory.edit();   // ⚙ 修改 → settings 齿轮
IconFactory.size(editIcon, 13);
Button editBtn = new Button();
editBtn.setGraphic(editIcon);
editBtn.getStyleClass().add("btn-cell");
editBtn.setTooltip(new Tooltip("修改"));
editBtn.setOnAction(e -> doEdit(name));
SVGPath delIcon = IconFactory.delete();
IconFactory.size(delIcon, 13);
Button delBtn = new Button();
delBtn.setGraphic(delIcon);
delBtn.getStyleClass().add("btn-cell");
delBtn.setTooltip(new Tooltip("删除"));
delBtn.setOnAction(e -> doDelete(name));
```

注意：修改按钮用 `IconFactory.settings()`（齿轮，同现 ⚙ 语义），不是 edit。

- [ ] **Step 3: WorkspaceListView 当前空间标记 ● → dot 图形**

`:73`：

```java
Label nameLabel = new Label(name);
nameLabel.setGraphic(name.equals(workspaces.currentName()) ? IconFactory.dot() : null);
nameLabel.setContentDisplay(javafx.scene.control.ContentDisplay.RIGHT); // 图形在文本右侧，替代 "  ●" 文本
nameLabel.setGraphicTextGap(4);
```

dot 尺寸 8：`IconFactory.size(nameLabel.getGraphic(), 8)` 需在 setGraphic 后且仅当前空间时调用——拆开：

```java
Label nameLabel = new Label(name);
boolean current = name.equals(workspaces.currentName());
if (current) {
    SVGPath dot = IconFactory.dot();
    IconFactory.size(dot, 8);
    nameLabel.setGraphic(dot);
}
nameLabel.setContentDisplay(javafx.scene.control.ContentDisplay.RIGHT);
nameLabel.setGraphicTextGap(4);
```

- [ ] **Step 4: import 并编译**

两个文件均加：
```java
import javafx.scene.shape.SVGPath;
import com.minion.gui.icon.IconFactory;
```

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

消息文件（Write 覆盖）：
```
feat: 侧栏会话/工作空间按钮与当前标记改 SVG 图标

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/sidebar/SessionListView.java src/main/java/com/minion/gui/sidebar/WorkspaceListView.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 4: 设置窗标记 SVG 化

**Files:**
- Modify: `src/main/java/com/minion/gui/dialog/SettingsDialog.java:96-98`（模型列表当前标记）、`:277-278`（MCP 状态点）

**Interfaces:**
- Consumes: `IconFactory.dot()`、`IconFactory.size(icon, px)`

- [ ] **Step 1: 模型列表当前标记 ● → dot**

`:93-99` cell factory 中：

```java
super.updateItem(item, empty);
if (empty || item == null) {
    setGraphic(null);
    setText(null);
    return;
}
setText(item);
boolean current = item.equals(models.currentName());
if (current) {
    SVGPath dot = IconFactory.dot();
    IconFactory.size(dot, 8);
    setGraphic(dot);
    setContentDisplay(ContentDisplay.RIGHT); // 图形在文本右侧
    setGraphicTextGap(4);
} else {
    setGraphic(null);
    setContentDisplay(ContentDisplay.RIGHT);
}
```

（updateItem 每次重建：非当前时也显式置 null 防复用残留）

- [ ] **Step 2: MCP 状态点 ● → dot（4 态色内联）**

`:277-278`：

```java
SVGPath dot = IconFactory.dot();
IconFactory.size(dot, 8);
dot.setStyle("-fx-fill: " + color + ";"); // 4 态色（gray/green/orange/red）内联，与现状 setStyle 同款
```

`:294` `HBox box = new HBox(8, dot, name, meta, on);` 不变（dot 变量类型 Label → SVGPath，HBox 构造参数兼容 Node）。

- [ ] **Step 3: import 并编译**

```java
import javafx.scene.control.ContentDisplay;
import javafx.scene.shape.SVGPath;
import com.minion.gui.icon.IconFactory;
```

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile`
Expected: BUILD SUCCESS

- [ ] **Step 4: 回归 SettingsDialogTest**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=SettingsDialogTest`
Expected: PASS

- [ ] **Step 5: Commit**

消息文件（Write 覆盖）：
```
feat: 设置窗当前模型/MCP 状态标记改 SVG 圆点

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/dialog/SettingsDialog.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 5: CollapsibleText 折叠段 Node 化（chevron 图标 + Node 摘要）

**Files:**
- Modify: `src/main/java/com/minion/gui/chat/CollapsibleText.java`

**Interfaces:**
- Consumes: `IconFactory.chevronDown()/chevronRight()`、`IconFactory.size(icon, px)`、`CHEVRON_DOWN_PATH/CHEVRON_RIGHT_PATH`
- Produces（Task 6 依赖）：
  - `public CollapsibleText(Node summary, String text, boolean defaultExpanded)` — 新增 Node 构造
  - 原 `public CollapsibleText(String summary, String text, boolean defaultExpanded)` 保留（委托 Node 构造，ChatView 现有调用不破坏）

- [ ] **Step 1: 改 CollapsibleText：toggle 行 = chevron + 摘要 + 状态词**

整体替换关键部分：

```java
package com.minion.gui.chat;

import com.minion.gui.icon.IconFactory;
import javafx.scene.Node;
import javafx.scene.control.Label;
import javafx.scene.layout.HBox;
import javafx.scene.shape.SVGPath;
import javafx.scene.layout.VBox;

/** 可折叠文本段：摘要行（可点击切换）+ 内容体（MessageTextArea 高度自适应）。
 *  内容 ≥COLLAPSE_THRESHOLD 字符时默认折叠，短内容默认展开；均可手动点击切换。
 *  折叠态内容体不参与布局（managed=false），滚动只由外层 ScrollPane 统一负责。
 *  摘要行 = [chevron 图标][摘要节点][收起/展开（N 字符）]，chevron 随折叠态切换图形（SVG，不依赖字体）。 */
public class CollapsibleText extends VBox {

    /** 默认折叠阈值：内容长度 ≥ 此值默认折叠（常量可调，不设设置项） */
    public static final int COLLAPSE_THRESHOLD = 500;

    private final MessageTextArea content;
    private final HBox toggle;          // 摘要行（可点击）
    private final SVGPath chevron;      // 折叠态指示（expand_more/chevron_right）
    private final Label state;          // 「收起」/「展开（N 字符）」
    private final Node summaryNode;     // 摘要（String 构造时 = Label）
    private String text; // 流式思考段需更新，不可 final
    private boolean expanded;

    public CollapsibleText(String summary, String text, boolean defaultExpanded) {
        this(new Label(summary == null ? "" : summary), text, defaultExpanded);
    }

    public CollapsibleText(Node summary, String text, boolean defaultExpanded) {
        this.summaryNode = summary;
        this.text = text == null ? "" : text;
        this.expanded = defaultExpanded;
        getStyleClass().add("log-collapsible");
        setSpacing(2);

        chevron = new SVGPath();
        chevron.getStyleClass().add("icon-chevron");
        IconFactory.size(chevron, 12);
        state = new Label();
        state.getStyleClass().add("log-collapse-toggle");
        toggle = new HBox(4);
        toggle.getStyleClass().add("log-collapse-toggle");
        toggle.getChildren().addAll(chevron, summaryNode, state);
        toggle.setOnMouseClicked(e -> setExpanded(!expanded));

        content = new MessageTextArea(this.text);
        content.getStyleClass().add("log-body");

        getChildren().addAll(toggle, content);
        update();
    }

    /** 内容体（焦点治理等需要直接访问内部 TextArea 的场景） */
    public MessageTextArea contentArea() { return content; }

    public boolean isExpanded() { return expanded; }

    public void setExpanded(boolean v) {
        expanded = v;
        update();
    }

    /** 流式内容更新（思考段）：更新正文并强制展开——流式过程内容持续增长，
     *  折叠会遮挡实时输出；定稿折叠由 finalizeLength 按最终长度决定 */
    public void setStreamText(String t) {
        this.text = t == null ? "" : t;
        content.setStreamText(this.text);
        setExpanded(true);
    }

    /** 内容定稿：按最终长度决定折叠态（≥阈值折叠、短内容展开），流式结束后调用一次 */
    public void finalizeLength() {
        setExpanded(!shouldCollapse(text));
    }

    private void update() {
        chevron.setContent(expanded ? IconFactory.CHEVRON_DOWN_PATH : IconFactory.CHEVRON_RIGHT_PATH);
        state.setText(expanded ? "收起" : "展开（" + text.length() + " 字符）");
        content.setVisible(expanded);
        content.setManaged(expanded);
    }

    /** 默认折叠判定（纯逻辑可单测）：null/空不折叠 */
    public static boolean shouldCollapse(String text) {
        return text != null && text.length() >= COLLAPSE_THRESHOLD;
    }
}
```

原 `private final Label toggle;` 字段改为 `HBox toggle`；原构造中的 `summary` 前缀文本（"summary   ▾ 收起"）不再拼接进 state——摘要由 summaryNode 独立渲染，state 只放状态词。

- [ ] **Step 2: 编译 + 回归测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=CollapsibleTextTest,ChatViewToolBodyTest`
Expected: PASS（shouldCollapse 逻辑未动；ChatView 走 String 构造委托路径，编译兼容）

- [ ] **Step 3: Commit**

消息文件（Write 覆盖）：
```
feat: 折叠段摘要行 Node 化并图形化折叠箭头（chevron SVG）

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/chat/CollapsibleText.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 6: 正文工具区状态图标 SVG 化

**Files:**
- Modify: `src/main/java/com/minion/gui/chat/ChatView.java`（Seg 类 `:67-97`、onEvent 各分支 `:194-228`、toolCallSummary `:376-386`、STATS 分支 `:217-219`）
- Test: `src/test/java/com/minion/gui/chat/ChatViewToolBodyTest.java:51-60`

**Interfaces:**
- Consumes: `IconFactory.help()/success()/error()/play()/check()/build()/timer()`、`IconFactory.size(icon, px)`；Task 5 的 `CollapsibleText(Node, String, boolean)` 构造
- Produces: `toolCallSummary(String, Object)` 返回纯文本（去 ⛭ 前缀，单测断言同步改）

- [ ] **Step 1: Seg 支持 Node 摘要**

`ChatView.Seg` 字段 `String summary` → 加 Node 构造（保留 String 重载委托，THINK 流式段等现有调用不变）：

```java
private static class Seg {
    final Label tag;
    final MessageTextArea body;      // 焦点治理目标（collapsible 段 = 内部内容体）
    final CollapsibleText collapsible; // null = 普通段
    final StreamKind kind;
    boolean thinkFinalized; // THINK 段已按最终长度定稿（防 CONTENT 增量重复折叠覆盖手动展开）
    Seg(String tagText, String tagColorClass, String text, StreamKind kind) {
        this(tagText, tagColorClass, (Node) null, text, false, kind);
    }
    Seg(String tagText, String tagColorClass, String summary, String text,
        boolean defaultExpanded, StreamKind kind) {
        this(tagText, tagColorClass, new Label(summary == null ? "" : summary),
                text, defaultExpanded, kind);
    }
    Seg(String tagText, String tagColorClass, Node summary, String text,
        boolean defaultExpanded, StreamKind kind) {
        tag = new Label(tagText);
        tag.getStyleClass().addAll("log-tag", tagColorClass);
        if (summary == null) {
            collapsible = null;
            body = new MessageTextArea(text);
        } else {
            collapsible = new CollapsibleText(summary, text, defaultExpanded);
            body = collapsible.contentArea();
        }
        body.getStyleClass().addAll("log-body", "log-body-" + tagColorClass.substring(4));
        HBox.setHgrow(node(), Priority.ALWAYS);
        this.kind = kind;
    }
    /** 加入段行的节点：折叠段为 CollapsibleText 整体，普通段为正文 */
    Node node() { return collapsible != null ? collapsible : body; }
}
```

- [ ] **Step 2: appendCollapsible 加 Node 重载**

现有 `appendCollapsible(String, String, String, String)` 与 `(..., StreamKind)` 之后新增：

```java
/** 追加可折叠段（Node 摘要：图标+文本组合行） */
private void appendCollapsible(String tagText, String tagColorClass, Node summary, String text) {
    appendCollapsible(tagText, tagColorClass, summary, text, StreamKind.NONE);
}

private void appendCollapsible(String tagText, String tagColorClass, Node summary, String text,
                               StreamKind kind) {
    if (text == null || text.trim().isEmpty()) {
        // 无正文（子任务行/统计行）：CollapsibleText 空内容 → 只渲染摘要行
        if (empty) {
            getChildren().clear(); // 占位提示随首段出现清除
            empty = false;
        }
        Seg seg = new Seg(tagText, tagColorClass, summary, "", false, kind);
        attachFocusGovernance(seg);
        segs.add(seg);
        getChildren().add(new HBox(seg.tag, seg.node()));
        trimHead();
        return;
    }
    if (empty) {
        getChildren().clear();
        empty = false;
    }
    boolean expanded = kind == StreamKind.THINK ? true : defaultExpanded(text);
    Seg seg = new Seg(tagText, tagColorClass, summary, text, expanded, kind);
    attachFocusGovernance(seg);
    segs.add(seg);
    getChildren().add(new HBox(seg.tag, seg.node()));
    trimHead();
}
```

（原 String 版 appendCollapsible 内部改为委托：`appendCollapsible(tagText, tagColorClass, new Label(summary), text, kind)`——但原实现有"空文本走 append()"的分支，委托后空文本走 Node 版空分支，渲染行为等价：仅摘要行。）

- [ ] **Step 3: onEvent 各分支换 Node 摘要**

`TOOL_CALL` 分支（`:194-202`）：

```java
case TOOL_CALL: {
    if ("AskUserQuestion".equals(e.text)) {
        appendCollapsible("【工具】", "log-tool", questionSummary(), askQuestionOf(e.data));
    } else {
        appendCollapsible("【工具】", "log-tool",
                toolSummary(toolCallSummary(e.text, e.data)), toolCallBody(e.text, e.data));
    }
    break;
}
```

`TOOL_RESULT` 分支（`:203-210`）：

```java
case TOOL_RESULT: {
    String data = e.data == null ? "" : e.data.toString();
    boolean ok = data.startsWith("ok");
    Node summary = ok
            ? statusSummary(IconFactory.success(), e.text + " 成功")
            : statusSummary(IconFactory.error(), e.text + " 失败");
    appendCollapsible(ok ? "【工具】" : "【系统】", ok ? "log-tool" : "log-error",
            summary, toolResultBody(data));
    break;
}
```

`SUB_AGENT_START/DONE`（`:223-228`）：

```java
case SUB_AGENT_START:
    appendCollapsible("【工具】", "log-tool", statusSummary(IconFactory.play(), "子任务: " + e.text), "");
    break;
case SUB_AGENT_DONE:
    appendCollapsible("【工具】", "log-tool", statusSummary(IconFactory.check(), "子任务完成: " + e.text), "");
    break;
```

`STATS`（`:217-219`）：剥离 "⏱ " 前缀加 timer 图标：

```java
case STATS:
    if (e.text != null && e.text.startsWith("⏱ ")) {
        appendCollapsible("【系统】", "log-sys",
                statusSummary(IconFactory.timer(), e.text.substring(2)), "");
    } else {
        append("【系统】", "log-sys", e.text, StreamKind.NONE); // 无前缀（旧数据）原样整行
    }
    break;
```

新增私有 helper（放 `toolCallSummary` 附近）：

```java
/** 状态图标 + 文本 摘要行（14px 图标） */
private static Node statusSummary(SVGPath icon, String text) {
    IconFactory.size(icon, 14);
    HBox row = new HBox(6);
    Label label = new Label(text);
    label.getStyleClass().add("log-summary-text");
    row.getChildren().addAll(icon, label);
    row.setAlignment(javafx.geometry.Pos.CENTER_LEFT);
    return row;
}
/** 提问摘要：问号图标 + 文案 */
private static Node questionSummary() {
    return statusSummary(IconFactory.help(), "模型向你提问");
}
/** 工具摘要：扳手图标 + 纯文本 */
private static Node toolSummary(String text) {
    return statusSummary(IconFactory.build(), text);
}
```

- [ ] **Step 4: toolCallSummary 去 ⛭ 前缀**

`:376-386`：

```java
/** 工具调用摘要文本：Edit/Write 附带路径，其余仅名称（纯文本；图标由调用方组合，package-private 供单测） */
static String toolCallSummary(String name, Object data) {
    if ("Edit".equals(name) || "Write".equals(name)) {
        try {
            JsonObject o = JsonParser.parseString(data == null ? "{}" : data.toString()).getAsJsonObject();
            if (o.has("path") && !o.get("path").isJsonNull()) {
                return name + " → " + o.get("path").getAsString();
            }
        } catch (Exception ignored) { }
    }
    return name;
}
```

- [ ] **Step 5: 更新 ChatViewToolBodyTest**

`:51-60` 两断言：

```java
@Test
public void toolCallSummary_edit_includesPath() {
    assertEquals("Edit → src/foo.java",
            ChatView.toolCallSummary("Edit", "{\"path\":\"src/foo.java\"}"));
}

@Test
public void toolCallSummary_otherTool_nameOnly() {
    assertEquals("Bash", ChatView.toolCallSummary("Bash", "{\"command\":\"ls\"}"));
}
```

- [ ] **Step 6: import 并编译测试**

ChatView 加：
```java
import com.minion.gui.icon.IconFactory;
import javafx.scene.Node;
import javafx.scene.control.Label;
import javafx.scene.layout.HBox;
import javafx.scene.shape.SVGPath;
```

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ChatViewToolBodyTest,ChatViewStreamBufferTest`
Expected: PASS（toolCallSummary 新断言 + 原有 body 断言）

- [ ] **Step 7: Commit**

消息文件（Write 覆盖）：
```
feat: 正文工具区状态图标 SVG 化（提问/成功/失败/子任务/工具摘要/统计）

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/chat/ChatView.java src/test/java/com/minion/gui/chat/ChatViewToolBodyTest.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 7: 输入框块删除图标 + 既有 SVG 迁移工厂

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`（字段声明区 arrowIcon/stopIcon/uploadIcon、`:99-107` 初始化、`:307` 块删除按钮）

**Interfaces:**
- Consumes: `IconFactory.send()/stop()/attachFile()/close()`、`IconFactory.size(icon, px)`

- [ ] **Step 1: 字段初始化改工厂**

字段声明区（构造器外 `private final SVGPath arrowIcon = new SVGPath();` 等三处）改为：

```java
private final SVGPath arrowIcon = IconFactory.send();
private final SVGPath stopIcon = IconFactory.stop();
private final SVGPath uploadIcon = IconFactory.attachFile(); // outlined attach_file 填充版
```

（样式类已由工厂设置：icon-send/icon-stop/icon-upload，CSS 类名不变）

- [ ] **Step 2: 删除原 setContent + 样式类赋值**

`:99-106` 删除三处 `setContent(...)` 与 `getStyleClass().add(...)`（工厂已做），保留注释说明：

```java
// 上箭头（Claude Code 同款语义：可发送）；方块 = 终止；回形针 = 上传（均为 IconFactory 集中管理）
```

- [ ] **Step 3: 块删除 ✕ → close 图标**

`:307`：

```java
SVGPath closeIcon = IconFactory.close();
IconFactory.size(closeIcon, 11);
Button close = new Button();
close.setGraphic(closeIcon);
```

（原 `new Button("✕")` 的样式类/事件绑定保持不变）

- [ ] **Step 4: import 并编译**

```java
import com.minion.gui.icon.IconFactory;
```

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputViewKeyTest,InputViewButtonTest`
Expected: PASS

- [ ] **Step 5: Commit**

消息文件（Write 覆盖）：
```
feat: 输入框块删除图标与发送/停止/上传图标迁移 IconFactory

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/input/InputView.java && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 8: theme.css 图标样式与 hover 规则

**Files:**
- Modify: `src/resource/theme/theme.css`

**Interfaces:**
- Consumes: Task 1-7 全部 `icon-*` 样式类名

- [ ] **Step 1: 新增图标颜色与 hover 规则**

`theme.css` 中 `.btn-ghost`（`:53-59`）之后插入标题栏图标块：

```css
/* 标题栏窗口按钮图标（SVGPath 不继承 -fx-text-fill，显式上色；hover 随按钮态变） */
.icon-settings { -fx-fill: #a8b0bb; }
.icon-min { -fx-fill: #a8b0bb; }
.icon-max { -fx-fill: #a8b0bb; }
.icon-restore { -fx-fill: #a8b0bb; }
.icon-close { -fx-fill: #a8b0bb; }
.btn-ghost:hover .icon-settings { -fx-fill: #f0f2f6; }
.btn-ghost:hover .icon-min { -fx-fill: #f0f2f6; }
.btn-ghost:hover .icon-max { -fx-fill: #f0f2f6; }
.btn-ghost:hover .icon-restore { -fx-fill: #f0f2f6; }
.btn-close:hover .icon-close { -fx-fill: white; }
```

`.btn-cell`（`:274-283`）附近新增侧栏图标规则并更新注释：

```css
/* 侧栏行内悬停小按钮（会话/工作空间 编辑、删除；SVG 图标） */
.btn-cell {
    -fx-background-color: transparent;
    -fx-text-fill: #a8b0bb;
    -fx-background-radius: 4;
    -fx-padding: 2 6 2 6;
    -fx-cursor: hand;
}
.btn-cell:hover { -fx-text-fill: #f0f2f6; -fx-background-color: #2a2f3a; }
.icon-edit { -fx-fill: #a8b0bb; }
.icon-delete { -fx-fill: #a8b0bb; }
.btn-cell:hover .icon-edit { -fx-fill: #4f8cff; }
.btn-cell:hover .icon-delete { -fx-fill: #e05b5b; }
```

（原 `-fx-min-width: 34px` 及字形宽度注释一并删除——SVG 图标按钮宽度由图形+padding 撑起，不再需要保字形完整。若担心视觉回归可保留 min-width 26px，二选一，推荐直接删除。）

文件末尾（或 `.running-indicator` 附近）新增正文/标记图标块：

```css
/* 当前标记圆点 */
.icon-dot { -fx-fill: #4f8cff; }

/* 正文状态图标（深色主题语义色） */
.icon-help { -fx-fill: #e6a23c; }
.icon-success { -fx-fill: #4caf50; }
.icon-error { -fx-fill: #e05b5b; }
.icon-play { -fx-fill: #4f8cff; }
.icon-check { -fx-fill: #4caf50; }
.icon-build { -fx-fill: #8a8f98; }
.icon-chevron { -fx-fill: #8a8f98; }
.icon-timer { -fx-fill: #8a8f98; }
/* 正文摘要行文本（跟随图标语义色主题，字号小一号） */
.log-summary-text { -fx-text-fill: #d3d7de; -fx-font-size: 13px; }
```

- [ ] **Step 2: .icon-upload 改填充版样式**

`:98-105` 替换：

```css
/* 上传按钮（输入框底部操作行左）：回形针图标（outlined 填充版） */
.btn-upload { -fx-background-color: transparent; -fx-background-radius: 6; -fx-cursor: hand; }
.icon-upload {
    -fx-fill: #a8b0bb;
}
.btn-upload:hover .icon-upload { -fx-fill: #f0f2f6; }
```

（原 fill:transparent + stroke 样式删除——attachFile 已是填充型 path）

- [ ] **Step 3: 检查 .log-collapse-toggle 样式仍生效**

`theme.css` 中查找 `.log-collapse-toggle`，确认其样式（-fx-text-fill 等）对 HBox（toggle 行）与 Label（state）均适用；若选择器含 `.log-collapse-toggle:hover` 之类的后代选择器，将 state Label 也加 `log-collapse-toggle` 类（Task 5 已加）。

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile`（CSS 不参与编译，仅验证无代码破坏）
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

消息文件（Write 覆盖）：
```
style: theme.css 图标样式与 hover 规则（SVG 颜色/语义色/主色红蓝）

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/resource/theme/theme.css && git commit -F /tmp/minion_commit_msg.txt
```

---

### Task 9: 文档同步 + 全量回归

**Files:**
- Modify: `README.md`
- Modify: `docs/ARCHITECTURE.md`
- Modify: `src/main/java/com/minion/gui/RunningIndicator.java`（齿轮 path 迁移到 IconFactory.gear()）

- [ ] **Step 1: RunningIndicator 齿轮迁移**

`RunningIndicator.java`：删除 `GEAR_PATH` 常量（`:33-43`），`gear.setContent(GEAR_PATH)`（`:55`）改为：

```java
gear = IconFactory.gear(); // 齿轮图标（IconFactory 集中管理，样式类 running-indicator-gear）
```

字段声明 `private final SVGPath gear = new SVGPath();` 改为 `private final SVGPath gear = IconFactory.gear();`，删除构造器内 `gear.setContent(GEAR_PATH)` 与 `gear.getStyleClass().add("running-indicator-gear")`（工厂已设）。

- [ ] **Step 2: README 图标说明**

README.md 末尾「文档与约定」或界面说明处加一段：

```markdown
## 图标

界面图标统一为 Material Symbols Outlined 风格 SVG（JavaFX SVGPath，Apache-2.0），
集中在 `com.minion.gui.icon.IconFactory` 管理（颜色由 theme.css 的 icon-* 样式类控制）。
选型原因：Win7 字体回退链缺 Unicode 图标字形（显示方块），SVGPath 不依赖字体、任意 Windows 版本一致渲染。
```

- [ ] **Step 3: ARCHITECTURE.md 包结构补充**

`gui/` 段落后补一行：

```markdown
│   ├── icon/             IconFactory（SVG 图标集中管理：Material path + 样式类 + size 缩放）
```

- [ ] **Step 4: 全量构建 + 测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn clean test`
Expected: BUILD SUCCESS，全部测试 PASS（含 RunningIndicatorTest、StatsLineTest、AgentLoopTest 中 ⏱ 断言——均未改动）

- [ ] **Step 5: Commit（含中文文件名用目录级 add）**

消息文件（Write 覆盖）：
```
docs: 图标方案文档同步与 RunningIndicator 齿轮迁移 IconFactory

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add src/main/java/com/minion/gui/RunningIndicator.java README.md docs/ARCHITECTURE.md && git commit -F /tmp/minion_commit_msg.txt
```

- [ ] **Step 6: 提交计划文档（中文文件名，目录级 add）**

消息文件（Write 覆盖）：
```
docs: SVG 图标替换实施计划

Co-Authored-By: Claude <noreply@anthropic.com>
```
Bash：
```bash
cd "e:/javame/code/code2/minion" && git add docs/superpowers/plans/ && git commit -F /tmp/minion_commit_msg.txt
```

- [ ] **Step 7: 人工验证（GUI）**

运行 `java -jar target/minion-0.1.0.jar`，核对：
1. 标题栏 5 个按钮图标正常（设置 hover 变白、关闭 hover 红底白叉）
2. 侧栏会话/工作空间行内悬停按钮（铅笔 hover 蓝、垃圾桶 hover 红）、当前空间名右侧主色圆点
3. 设置窗：模型列表当前项右侧圆点；MCP 页状态点四色正常
4. 正文：工具调用显示扳手摘要、成功绿圈勾/失败红圈叹、提问琥珀问号、子任务蓝箭头/绿勾、统计行计时图标；折叠段 chevron 随展开/收起切换
5. 输入框：块删除小叉、发送/停止/回形针图标正常
6. 长路径 Edit 摘要不换行溢出（HBox 宽度约束，必要时调 .log-summary-text）

---

## 自查记录（写入时执行）

- **Spec 覆盖**：图标清单 21 项 → Task 2（标题栏 5）、Task 3（侧栏 5）、Task 4（设置 2）、Task 6（正文 6+统计）、Task 7（块删除 1）、Task 5（折叠箭头 1）、Task 1（工厂/迁移基础）✓；StatsLine 不动 ✓（Task 6 只改 GUI 渲染）；删除改垃圾桶 ✓（Task 3）
- **占位符**：全部步骤含具体代码与命令，无 TBD ✓
- **类型一致性**：`IconFactory.size(SVGPath, double)` 全任务同签名；`CollapsibleText(Node, String, boolean)` Task 5 定义、Task 6 Seg 使用；`appendCollapsible` Node 重载 Task 6 定义并自用 ✓
