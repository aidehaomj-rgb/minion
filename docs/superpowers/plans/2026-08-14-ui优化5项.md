# UI 优化 5 项 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 5 项 GUI 优化：侧栏激活微亮框、页签行固定、输入框加高上移、删除分区标题文案、最大化不挡任务栏。

**Architecture:** 全部为 JavaFX 8 界面层改动，涉及 5 个文件（theme.css、SessionListView、MainWindow、InputView、TitleBar）。核心修复是限制 ScrollPane 的 prefHeight 固定 + GridPane 行 vgrow（防页签行被压缩消失，已用探针验证）。最大化改为手动管理 bounds（无边框窗口 setMaximized 在 Windows 上覆盖任务栏）。

**Tech Stack:** Java 8 + JavaFX 8（JDK 自带 jfxrt）、Maven 单模块。构建必须 JDK8：`JAVA_HOME="D:/javame/jdk1.8" mvn ...`（本机实际 JDK8 路径，非 CLAUDE.md 中已失效的 E:/javame/jdk8）。

## Global Constraints

- JDK 8 兼容；不新增依赖
- 注释、commit 均用中文（commit 用 conventional 格式）
- 资源目录是 `src/resource`（非 src/main/resources）
- 完成前 `mvn compile` + 现有测试通过
- 本项目无 JavaFX 单测基础设施（Zulu 8 的 Platform 无 startup 方法），GUI 行为通过手工验证；每任务不得破坏现有测试

---

### Task 1: 侧栏列表条目激活微亮框

**Files:**
- Modify: `src/resource/theme/theme.css:108`
- Modify: `src/main/java/com/minion/gui/sidebar/SessionListView.java:64-66`

**Interfaces:**
- Consumes: `SessionManager.currentSession()`（返回 `SessionHandle` 或 null，MainWindow 已使用）
- Produces: 无（纯界面调整）

- [ ] **Step 1: CSS 选中背景改页签同款微亮色**

`src/resource/theme/theme.css` 第 108 行：

```css
/* 修改前 */
.list-view .list-cell:selected { -fx-background-color: #2a3344; -fx-text-fill: #f0f2f6; }
/* 修改后 */
.list-view .list-cell:selected { -fx-background-color: #232a38; -fx-text-fill: #f0f2f6; }
```

- [ ] **Step 2: 会话列表 refresh 同步选中当前激活会话**

`src/main/java/com/minion/gui/sidebar/SessionListView.java`，`refresh()` 方法（第 64-66 行）改为：

```java
    public void refresh() {
        Platform.runLater(() -> {
            getItems().setAll(manager.sessions());
            SessionHandle cur = manager.currentSession();
            if (cur != null) getSelectionModel().select(cur); // 选中=激活：微亮框即激活指示（删除会话等路径后重新同步）
        });
    }
```

（`SessionHandle` 已 import，无需新增。）

- [ ] **Step 3: 编译 + 现有测试通过**

Run: `cd "d:/javame/code2/minion" && JAVA_HOME="D:/javame/jdk1.8" mvn -o test -q`
Expected: BUILD SUCCESS（全部测试通过）

- [ ] **Step 4: 手工验证**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn -o package -q && minion.bat`
Expected: 侧栏激活会话条目背景为页签同款微亮色 `#232a38`；切换工作空间后当前工作空间条目同样微亮；未选中条目不变。

- [ ] **Step 5: Commit**

```bash
git add src/resource/theme/theme.css src/main/java/com/minion/gui/sidebar/SessionListView.java
git commit -m "feat: 侧栏列表条目激活背景微亮（页签同款 #232a38，会话列表选中=激活）"
```

---

### Task 2: 页签行固定一行（消息增多不再消失）

**Files:**
- Modify: `src/main/java/com/minion/gui/MainWindow.java:145-148`
- Modify: `src/main/java/com/minion/gui/MainWindow.java:169-177`
- Modify: `src/main/java/com/minion/gui/MainWindow.java:28-41`（import 区）

**Interfaces:**
- Consumes: 无
- Produces: 无（根因已由探针验证：ScrollPane prefHeight 随内容增长挤压 tabsBar）

- [ ] **Step 1: 固定 chatScroll 的 prefHeight**

`src/main/java/com/minion/gui/MainWindow.java` 第 145-148 行：

```java
        chatScroll = new ScrollPane();
        chatScroll.setFitToWidth(true);
        chatScroll.setPrefHeight(200); // 固定 pref：否则 prefHeight 随消息内容增长，挤压右侧 VBox 把页签行压扁（探针验证）
        chatScroll.setContent(new Region()); // 激活会话后换 ChatView
```

- [ ] **Step 2: GridPane 行 vgrow 撑满可用空间**

`src/main/java/com/minion/gui/MainWindow.java` 第 169-177 行的 GridPane 配置处，`center.getColumnConstraints().addAll(leftCol, rightCol);` 之后加：

```java
        RowConstraints row = new RowConstraints();
        row.setVgrow(Priority.ALWAYS); // 行撑满可用空间：chatScroll pref 固定后防底部留白
        center.getRowConstraints().add(row);
```

import 区（第 34 行 `import javafx.scene.layout.Region;` 附近）新增：

```java
import javafx.scene.layout.RowConstraints;
```

- [ ] **Step 3: 编译 + 现有测试通过**

Run: `cd "d:/javame/code2/minion" && JAVA_HOME="D:/javame/jdk1.8" mvn -o test -q`
Expected: BUILD SUCCESS

- [ ] **Step 4: 手工验证**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn -o package -q && minion.bat`
Expected: 同一会话持续对话至消息较多时，页签行始终为固定一行（不压扁、不消失）；滚轮滚动消息不影响页签行；聊天区占满页签与输入框之间的空间，无底部空白。

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/minion/gui/MainWindow.java
git commit -m "fix: 页签行固定一行（ScrollPane pref 固定 + GridPane 行 vgrow，消息增多不再压扁页签）"
```

---

### Task 3: 输入框加高一行、上移半行

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java:50`
- Modify: `src/main/java/com/minion/gui/input/InputView.java:55`

**Interfaces:**
- Consumes: 无
- Produces: 无

- [ ] **Step 1: prefRowCount 2 → 3**

`src/main/java/com/minion/gui/input/InputView.java` 第 55 行：

```java
        input.setPrefRowCount(3); // 加一行
```

- [ ] **Step 2: 底部 padding 12 → 0（输入区上移半行）**

`src/main/java/com/minion/gui/input/InputView.java` 第 50 行：

```java
        setPadding(new Insets(12, 16, 0, 16)); // 底部 0：整体上移半行（半行≈12px）
```

- [ ] **Step 3: 编译 + 现有测试通过**

Run: `cd "d:/javame/code2/minion" && JAVA_HOME="D:/javame/jdk1.8" mvn -o test -q`
Expected: BUILD SUCCESS

- [ ] **Step 4: 手工验证**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn -o package -q && minion.bat`
Expected: 输入框为 3 行高、整体比原来上移约半行（底部贴窗沿）；输入多行文本时高度自适应上限不变。

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/minion/gui/input/InputView.java
git commit -m "feat: 输入框加高一行并上移半行"
```

---

### Task 4: 删除「会话管理/工作空间」分区标题

**Files:**
- Modify: `src/main/java/com/minion/gui/MainWindow.java:113-114`
- Modify: `src/main/java/com/minion/gui/MainWindow.java:129-130`
- Modify: `src/main/java/com/minion/gui/MainWindow.java:140`

**Interfaces:**
- Consumes: 无
- Produces: 无

- [ ] **Step 1: 删除两个标题 Label 及容器引用**

`src/main/java/com/minion/gui/MainWindow.java`：

删除第 113-114 行：

```java
        Label sessionTitle = new Label("会话管理");
        sessionTitle.getStyleClass().add("section-title");
```

删除第 129-130 行：

```java
        Label wsTitle = new Label("工作空间");
        wsTitle.getStyleClass().add("section-title");
```

第 140 行：

```java
        // 修改前
        sidebar.getChildren().setAll(sessionTitle, sessionBox, wsTitle, wsBox);
        // 修改后（VBox(8) 间距自然分隔上下两区，不加分隔线）
        sidebar.getChildren().setAll(sessionBox, wsBox);
```

- [ ] **Step 2: 编译 + 现有测试通过**

Run: `cd "d:/javame/code2/minion" && JAVA_HOME="D:/javame/jdk1.8" mvn -o test -q`
Expected: BUILD SUCCESS（`Label` import 仍在 MainWindow 其他位置使用，勿删）

- [ ] **Step 3: 手工验证**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn -o package -q && minion.bat`
Expected: 侧栏无「会话管理」「工作空间」文案，布局为：＋新建会话按钮 / 会话列表 / ＋新建工作空间按钮 / 工作空间列表，无异常空隙。

- [ ] **Step 4: Commit**

```bash
git add src/main/java/com/minion/gui/MainWindow.java
git commit -m "feat: 侧栏删除会话管理/工作空间分区标题及占用空间"
```

---

### Task 5: 最大化不挡 Windows 任务栏（手动最大化）

**Files:**
- Modify: `src/main/java/com/minion/gui/TitleBar.java`（全部改造点）
- Test: `src/test/java/com/minion/gui/TitleBarTest.java`（新增）

**Interfaces:**
- Consumes: `javafx.stage.Stage`（构造参数）、`javafx.stage.Screen.getPrimary().getVisualBounds()`（Rectangle2D，已排除任务栏）
- Produces: `TitleBar` 内部字段 `maxed`（boolean）、`restore`（Rectangle2D，null=未最大化）；方法 `toggleMaximize()`（无参、void，内部维护状态并切换窗口 bounds）；静态 `maxBounds(Rectangle2D visual)` 供单测
- 静态方法 `maxBounds` 被 MainWindow 不依赖，仅测试用

- [ ] **Step 1: 写失败测试**

新建 `src/test/java/com/minion/gui/TitleBarTest.java`：

```java
package com.minion.gui;

import javafx.geometry.Rectangle2D;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

/** 手动最大化的目标 bounds 计算（无边框窗口 setMaximized 在 Windows 上覆盖任务栏，改手动定位） */
public class TitleBarTest {

    @Test
    public void maxBounds_returnsVisualBounds() {
        Rectangle2D vb = new Rectangle2D(0, 0, 1920, 1040); // visualBounds 已排除任务栏
        assertEquals(vb, TitleBar.maxBounds(vb));
    }

    @Test
    public void maxBounds_preservesOriginAndSize() {
        Rectangle2D vb = new Rectangle2D(100, 50, 1600, 900);
        Rectangle2D r = TitleBar.maxBounds(vb);
        assertEquals(100, r.getMinX(), 0.001);
        assertEquals(50, r.getMinY(), 0.001);
        assertEquals(1600, r.getWidth(), 0.001);
        assertEquals(900, r.getHeight(), 0.001);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "d:/javame/code2/minion" && JAVA_HOME="D:/javame/jdk1.8" mvn -o test -Dtest=TitleBarTest -q`
Expected: FAIL — 找不到符号 `TitleBar.maxBounds`

- [ ] **Step 3: 改造 TitleBar 实现手动最大化**

`src/main/java/com/minion/gui/TitleBar.java` 整体替换为：

```java
package com.minion.gui;

import javafx.geometry.Rectangle2D;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.HBox;
import javafx.scene.layout.Priority;
import javafx.scene.layout.Region;
import javafx.stage.Screen;
import javafx.stage.Stage;

/**
 * 自绘标题栏（无边框窗口）：应用名 | 模型标签 | 留白（弹性）| ⚙ 设置 | — 最小化 | □ 最大化 | ✕ 关闭。
 * 拖动标题栏移动窗口；双击空白区切换最大化。关闭走 confirmClose（与系统关闭共用退出确认）。
 * 最大化不用 stage.setMaximized（无边框窗口在 Windows 上会覆盖任务栏），改手动定位到
 * Screen.getPrimary().getVisualBounds()（系统已排除任务栏区域），还原时恢复记录的原 bounds。
 */
public class TitleBar extends HBox {

    private final Stage stage;
    private double dragX;
    private double dragY;
    private boolean maxed;          // 手动最大化状态（自管理，不依赖系统窗口状态）
    private Rectangle2D restore;    // 最大化前的窗口 bounds（null=未最大化）
    private final Button max;

    public TitleBar(Stage stage, Label modelLabel,
                    Runnable openSettings, Runnable confirmClose) {
        this.stage = stage;
        this.modelLabel = modelLabel; // 设置窗关闭后 MainWindow 刷新顶部模型名用
        getStyleClass().add("topbar");
        setSpacing(10);

        Label app = new Label("minion");
        app.getStyleClass().add("topbar-title");

        Region spacer = new Region();
        HBox.setHgrow(spacer, Priority.ALWAYS); // 原页签区弹性占位：模型名与右侧按钮之间留白

        Button gear = new Button("⚙");
        gear.getStyleClass().add("btn-ghost");
        gear.setOnAction(e -> openSettings.run());

        Button min = new Button("—");
        min.getStyleClass().add("btn-ghost");
        min.setOnAction(e -> stage.setIconified(true));

        max = new Button("□");
        max.getStyleClass().add("btn-ghost");
        max.setOnAction(e -> toggleMaximize()); // 手动最大化（不挡任务栏）

        Button close = new Button("✕");
        close.getStyleClass().add("btn-close");
        close.setOnAction(e -> confirmClose.run());

        getChildren().addAll(app, modelLabel, spacer, gear, min, max, close);

        // 拖动移动窗口（记录按下偏移，拖拽按屏幕坐标差值移动）
        setOnMousePressed(e -> {
            dragX = e.getScreenX() - stage.getX();
            dragY = e.getScreenY() - stage.getY();
        });
        setOnMouseDragged(e -> {
            stage.setX(e.getScreenX() - dragX);
            stage.setY(e.getScreenY() - dragY);
        });
        // 双击标题栏空白处切换最大化：只认标题栏自身与其两个 Label（应用名/模型名）
        setOnMouseClicked(e -> {
            if (e.getClickCount() == 2
                    && (e.getTarget() == this || e.getTarget() == app || e.getTarget() == modelLabel)) {
                toggleMaximize();
            }
        });
    }

    /** 手动最大化目标 bounds：直接使用可视区（Windows 已排除任务栏；本方法独立成静态以便单测） */
    static Rectangle2D maxBounds(Rectangle2D visual) {
        return visual;
    }

    /** 切换最大化：未最大化 → 记录当前 bounds 并移动到可视区；已最大化 → 还原记录 */
    private void toggleMaximize() {
        if (maxed) {
            stage.setX(restore.getMinX());
            stage.setY(restore.getMinY());
            stage.setWidth(restore.getWidth());
            stage.setHeight(restore.getHeight());
        } else {
            restore = new Rectangle2D(stage.getX(), stage.getY(), stage.getWidth(), stage.getHeight());
            Rectangle2D vb = maxBounds(Screen.getPrimary().getVisualBounds());
            stage.setX(vb.getMinX());
            stage.setY(vb.getMinY());
            stage.setWidth(vb.getWidth());
            stage.setHeight(vb.getHeight());
        }
        maxed = !maxed;
        max.setText(maxed ? "❐" : "□");
    }

    /** 模型标签（MainWindow 设置窗关闭后刷新顶部模型名用） */
    public Label modelLabel() { return modelLabel; }

    private final Label modelLabel;
}
```

注意：原第 44-45 行 `stage.maximizedProperty().addListener(...)` 随旧逻辑一并删除（不再使用系统最大化，无该事件）。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "d:/javame/code2/minion" && JAVA_HOME="D:/javame/jdk1.8" mvn -o test -q`
Expected: BUILD SUCCESS（TitleBarTest 2 个用例 + 现有测试全部通过）

- [ ] **Step 5: 手工验证**

Run: `JAVA_HOME="D:/javame/jdk1.8" mvn -o package -q && minion.bat`
Expected: 点 □ 最大化后窗口占满除任务栏外的屏幕（任务栏可见）；图标变 ❐；再点还原到原位置/大小、图标回 □；双击标题栏空白区同样切换；最大化后拖动/缩放不受影响。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/minion/gui/TitleBar.java src/test/java/com/minion/gui/TitleBarTest.java
git commit -m "feat: 最大化改手动定位不挡 Windows 任务栏（visualBounds + 还原记录）"
```

---

## 自检说明

- 规格覆盖：5 项需求各对应 Task 1-5，无遗漏；涉及文件与规格「涉及文件」表一致
- 根因（ScrollPane prefHeight 增长）与修复已由临时探针在规划阶段验证，探针已删除
- 本计划无 TDD 空白：Task 5 含纯逻辑单测（maxBounds）；Task 1-4 为纯界面调整（CSS/容器/尺寸），无纯逻辑可测，以编译+现有测试+手工验证兜底
