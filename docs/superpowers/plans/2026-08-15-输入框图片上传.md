# 输入框图片上传 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 输入框新增图片上传，图片以 OpenAI 标准 content 数组传给模型（qwen-vl 支持），UI 改为底部操作行（上传左/发送右）、发送按钮改红。

**Architecture:** 图片 base64 随 `Message.images`（`List<ImagePart>`）流转：InputView 上传成 IMAGE chip → SessionManager/AgentLoop 传图片列表 → `toApiJson()` 带图时 content 输出 `[{type:text},{type:image_url}]` 数组，无图零回归（纯字符串）。图片随 Gson 落盘，重启后仍可回传。

**Tech Stack:** Java 8、JavaFX 8、Gson、JUnit4、Maven。

## Global Constraints

- 无新依赖；JDK 8 兼容（`java.util.Base64` 可用）。
- 单张图片 ≤5MB（`ImagePart.MAX_FILE_BYTES`），每条消息 ≤3 张（`ImagePart.MAX_IMAGES`）。
- 无图时 `Message.toApiJson()` 输出与现状完全一致（content 纯字符串）——零回归保护。
- 模型视觉能力不检测、不拦截；API 报错走现有 notifyError 展示。
- 聊天区不渲染图片本体：事件文本拼「图片：<名> 」占位（`ImagePart.displayText`）。
- 回答（ANSWER）模式带图：点击发送提示「回答模式暂不支持图片」，不发送。
- 提交规约：commit 消息用 Write 写入 `.git/COMMIT_MSG.tmp` 后 `git commit -F .git/COMMIT_MSG.tmp` 并删除；**git 命令行禁止出现非 ASCII 字符**（bash wrapper 崩溃）——Java 文件名全 ASCII 可直接 `git add <路径>`，中文名文档用目录路径 add。
- 只 add 本任务涉及文件（并行 agent 共享仓库）。

---

### Task 1: ImagePart 模型 + Message 图片字段与 content 数组（core/llm）

**Files:**
- Create: `src/main/java/com/minion/core/llm/ImagePart.java`
- Modify: `src/main/java/com/minion/core/llm/Message.java`
- Test: `src/test/java/com/minion/core/llm/MessageTest.java`

**Interfaces:**
- Produces: `ImagePart`（字段 `mime`/`base64`/`name`；静态常量 `MAX_FILE_BYTES`、`MAX_IMAGES`、`IMAGE_TOKENS=500`；静态 `displayText(List<ImagePart>, String)`）；`Message.images` 字段、`Message.userWithImages(String, List<ImagePart>)`、`Message.userSupplement(String, List<ImagePart>)`。
- Consumes: 无（Task 3/4/5 依赖本任务产物）。

- [ ] **Step 1: 写失败测试**（MessageTest.java 追加 3 个测试）

```java
/** 图片消息：content 输出 OpenAI 标准数组（text + image_url），data URI 带 mime */
@Test
public void userWithImages_emitsContentArray() {
    ImagePart img = new ImagePart();
    img.mime = "image/png";
    img.base64 = "QUJD";
    img.name = "截图.png";
    Message m = Message.userWithImages("看这张图", Collections.singletonList(img));
    JsonObject api = m.toApiJson();
    JsonArray parts = api.getAsJsonArray("content");
    assertEquals(2, parts.size());
    assertEquals("text", parts.get(0).getAsJsonObject().get("type").getAsString());
    assertEquals("看这张图", parts.get(0).getAsJsonObject().get("text").getAsString());
    JsonObject imgUrl = parts.get(1).getAsJsonObject().get("image_url").getAsJsonObject();
    assertEquals("data:image/png;base64,QUJD", imgUrl.get("url").getAsString());
}

/** 图片随 Gson 落盘往返（重启后仍可回传模型） */
@Test
public void userWithImages_roundTripsViaGson() {
    ImagePart img = new ImagePart();
    img.mime = "image/jpeg"; img.base64 = "QUJD"; img.name = "a.jpg";
    Message m = Message.userWithImages("图", Collections.singletonList(img));
    String json = gson.toJson(m);
    Message back = gson.fromJson(json, Message.class);
    assertEquals(1, back.images.size());
    assertEquals("image/jpeg", back.images.get(0).mime);
    assertEquals("QUJD", back.images.get(0).base64);
}

/** 无图零回归：content 仍为纯字符串；补充消息带图 supplement 标志保留 */
@Test
public void userWithImages_nullImages_keepsStringContent() {
    JsonObject api = Message.userWithImages("你好", null).toApiJson();
    assertEquals("你好", api.get("content").getAsString());
    ImagePart img = new ImagePart();
    img.mime = "image/png"; img.base64 = "QUJD"; img.name = "s.png";
    Message sup = Message.userSupplement("补充", Collections.singletonList(img));
    assertTrue(sup.supplement);
    assertFalse(sup.toApiJson().has("supplement"));
}
```

需补 import：`com.google.gson.JsonArray`。

- [ ] **Step 2: 跑测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=MessageTest -pl .`
Expected: FAIL（ImagePart 不存在，编译错误）

- [ ] **Step 3: 实现 ImagePart.java**

```java
package com.minion.core.llm;

import java.util.List;

/** 图片内容块（OpenAI 兼容视觉协议 image_url part）。仅 user 消息使用。 */
public class ImagePart {

    /** 单张图片大小上限（5MB，用户确认） */
    public static final long MAX_FILE_BYTES = 5L * 1024 * 1024;
    /** 每条消息最多图片数（用户确认） */
    public static final int MAX_IMAGES = 3;
    /** 单图 token 粗估（压缩阈值估算用；不做像素级精确计算） */
    public static final int IMAGE_TOKENS = 500;

    public String mime;    // image/png 等
    public String base64;  // 纯 base64（不含 data: 前缀）
    public String name;    // 原始文件名（占位展示）

    /** 聊天区占位展示文本：图片：<名> 前缀 + 文本（空格分隔）；无图原样返回 text */
    public static String displayText(List<ImagePart> images, String text) {
        if (images == null || images.isEmpty()) return text;
        StringBuilder sb = new StringBuilder();
        for (ImagePart ip : images) {
            if (ip == null || ip.name == null) continue;
            if (sb.length() > 0) sb.append(' ');
            sb.append("图片：").append(ip.name);
        }
        if (text != null && !text.isEmpty()) {
            if (sb.length() > 0) sb.append(' ');
            sb.append(text);
        }
        return sb.toString();
    }
}
```

- [ ] **Step 4: 修改 Message.java**

字段与工厂（追加到现有字段/工厂区）：

```java
/** 图片 parts（仅 user 消息使用，其他角色忽略）；null = 无图 */
public List<ImagePart> images;

/** user 消息带图：images 为 null/空时行为与 user() 一致 */
public static Message userWithImages(String content, List<ImagePart> images) {
    Message m = user(content);
    m.images = images;
    return m;
}

/** 运行中用户补充（带图）：USER 角色 + supplement=true + images */
public static Message userSupplement(String content, List<ImagePart> images) {
    Message m = userWithImages(content, images);
    m.supplement = true;
    return m;
}
```

`toApiJson()` 的 content 部分替换为：

```java
if (role == Role.USER && images != null && !images.isEmpty()) {
    JsonArray parts = new JsonArray();
    JsonObject textPart = new JsonObject();
    textPart.addProperty("type", "text");
    textPart.addProperty("text", content == null ? "" : content);
    parts.add(textPart);
    for (ImagePart ip : images) {
        if (ip == null || ip.mime == null || ip.base64 == null) continue;
        JsonObject img = new JsonObject();
        img.addProperty("type", "image_url");
        JsonObject url = new JsonObject();
        url.addProperty("url", "data:" + ip.mime + ";base64," + ip.base64);
        img.add("image_url", url);
        parts.add(img);
    }
    o.add("content", parts);
} else if (content != null) {
    o.addProperty("content", content);
}
```

（原 `if (content != null) o.addProperty("content", content);` 保留为 else 分支，无图零回归。）

- [ ] **Step 5: 跑测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=MessageTest -pl .`
Expected: PASS（新增 3 个 + 原有全部）

- [ ] **Step 6: 提交**

```bash
git add src/main/java/com/minion/core/llm/ src/test/java/com/minion/core/llm/
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```
（COMMIT_MSG.tmp 内容：`feat: 图片上传——ImagePart 模型与 Message content 数组化（OpenAI 视觉协议）` + Co-Authored-By 行）

---

### Task 2: InputChip IMAGE 类型（gui/input）

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputChip.java`
- Test: `src/test/java/com/minion/gui/input/InputChipTest.java`

**Interfaces:**
- Consumes: 无
- Produces: `InputChip.Type.IMAGE`、`InputChip.imageChip(String mime, String base64, String name)`（content = `data:<mime>;base64,<base64>`，display = `图片：<名>`）；`compose()` 跳过 IMAGE 块。

- [ ] **Step 1: 写失败测试**（InputChipTest.java 追加）

```java
@Test public void imageChip_holdsDataUriAndDisplay() {
    InputChip c = InputChip.imageChip("image/png", "QUJD", "截图.png");
    assertEquals(InputChip.Type.IMAGE, c.type);
    assertEquals("data:image/png;base64,QUJD", c.content);
    assertEquals("图片：截图.png", c.display);
}

@Test public void compose_skipsImageChips() {
    InputChip img = InputChip.imageChip("image/png", "QUJD", "截图.png");
    assertEquals("文字", InputChip.compose(listOf(img), "文字"));
    assertEquals("/help 文字", InputChip.compose(listOf(cmd("/help"), img), "文字"));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputChipTest -pl .`
Expected: FAIL（imageChip 不存在）

- [ ] **Step 3: 实现**

enum 增加 IMAGE：

```java
public enum Type { COMMAND, SKILL, FILE, PASTE, IMAGE }
```

新增工厂（放 pasteChip 后）：

```java
/** 图片块：content = data URI（发送时解析拆 mime/base64），display = 图片：<文件名> */
public static InputChip imageChip(String mime, String base64, String name) {
    return new InputChip(Type.IMAGE, "data:" + mime + ";base64," + base64, "图片：" + name);
}
```

`compose()` 循环体加跳过：

```java
for (InputChip c : chips) {
    if (c == null || c.type == Type.IMAGE) continue; // 图片走独立通道，不进入文本
    ...
}
```

类 javadoc 第一行「/命令、@文件 与粘贴长文本」补「、上传图片」。

- [ ] **Step 4: 跑测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputChipTest -pl .`
Expected: PASS

- [ ] **Step 5: 提交**（消息：`feat: 图片上传——InputChip 新增 IMAGE 类型，compose 跳过图片块`）

```bash
git add src/main/java/com/minion/gui/input/ src/test/java/com/minion/gui/input/
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

---

### Task 3: 发送链路带图（Session/AgentLoop/SessionManager/SessionController）

**Files:**
- Modify: `src/main/java/com/minion/core/agent/Session.java`、`src/main/java/com/minion/core/agent/AgentLoop.java`、`src/main/java/com/minion/gui/session/SessionManager.java`、`src/main/java/com/minion/gui/session/SessionController.java`
- Test: `src/test/java/com/minion/core/agent/AgentLoopTest.java`、`src/test/java/com/minion/gui/session/SessionManagerTest.java`、`src/test/java/com/minion/gui/session/SessionControllerTest.java`

**Interfaces:**
- Consumes: Task 1 的 `ImagePart`/`Message.userWithImages`/`Message.userSupplement(content, images)`/`ImagePart.displayText`
- Produces: `Session.pendingSupplementImages`（`List<List<ImagePart>>`，与 pendingSupplements 按索引对应）；`AgentLoop.runUserTurn(String, List<ImagePart>)`、`AgentLoop.offerSupplement(String, List<ImagePart>)`（旧签名委托新签名）；`SessionManager.send(h, text, images)`、`SessionManager.sendSupplement(h, text, images)`（旧签名委托）；replayHistory 带图占位。

- [ ] **Step 1: 写失败测试**

AgentLoopTest.java 追加（紧随 supplement_injectedAfterToolResults 测试后；需在 import 区追加 `import com.minion.core.llm.ImagePart;`）：

```java
/** 补充带图：pendingSupplementImages 与文本同步入队，合并后 user 消息 images 保留 */
@Test
public void supplement_withImages_keepsImagesInMessage() {
    llm.addTurn("好的");
    AgentLoop loop = newLoop();
    ImagePart img = new ImagePart();
    img.mime = "image/png"; img.base64 = "QUJD"; img.name = "截图.png";
    loop.offerSupplement("看图", Collections.singletonList(img));
    loop.runUserTurn("继续");
    List<Message> msgs = loop.messages();
    // 0:user(补充带图) 1:user(输入) 2:assistant
    assertEquals(3, msgs.size());
    assertEquals(Message.Role.USER, msgs.get(0).role);
    assertTrue(msgs.get(0).supplement);
    assertEquals(1, msgs.get(0).images.size());
    assertEquals("截图.png", msgs.get(0).images.get(0).name);
    assertNull(msgs.get(1).images); // 无图输入不产生 images
}
```

SessionManagerTest.java 追加（需在 import 区追加 `import com.minion.core.llm.ImagePart;`；`java.util.Collections` 本文件未导入，测试内用全限定名）：

```java
/** 带图发送：入历史 user 消息 images 保留（FakeLlmClient 单回合；SpyManager 模式同 sendAnswer 测试） */
@Test
public void send_withImages_keepsImagesInMessage() throws Exception {
    Path jar = tmp.newFolder("jar").toPath();
    Config config = Config.load(jar);
    WorkspaceManager ws = WorkspaceManager.load(jar);
    ModelManager models = ModelManager.load(jar);
    SpyManager m = new SpyManager(FAKE_UI, config, jar, ws, models);
    SessionHandle h = m.createSession(null);
    FakeLlmClient llm = m.created.get(0);
    llm.addTurn("看到了");
    ImagePart img = new ImagePart();
    img.mime = "image/png"; img.base64 = "QUJD"; img.name = "截图.png";
    final CountDownLatch idle = new CountDownLatch(1);
    m.addListener(new SessionManager.Listener() {
        @Override public void onSessionTitleChanged(SessionHandle h) { }
        @Override public void onSessionRunningChanged(SessionHandle h, boolean running) {
            if (!running) idle.countDown();
        }
        @Override public void onSessionActivated(SessionHandle h) { }
        @Override public void onWorkspaceChanged() { }
        @Override public void onError(String message) { }
    });
    m.send(h, "看这张图", java.util.Collections.singletonList(img));
    assertTrue("等待会话空闲超时", idle.await(5, TimeUnit.SECONDS));
    Message last = h.session.messages.get(h.session.messages.size() - 1);
    assertEquals("看这张图", last.content);
    assertEquals(1, last.images.size());
}

/** 带图补充：挂起队列 + 事件文本带图片占位 */
@Test
public void sendSupplement_withImages_queuesAndEmitsPlaceholder() throws Exception {
    SessionManager m = newManager();
    SessionHandle h = m.createSession(null);
    ImagePart img = new ImagePart();
    img.mime = "image/png"; img.base64 = "QUJD"; img.name = "截图.png";
    m.sendSupplement(h, "补充内容", java.util.Collections.singletonList(img));
    assertEquals(1, h.session.pendingSupplements.size());
    assertEquals(1, h.session.pendingSupplementImages.size());
    List<EventList.Ev> evs = h.controller.eventList().snapshot();
    assertEquals(1, evs.size());
    assertEquals(EventList.Kind.USER_SUPPLEMENT, evs.get(0).kind);
    assertEquals("图片：截图.png 补充内容", evs.get(0).text);
}
```

SessionControllerTest.java 追加（若文件为空则新建，结构见 Task 3 Step 5 说明）：

```java
/** 恢复历史：带图 user 消息事件文本拼图片占位 */
@Test
public void replayHistory_userWithImages_emitsPlaceholder() {
    SessionController c = new SessionController();
    ImagePart img = new ImagePart();
    img.mime = "image/png"; img.base64 = "QUJD"; img.name = "截图.png";
    Message m = Message.userWithImages("看这张图", Collections.singletonList(img));
    c.replayHistory(Collections.singletonList(m));
    List<EventList.Ev> evs = c.eventList().snapshot();
    assertEquals(1, evs.size());
    assertEquals(EventList.Kind.USER_MESSAGE, evs.get(0).kind);
    assertEquals("图片：截图.png 看这张图", evs.get(0).text);
}
```

（SessionControllerTest.java 若已有内容则追加方法；需要 import `com.minion.core.llm.ImagePart`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=AgentLoopTest,SessionManagerTest,SessionControllerTest -pl .`
Expected: FAIL（runUserTurn 无带图重载 / images 字段不存在）

- [ ] **Step 3: Session.java 增加字段**

```java
/** 挂起补充的图片列表（与 pendingSupplements 按索引一一对应；无图 = 空列表）。
 *  随会话落盘：应用退出时未注入的补充图片不丢 */
public List<List<ImagePart>> pendingSupplementImages = new ArrayList<List<ImagePart>>();
```

import 增加 `com.minion.core.llm.ImagePart`。

- [ ] **Step 4: AgentLoop.java 改造**

（1）`runUserTurn` 拆重载（现有方法体改签名行后，第 277-278 行改）：

```java
public void runUserTurn(String input) { runUserTurn(input, null); }

public void runUserTurn(String input, List<ImagePart> images) {
    interrupted = false;
    long start = System.currentTimeMillis(); // 统计行：轮次耗时
    // 上次回合遗留的挂起补充先入历史（模型提问自然收尾/中断遗留），与本次输入拼接发送
    drainSupplements();
    ui.onUserMessage(ImagePart.displayText(images, input));
    session.messages.add(Message.userWithImages(input, images));
    int rounds = 0;
    ...
```

（2）`offerSupplement` 拆重载（第 165-170 行替换）：

```java
/** 运行中补充：入挂起队列（随会话落盘），检查点或下次发送时入历史 */
public void offerSupplement(String text) { offerSupplement(text, null); }

public void offerSupplement(String text, List<ImagePart> images) {
    if (text == null || text.trim().isEmpty()) return;
    synchronized (session.pendingSupplements) {
        session.pendingSupplements.add(text);
        session.pendingSupplementImages.add(images == null
                ? new ArrayList<ImagePart>() : new ArrayList<ImagePart>(images));
    }
}
```

（3）`drainSupplements`（第 173-181 行替换）：

```java
/** 挂起补充全部入历史并清空队列（UI 事件在点击时已发，此处不再发） */
private void drainSupplements() {
    List<String> texts;
    List<List<ImagePart>> imgs;
    synchronized (session.pendingSupplements) {
        if (session.pendingSupplements.isEmpty()) return;
        texts = new ArrayList<String>(session.pendingSupplements);
        imgs = new ArrayList<List<ImagePart>>(session.pendingSupplementImages);
        session.pendingSupplements.clear();
        session.pendingSupplementImages.clear();
    }
    for (int i = 0; i < texts.size(); i++) {
        List<ImagePart> images = i < imgs.size() ? imgs.get(i) : null;
        session.messages.add(Message.userSupplement(texts.get(i), images));
    }
}
```

（4）`restoreSession` 增加防御（现有 pendingSupplements 赋值后追加）：

```java
session.pendingSupplementImages = s.pendingSupplementImages != null
        ? s.pendingSupplementImages : new ArrayList<List<ImagePart>>();
```

（5）import 增加 `com.minion.core.llm.ImagePart`。

- [ ] **Step 5: SessionManager.java 改造**

（1）`send` 拆重载：

```java
public void send(final SessionHandle h, final String text) { send(h, text, null); }

/** 发送（带图）：新会话（titlePending）先本地置标题，再跑正式任务 */
public void send(final SessionHandle h, final String text, final List<ImagePart> images) {
    if (h == null) return;
    ...原方法体不变，仅 h.loop.runUserTurn(text) 改为 h.loop.runUserTurn(text, images)...
}
```

（2）`sendSupplement` 拆重载：

```java
public void sendSupplement(final SessionHandle h, final String text) { sendSupplement(h, text, null); }

/** 运行中补充（带图）：入 AgentLoop 挂起队列 + 发聊天标识事件（UI 事件仅在点击时发一次，注入不重发） */
public void sendSupplement(final SessionHandle h, final String text, final List<ImagePart> images) {
    if (h == null || text == null || text.trim().isEmpty()) return;
    h.loop.offerSupplement(text, images);
    h.controller.onUserSupplement(ImagePart.displayText(images, text));
}
```

（3）import 增加 `com.minion.core.llm.ImagePart`。

- [ ] **Step 6: SessionController.java 改造**

`replayHistory` 的 USER 分支（第 28-31 行）改：

```java
if (m.role == Message.Role.USER) {
    events.add(new EventList.Ev(m.supplement
            ? EventList.Kind.USER_SUPPLEMENT : EventList.Kind.USER_MESSAGE,
            ImagePart.displayText(m.images, m.content), null));
}
```

import 增加 `com.minion.core.llm.ImagePart`。

- [ ] **Step 7: 跑测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=AgentLoopTest,SessionManagerTest,SessionControllerTest -pl .`
Expected: PASS（新增 4 个 + 原有全部）

- [ ] **Step 8: 提交**（消息：`feat: 图片上传——发送链路带图（send/补充/恢复重演占位）`）

```bash
git add src/main/java/com/minion/core/agent/ src/test/java/com/minion/core/agent/ src/main/java/com/minion/gui/session/SessionManager.java src/main/java/com/minion/gui/session/SessionController.java src/test/java/com/minion/gui/session/
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```
（注意：add 前先 `git status --short` 确认 gui/session 无其他 agent 未提交改动；SessionController.java 会话开始时有并行改动，若此时仍未提交则停下询问。）

---

### Task 4: TokenCounter 图片 token 粗估

**Files:**
- Modify: `src/main/java/com/minion/core/context/TokenCounter.java`
- Test: `src/test/java/com/minion/core/context/TokenCounterTest.java`

**Interfaces:**
- Consumes: `ImagePart.IMAGE_TOKENS`
- Produces: `TokenCounter.estimateMessages` 对带图消息追加 `IMAGE_TOKENS × 图片数`。

- [ ] **Step 1: 写失败测试**（TokenCounterTest.java 追加）

```java
@Test
public void estimateMessages_countsImages() {
    ImagePart img = new ImagePart();
    img.mime = "image/png"; img.base64 = "QUJD"; img.name = "s.png";
    Message u = Message.userWithImages("看图", Collections.singletonList(img));
    int n = TokenCounter.estimateMessages(Collections.singletonList(u));
    // 消息开销 4 + 文本 + 图片 500
    assertTrue(n >= 4 + 500);
    Message plain = Message.user("看图");
    assertTrue(TokenCounter.estimateMessages(Collections.singletonList(plain))
            < TokenCounter.estimateMessages(Collections.singletonList(u)));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=TokenCounterTest -pl .`
Expected: FAIL（估算未计入图片）

- [ ] **Step 3: 实现**

`estimateMessages` 循环体内（`estimate(m.content)` 之后）追加：

```java
if (m.images != null) total += ImagePart.IMAGE_TOKENS * m.images.size();
```

import 增加 `com.minion.core.llm.ImagePart`。

- [ ] **Step 4: 跑测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=TokenCounterTest -pl .`
Expected: PASS

- [ ] **Step 5: 提交**（消息：`feat: 图片上传——TokenCounter 按每图 500 粗估`）

```bash
git add src/main/java/com/minion/core/context/ src/test/java/com/minion/core/context/
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

---

### Task 5: InputView UI 重构（底部操作行 + 上传按钮 + 红色发送 + ANSWER 拦截）+ CSS

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`
- Modify: `src/resource/theme/theme.css`
- Test: `src/test/java/com/minion/gui/input/InputViewButtonTest.java`

**Interfaces:**
- Consumes: `ImagePart`（常量/displayText 不用——本任务用 `ImagePart.MAX_FILE_BYTES`/`MAX_IMAGES`）、`InputChip.imageChip`
- Produces: `InputView.buttonStyleClass(BtnMode)`（静态纯函数，全部模式返回 `btn-danger`）；`InputView` 布局：frame=VBox{composer, bottomRow(HBox{uploadButton, spacer, sendButton})}；上传按钮；`composedImages()`。

- [ ] **Step 1: 写失败测试**（InputViewButtonTest.java 追加）

```java
/** 发送/补充/回答/终止统一红色背景（btn-danger，与停止一致） */
@Test
public void buttonStyleClass_allModesRed() {
    assertEquals("btn-danger", InputView.buttonStyleClass(InputView.BtnMode.SEND));
    assertEquals("btn-danger", InputView.buttonStyleClass(InputView.BtnMode.SEND_DIM));
    assertEquals("btn-danger", InputView.buttonStyleClass(InputView.BtnMode.SUPPLEMENT));
    assertEquals("btn-danger", InputView.buttonStyleClass(InputView.BtnMode.ANSWER));
    assertEquals("btn-danger", InputView.buttonStyleClass(InputView.BtnMode.ANSWER_DIM));
    assertEquals("btn-danger", InputView.buttonStyleClass(InputView.BtnMode.STOP));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputViewButtonTest -pl .`
Expected: FAIL（buttonStyleClass 不存在）

- [ ] **Step 3: InputView.java 布局改造**

（1）类 javadoc 更新（第 29-31 行）：补「底部操作行：上传按钮（左）+ 发送按钮（右）」。

（2）字段区新增：

```java
private final Button uploadButton = new Button();
private final SVGPath uploadIcon = new SVGPath();
```

（3）构造器中图标定义区（stopIcon 后）追加：

```java
// 回形针（描边图标，CSS 定义 stroke 颜色）
uploadIcon.setContent("M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48");
uploadIcon.getStyleClass().add("icon-upload");
uploadButton.setGraphic(uploadIcon);
uploadButton.setMinSize(30, 30);
uploadButton.setPrefSize(30, 30);
uploadButton.getStyleClass().add("btn-upload");
uploadButton.setTooltip(new Tooltip("上传图片 (最多" + ImagePart.MAX_IMAGES + "张, 每张≤" + (ImagePart.MAX_FILE_BYTES / 1024 / 1024) + "MB)"));
uploadButton.setOnAction(e -> chooseImages());
```

（4）frame 构建替换（第 130-144 行整段）：

```java
// 大框：上=块行+输入框，下=底部操作行（上传按钮左 + 弹性空白 + 发送按钮右）
VBox frame = new VBox(6);
frame.getStyleClass().add("input-frame");
chipRow.getStyleClass().add("chip-row");
VBox composer = new VBox(6);
composer.getChildren().addAll(chipRow, input);
VBox.setVgrow(composer, Priority.ALWAYS);
Region spacer = new Region();
HBox.setHgrow(spacer, Priority.ALWAYS);
HBox bottomRow = new HBox(8);
bottomRow.getChildren().addAll(uploadButton, spacer, sendButton);
frame.getChildren().addAll(composer, bottomRow);
```

（删除 divider 变量及其使用；`input-divider` CSS 保留不删。）

（5）`chipTypeClass` switch 加分支：

```java
case IMAGE:   return "input-chip-image";
```

（6）`updateButton` 整体替换（样式类统一走 buttonStyleClass）：

```java
/** 按钮模式 → 背景样式类：发送/补充/回答/终止统一红色（与停止一致）；纯静态可单测 */
static String buttonStyleClass(BtnMode mode) {
    return "btn-danger";
}

private void updateButton() {
    BtnMode mode = buttonMode(running, askPending, hasContent());
    switch (mode) {
        case SEND:       applyStyle(arrowIcon, buttonStyleClass(mode), 1.0, "发送 (Ctrl+Enter)"); break;
        case SEND_DIM:   applyStyle(arrowIcon, buttonStyleClass(mode), 0.35, "输入消息后发送 (Ctrl+Enter)"); break;
        case SUPPLEMENT: applyStyle(arrowIcon, buttonStyleClass(mode), 1.0, "补充信息给正在运行的模型 (Ctrl+Enter)"); break;
        case ANSWER:     applyStyle(arrowIcon, buttonStyleClass(mode), 1.0, "回答模型的提问 (Ctrl+Enter)"); break;
        case ANSWER_DIM: applyStyle(arrowIcon, buttonStyleClass(mode), 0.35, "输入回答后发送 (Ctrl+Enter)"); break;
        case STOP:       applyStyle(stopIcon, buttonStyleClass(mode), 1.0, "终止当前运行 (Esc)"); break;
    }
}
```

（7）`onAction` 的 ANSWER 分支（第 389-395 行）加图片拦截：

```java
case ANSWER: {
    if (!composedImages().isEmpty()) {
        notifyChat("回答模式暂不支持图片，请删除图片后再回答");
        return;
    }
    String text = composedText();
    if (text == null || text.trim().isEmpty()) return;
    clearComposer();
    if (current != null) manager.sendAnswer(current, text);
    break;
}
```

（8）`onSend`（第 405-416 行）替换：

```java
private void onSend() {
    String text = composedText();
    List<ImagePart> images = composedImages();
    if ((text == null || text.trim().isEmpty()) && images.isEmpty()) return;
    clearComposer();
    SessionHandle target = current;
    if (target == null) {
        target = manager.createSession(null);
        if (target == null) return;
        manager.activateSession(target);
    }
    // 带图消息不走斜杠命令分发（图片无法本地处理，照发普通消息）
    if (images.isEmpty()) {
        manager.dispatchCommand(target, text);
    } else {
        manager.send(target, text, images);
    }
}
```

（9）新增方法（clearComposer 后）：

```java
/** 图片块 → ImagePart 列表：解析 data URI 头拆 mime/base64；name 取 display 的「图片：」前缀之后 */
private List<ImagePart> composedImages() {
    List<ImagePart> out = new ArrayList<ImagePart>();
    for (InputChip c : chips) {
        if (c == null || c.type != InputChip.Type.IMAGE) continue;
        int comma = c.content.indexOf(',');
        if (comma < 0 || !c.content.startsWith("data:")) continue;
        String header = c.content.substring(0, comma); // data:<mime>;base64
        int semi = header.indexOf(';');
        String mime = semi > 5 ? header.substring(5, semi) : header.substring(5);
        ImagePart p = new ImagePart();
        p.mime = mime;
        p.base64 = c.content.substring(comma + 1);
        p.name = c.display.startsWith("图片：") ? c.display.substring(3) : c.display;
        out.add(p);
    }
    return out;
}

/** 上传图片：FileChooser 选图 → 大小/数量校验 → 转 base64 建 IMAGE 块 */
private void chooseImages() {
    FileChooser chooser = new FileChooser();
    chooser.setTitle("上传图片");
    chooser.getExtensionFilters().add(new ExtensionFilter("图片文件",
            "*.png", "*.jpg", "*.jpeg", "*.gif", "*.bmp", "*.webp"));
    File f = chooser.showOpenDialog(getScene().getWindow());
    if (f == null) return;
    if (f.length() > ImagePart.MAX_FILE_BYTES) {
        notifyChat("图片超过 " + (ImagePart.MAX_FILE_BYTES / 1024 / 1024) + "MB，拒绝上传: " + f.getName());
        return;
    }
    int have = 0;
    for (InputChip c : chips) if (c.type == InputChip.Type.IMAGE) have++;
    if (have >= ImagePart.MAX_IMAGES) {
        notifyChat("最多上传 " + ImagePart.MAX_IMAGES + " 张图片");
        return;
    }
    try {
        byte[] bytes = Files.readAllBytes(f.toPath());
        String b64 = Base64.getEncoder().encodeToString(bytes);
        addChip(InputChip.imageChip(mimeFor(f.getName()), b64, f.getName()));
    } catch (Exception ex) {
        notifyChat("读取图片失败: " + ex.getMessage());
    }
}

/** 文件名后缀 → mime；未知后缀兜底 png */
private static String mimeFor(String name) {
    String n = name == null ? "" : name.toLowerCase();
    if (n.endsWith(".jpg") || n.endsWith(".jpeg")) return "image/jpeg";
    if (n.endsWith(".gif")) return "image/gif";
    if (n.endsWith(".bmp")) return "image/bmp";
    if (n.endsWith(".webp")) return "image/webp";
    return "image/png";
}

/** 聊天区提示（无会话时静默忽略；发送前必先有会话或上传仅装饰） */
private void notifyChat(String msg) {
    if (current != null) current.controller.onWarning(msg);
}
```

（10）import 追加：`com.minion.core.llm.ImagePart`、`javafx.scene.control.Button`（已有）、`javafx.stage.FileChooser`、`javafx.stage.FileChooser.ExtensionFilter`、`java.io.File`、`java.nio.file.Files`、`java.util.Base64`。

- [ ] **Step 4: theme.css 追加样式**

`.input-divider` 规则后追加：

```css
/* 上传按钮（输入框底部操作行左）：回形针描边图标 */
.btn-upload { -fx-background-color: transparent; -fx-background-radius: 6; -fx-cursor: hand; }
.icon-upload {
    -fx-fill: transparent;
    -fx-stroke: #a8b0bb;
    -fx-stroke-width: 1.6;
    -fx-stroke-linecap: round;
    -fx-stroke-linejoin: round;
}
.btn-upload:hover .icon-upload { -fx-stroke: #f0f2f6; }
```

`.input-chip-paste` 规则后追加：

```css
.input-chip-image { -fx-background-color: #241f38; -fx-border-color: #7c5cd6; }
```

- [ ] **Step 5: 跑测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputViewButtonTest -pl .`
Expected: PASS

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile -pl .`
Expected: 编译通过（InputView 全部改动）

- [ ] **Step 6: 提交**（消息：`feat: 图片上传——输入框底部操作行（上传左/发送右），发送按钮改红`）

```bash
git add src/main/java/com/minion/gui/input/ src/test/java/com/minion/gui/input/ src/resource/theme/
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

---

### Task 6: 全量测试 + 文档更新

**Files:**
- Modify: `README.md`、`docs/ARCHITECTURE.md`

- [ ] **Step 1: 全量测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -pl .`
Expected: 全部 PASS（零回归：无图路径行为不变）

- [ ] **Step 2: README 功能说明补充**

在 README 功能列表输入区相关条目处补充：输入框支持上传图片（左下上传按钮，最多 3 张、每张 ≤5MB），图片随消息以 OpenAI 兼容视觉格式发送（需视觉模型如 qwen-vl；DeepSeek 不支持将报错）。

- [ ] **Step 3: ARCHITECTURE.md 类路径更新**

- `core/llm/` 行补 `ImagePart`（图片内容块，content 数组化）。
- `gui/input/` 行补上传按钮（InputView 底部操作行）。
- `core/agent/Session` 补 `pendingSupplementImages`。

- [ ] **Step 4: 提交**（消息：`docs: README 补充图片上传说明，ARCHITECTURE 同步类路径`）

```bash
git add README.md docs/
git commit -F .git/COMMIT_MSG.tmp && rm .git/COMMIT_MSG.tmp
```

- [ ] **Step 5: 全量测试复跑 + 汇总**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -pl .` → PASS 后 `git log --oneline -7` 汇总提交链，向用户报告。
