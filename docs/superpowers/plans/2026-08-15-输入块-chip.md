# 输入块（Chip）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 弹层确认的 /命令、@文件 与粘贴 ≥100 字符的长文本变为输入框上方的不可编辑块（chip），修复点击反显内容再次弹窗的干扰。

**Architecture:** 新增纯模型类 `InputChip`（类型/内容/显示 + compose/阈值/类型映射纯静态逻辑，可脱离 JavaFX 单测）；`InputView` 输入大框改为「上方块行 FlowPane + 下方 TextArea」的 VBox；确认路径/粘贴拦截/删除/发送全部走块模型，移除原 `suppressed` 回弹抑制与 `insertAt` 文本替换。

**Tech Stack:** Java 8 + JavaFX 8（JDK 自带 jfxrt）+ JUnit4；样式在 `src/resource/theme/theme.css`。

## Global Constraints

- JDK 8 兼容：Java 8 语法（lambda/匿名类均可，项目已用）、JavaFX 8 API（`javafx.scene.input.Clipboard`、FlowPane 等均为 8 已有）
- 文档、注释、commit 均用中文（commit 用 conventional 格式）
- **git commit 中文消息必须用 `-F 文件` 绕过 bash wrapper 崩溃**：先 Write 消息到 `.git/XXX_MSG.tmp`，再 `git commit -F .git/XXX_MSG.tmp`；命令行不得含中文
- **含中文的文件路径不能用 git add 直写**：用 ASCII 通配符，如 `git add docs/superpowers/plans/2026-08-15-*.md`
- 只 `git add` 自己任务的文件（并行 agent 共享仓库，勿动他人文件）
- 测试运行：`JAVA_HOME="E:/javame/jdk8" mvn test`（先 `mvn compile` 快速验证）
- 纯逻辑（compose/阈值/类型映射）必须放 `InputChip`（可单测），`InputView` 只做 JavaFX 接线
- 块显示：COMMAND/SKILL/FILE 显示原文本；PASTE 显示「粘贴内容，N 字符」
- tooltip 规则：`content.length() > 40` 且类型非 PASTE 时挂完整内容 tooltip（PASTE 不挂，字符数已显示）

---

### Task 1: InputChip 纯模型与纯逻辑（TDD）

**Files:**
- Create: `src/main/java/com/minion/gui/input/InputChip.java`
- Create: `src/test/java/com/minion/gui/input/InputChipTest.java`

**Interfaces:**
- Produces（后续任务依赖的精确 API）：
  - `enum InputChip.Type { COMMAND, SKILL, FILE, PASTE }`
  - `int InputChip.PASTE_CHIP_THRESHOLD = 100`
  - `String InputChip.content`（发送用完整文本）、`String InputChip.display`（块上显示文本）、`InputChip.Type InputChip.type`（public final 字段）
  - `static InputChip InputChip.textChip(InputChip.Type, String content)` — display = content
  - `static InputChip InputChip.pasteChip(String content)` — display = 「粘贴内容，N 字符」
  - `static InputChip.Type InputChip.modeToType(CompletionParser.Mode)` — SLASH→COMMAND、SLASH_SKILL→SKILL、FILE→FILE、NONE→COMMAND（兜底）
  - `static boolean InputChip.shouldChipPaste(String)` — 非 null 且 length（含换行）≥100
  - `static String InputChip.compose(List<InputChip>, String text)` — 块按序单空格连接；text 非空时补一个空格原样追加
- Consumes: `CompletionParser.Mode`（同包已有）

- [ ] **Step 1: 写失败测试 `InputChipTest.java`**

```java
package com.minion.gui.input;

import org.junit.Test;

import java.util.ArrayList;
import java.util.List;

import static org.junit.Assert.*;

/** 输入块纯逻辑：compose 组装 / 粘贴阈值 / 类型映射 / 粘贴块显示。无 JavaFX 依赖。 */
public class InputChipTest {

    private static List<InputChip> listOf(InputChip... chips) {
        List<InputChip> list = new ArrayList<InputChip>();
        if (chips != null) {
            for (InputChip c : chips) list.add(c);
        }
        return list;
    }

    private static InputChip cmd(String s) { return InputChip.textChip(InputChip.Type.COMMAND, s); }

    @Test public void compose_emptyAll() {
        assertEquals("", InputChip.compose(new ArrayList<InputChip>(), ""));
        assertEquals("", InputChip.compose(new ArrayList<InputChip>(), null));
    }

    @Test public void compose_textOnly_keepsAsIs() {
        assertEquals("你好", InputChip.compose(new ArrayList<InputChip>(), "你好"));
    }

    @Test public void compose_chipOnly() {
        assertEquals("/help", InputChip.compose(listOf(cmd("/help")), ""));
    }

    @Test public void compose_chipsJoinedBySpace() {
        assertEquals("/help @src/a.java",
                InputChip.compose(listOf(cmd("/help"), cmd("@src/a.java")), ""));
    }

    @Test public void compose_chipThenText_singleSpace() {
        assertEquals("/help 修复bug", InputChip.compose(listOf(cmd("/help")), "修复bug"));
    }

    @Test public void compose_twoChipsAndText() {
        assertEquals("/help @src/a.java 继续",
                InputChip.compose(listOf(cmd("/help"), cmd("@src/a.java")), "继续"));
    }

    @Test public void shouldChipPaste_threshold() {
        assertFalse(InputChip.shouldChipPaste(repeat('a', 99)));
        assertTrue(InputChip.shouldChipPaste(repeat('a', 100)));
        assertFalse(InputChip.shouldChipPaste(null));
    }

    @Test public void shouldChipPaste_newlinesCount() {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < 50; i++) sb.append("ab\n"); // 50 行 × 3 字符（含换行）= 150
        assertEquals(150, sb.toString().length());
        assertTrue(InputChip.shouldChipPaste(sb.toString()));
    }

    @Test public void modeToType_mapping() {
        assertEquals(InputChip.Type.COMMAND, InputChip.modeToType(CompletionParser.Mode.SLASH));
        assertEquals(InputChip.Type.SKILL, InputChip.modeToType(CompletionParser.Mode.SLASH_SKILL));
        assertEquals(InputChip.Type.FILE, InputChip.modeToType(CompletionParser.Mode.FILE));
        assertEquals(InputChip.Type.COMMAND, InputChip.modeToType(CompletionParser.Mode.NONE));
    }

    @Test public void pasteChip_displayShowsCount() {
        InputChip c = InputChip.pasteChip(repeat('x', 123));
        assertEquals(InputChip.Type.PASTE, c.type);
        assertEquals(123, c.content.length());
        assertEquals("粘贴内容，123 字符", c.display);
    }

    private static String repeat(char ch, int n) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < n; i++) sb.append(ch);
        return sb.toString();
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputChipTest`
Expected: 编译失败 / `cannot find symbol: class InputChip`

- [ ] **Step 3: 写实现 `InputChip.java`**

```java
package com.minion.gui.input;

import java.util.List;

/** 输入块（chip）：弹层确认的 /命令、@文件 与粘贴长文本在输入框上方的不可编辑块。
 *  纯模型 + 纯静态逻辑（compose/阈值/类型映射），可脱离 JavaFX 单测。 */
public final class InputChip {

    /** 块类型：COMMAND 斜杠命令 / SKILL 技能 / FILE 文件 / PASTE 粘贴长文本 */
    public enum Type { COMMAND, SKILL, FILE, PASTE }

    /** 粘贴变块阈值（字符数，含换行） */
    public static final int PASTE_CHIP_THRESHOLD = 100;

    public final Type type;
    /** 发送用的完整文本（命令/路径/粘贴全文） */
    public final String content;
    /** 块上显示的文本（粘贴块为「粘贴内容，N 字符」，其余同 content） */
    public final String display;

    private InputChip(Type type, String content, String display) {
        this.type = type;
        this.content = content;
        this.display = display;
    }

    /** 命令/技能/文件块：显示 = 内容 */
    public static InputChip textChip(Type type, String content) {
        return new InputChip(type, content, content);
    }

    /** 粘贴块：显示「粘贴内容，N 字符」 */
    public static InputChip pasteChip(String content) {
        return new InputChip(Type.PASTE, content, "粘贴内容，" + content.length() + " 字符");
    }

    /** 弹层模式 → 块类型（NONE 不进确认路径，兜底 COMMAND） */
    public static Type modeToType(CompletionParser.Mode mode) {
        switch (mode) {
            case SLASH: return Type.COMMAND;
            case SLASH_SKILL: return Type.SKILL;
            case FILE: return Type.FILE;
            default: return Type.COMMAND;
        }
    }

    /** 粘贴是否变块：长度（含换行）≥ 阈值 */
    public static boolean shouldChipPaste(String text) {
        return text != null && text.length() >= PASTE_CHIP_THRESHOLD;
    }

    /** 组装发送文本：块按列表顺序单个空格连接；文本非空时补一个空格再原样追加 */
    public static String compose(List<InputChip> chips, String text) {
        StringBuilder sb = new StringBuilder();
        for (InputChip c : chips) {
            if (sb.length() > 0) sb.append(' ');
            sb.append(c.content);
        }
        if (text != null && !text.isEmpty()) {
            if (sb.length() > 0) sb.append(' ');
            sb.append(text);
        }
        return sb.toString();
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=InputChipTest`
Expected: `Tests run: 10, Failures: 0`

- [ ] **Step 5: 提交**

先 Write 消息文件 `.git/CHIP_T1_MSG.tmp`：

```
feat: 输入块纯模型与纯逻辑（compose/粘贴阈值/类型映射）

Co-Authored-By: Claude <noreply@anthropic.com>
```

再运行（命令行纯 ASCII）：

```bash
git add src/main/java/com/minion/gui/input/InputChip.java src/test/java/com/minion/gui/input/InputChipTest.java && git commit -F .git/CHIP_T1_MSG.tmp && rm .git/CHIP_T1_MSG.tmp
```

---

### Task 2: InputView 块行 UI（布局 + 渲染 + 增删 + 样式）

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`（构造器大框组装段、新增字段与方法）
- Modify: `src/resource/theme/theme.css`（`-fx-border-radius` 段附近，输入区分节后新增块样式）

**Interfaces:**
- Consumes: Task 1 的 `InputChip`（textChip/pasteChip/modeToType/shouldChipPaste/compose 全量）
- Produces（后续任务依赖）：
  - 字段 `List<InputChip> chips`、`FlowPane chipRow`（构造器初始化，chipRow 样式类 `chip-row`）
  - `void addChip(InputChip)` — 追加尾部（模型+视图同步，内部调 `refreshChipRow()` + `updateButton()`）
  - `void removeChipAt(int i)` — 越界静默忽略
  - `boolean removeLastChip()` — 无块返回 false
  - `void refreshChipRow()` — 无块时 chipRow `setManaged(false)+setVisible(false)`（VBox 布局忽略不占位）
  - `Node chipView(InputChip)`、`static String chipTypeClass(InputChip.Type)`

- [ ] **Step 1: 新增 imports 与字段**

在 `InputView.java` 顶部 import 区新增（javafx 组插在 `javafx.scene.layout.GridPane` 行附近、`java.util` 组新增一行）：

```java
import javafx.scene.Node;
import javafx.scene.control.Label;
import javafx.scene.input.Clipboard; // Task 4 用，可一并加
import javafx.scene.layout.FlowPane;
import java.util.ArrayList;
import java.util.List;
```

字段区（`private final SuggestionPopup popup` 附近）新增：

```java
/** 块行与块列表：模型 List<InputChip> 与视图 FlowPane 同步维护（增删块后须 refreshChipRow + updateButton） */
private final List<InputChip> chips = new ArrayList<InputChip>();
private final FlowPane chipRow = new FlowPane();
```

- [ ] **Step 2: 改构造器布局（大框组装段）**

现构造器有：

```java
HBox.setHgrow(input, Priority.ALWAYS);
Region divider = new Region();
```

改为：

```java
// 块行（上方）+ 文本区（下方）：chipRow 无块时 unmanaged 不占位
chipRow.getStyleClass().add("chip-row");
VBox composer = new VBox(6);
composer.getChildren().addAll(chipRow, input);
HBox.setHgrow(composer, Priority.ALWAYS);
Region divider = new Region();
```

且 `frame.getChildren().addAll(input, divider, buttonBox);` 改为：

```java
frame.getChildren().addAll(composer, divider, buttonBox);
```

- [ ] **Step 3: 新增块操作方法（放在 `insertionText` 静态方法后）**

```java
/** 块行占位开关：无块时 unmanaged（VBox 布局忽略，不产生多余间距） */
private void refreshChipRow() {
    boolean has = !chips.isEmpty();
    chipRow.setManaged(has);
    chipRow.setVisible(has);
}

/** 追加块到尾部（模型 + 视图同步） */
private void addChip(InputChip chip) {
    if (chip == null) return;
    chips.add(chip);
    chipRow.getChildren().add(chipView(chip));
    refreshChipRow();
    updateButton();
}

/** 删除第 i 个块（越界静默忽略） */
private void removeChipAt(int i) {
    if (i < 0 || i >= chips.size()) return;
    chips.remove(i);
    chipRow.getChildren().remove(i);
    refreshChipRow();
    updateButton();
}

/** 删除最后一个块（Backspace 用）；无块返回 false */
private boolean removeLastChip() {
    if (chips.isEmpty()) return false;
    removeChipAt(chips.size() - 1);
    return true;
}

/** 渲染单个块：Label + ✕ 按钮；✕ 点击删除并还焦输入框；COMMAND/SKILL/FILE 超 40 字符挂完整内容 tooltip */
private Node chipView(InputChip chip) {
    Label label = new Label(chip.display);
    label.getStyleClass().add("chip-label");
    Button close = new Button("✕");
    close.getStyleClass().add("chip-close");
    close.setOnAction(e -> {
        removeChipAt(chips.indexOf(chip));
        input.requestFocus();
    });
    HBox box = new HBox(6);
    box.getStyleClass().addAll("input-chip", chipTypeClass(chip.type));
    box.getChildren().addAll(label, close);
    if (chip.type != InputChip.Type.PASTE && chip.content.length() > 40) {
        Tooltip.install(box, new Tooltip(chip.content));
    }
    return box;
}

/** 块类型 → 语义样式类 */
private static String chipTypeClass(InputChip.Type type) {
    switch (type) {
        case COMMAND: return "input-chip-command";
        case SKILL:   return "input-chip-skill";
        case FILE:    return "input-chip-file";
        default:      return "input-chip-paste";
    }
}
```

- [ ] **Step 4: theme.css 新增块样式**

在 `theme.css` 的 `.input-divider` 行（第 83 行）后新增：

```css

/* ===== 输入块（chip）：弹层确认的 /命令、@文件与粘贴长文本；不可编辑只可删除 ===== */
.chip-row { -fx-hgap: 8; -fx-vgap: 8; }
.input-chip {
    -fx-background-radius: 6;
    -fx-border-radius: 6;
    -fx-border-width: 1;
    -fx-padding: 2 4 2 8;
}
.chip-label { -fx-text-fill: #d3d7de; -fx-font-size: 12px; }
.chip-close {
    -fx-background-color: transparent;
    -fx-text-fill: #98a0ab;
    -fx-font-size: 11px;
    -fx-padding: 0 4 0 4;
    -fx-background-radius: 4;
    -fx-cursor: hand;
}
.chip-close:hover { -fx-text-fill: #f85149; -fx-background-color: rgba(248, 81, 73, 0.15); }
.input-chip-command, .input-chip-skill { -fx-background-color: #1f2f55; -fx-border-color: #3b6fe0; }
.input-chip-file { -fx-background-color: #14301f; -fx-border-color: #2ea043; }
.input-chip-paste { -fx-background-color: #2a2f3a; -fx-border-color: #3a4150; }
```

- [ ] **Step 5: 编译验证**

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile`
Expected: BUILD SUCCESS（现有测试不受影响，本任务无新增单测——纯 FX UI 接线）

- [ ] **Step 6: 提交**

Write `.git/CHIP_T2_MSG.tmp`：

```
feat: 输入框上方块行 UI（渲染/增删/样式）

Co-Authored-By: Claude <noreply@anthropic.com>
```

Run:

```bash
git add src/main/java/com/minion/gui/input/InputView.java src/resource/theme/theme.css && git commit -F .git/CHIP_T2_MSG.tmp && rm .git/CHIP_T2_MSG.tmp
```

---

### Task 3: 确认路径改建块，移除 suppressed/insertAt

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`

**Interfaces:**
- Consumes: Task 2 的 `addChip(InputChip)`；Task 1 的 `InputChip.textChip`/`modeToType`
- Produces: `void addChipFor(CompletionParser.Mode mode, String insert)` — 建块并追加（@ 补前缀，空文本忽略）

- [ ] **Step 1: 删除 `suppressed` 字段**

删除字段声明（现第 40 行）：`private String suppressed; // ...`

- [ ] **Step 2: 删除 `onTextChanged` 中的 suppressed 分支**

删除：

```java
            // 补全确认后抑制回弹：当前词与刚插入的补全文本完全一致（含 / @ 前缀）时不再弹层；
            // 词被修改后解除抑制恢复补全
            if (suppressed != null) {
                if (t.mode != CompletionParser.Mode.NONE
                        && suppressed.equals(input.getText().substring(t.start, t.end))) {
                    popup.hide();
                    lastToken = null;
                    return;
                }
                suppressed = null;
            }
```

（`onTextChanged` 其余 switch 分支不变。）

- [ ] **Step 3: 改鼠标确认路径 `confirmInsert` 并新增 `addChipFor`**

```java
    /** 鼠标点击弹层条目：按 token 模式建块追加，焦点还回输入框（防后续键盘事件落入弹层列表） */
    private void confirmInsert(String insert) {
        if (insert == null || lastToken == null) return;
        addChipFor(lastToken.mode, insert);
        lastToken = null;
        input.requestFocus();
    }

    /** 按弹层模式建块并追加：@ 模式补回 @ 前缀（FileSuggester insertText 为纯路径） */
    private void addChipFor(CompletionParser.Mode mode, String insert) {
        String text = insertionText(mode, insert);
        if (text == null || text.isEmpty()) return;
        addChip(InputChip.textChip(InputChip.modeToType(mode), text));
    }
```

- [ ] **Step 4: 改键盘确认路径 `confirmPopup`**

```java
    /** 键盘确认弹层选中：取插入文本 → 关弹层 → runLater 建块追加。
     *  runLater 保持原时序（KEY_PRESSED 派发期间改输入区与 behavior 竞争的老规避惯例，统一路径） */
    private void confirmPopup() {
        if (lastToken == null) return;
        final CompletionParser.Token t = lastToken;
        final String insert = popup.confirmSelected();
        popup.hide();
        if (insert == null) return;
        Platform.runLater(new Runnable() {
            @Override public void run() { addChipFor(t.mode, insert); }
        });
    }
```

- [ ] **Step 5: 删除 `insertAt` 方法**

删除整个 `insertAt(CompletionParser.Token t, String insert)` 方法（现第 198-208 行，含其注释；`insertionText` 静态方法保留）。

- [ ] **Step 6: 编译 + 现有测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=CompletionParserTest,InputChipTest,SuggestionPopupTest`
Expected: 全部 PASS

- [ ] **Step 7: 提交**

Write `.git/CHIP_T3_MSG.tmp`：

```
feat: 补全确认改为建输入块（移除 suppressed 回弹抑制）

Co-Authored-By: Claude <noreply@anthropic.com>
```

Run:

```bash
git add src/main/java/com/minion/gui/input/InputView.java && git commit -F .git/CHIP_T3_MSG.tmp && rm .git/CHIP_T3_MSG.tmp
```

---

### Task 4: 粘贴拦截 + Backspace 删块

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`（KEY_PRESSED 过滤器）

**Interfaces:**
- Consumes: Task 2 的 `addChip`/`removeLastChip`；Task 1 的 `pasteChip`/`shouldChipPaste`
- Produces: 无（行为性改动）

- [ ] **Step 1: 改 KEY_PRESSED 过滤器**

现过滤器（构造器内）：

```java
        input.addEventFilter(KeyEvent.KEY_PRESSED, e -> {
            if (new KeyCodeCombination(KeyCode.ENTER, KeyCombination.CONTROL_DOWN).match(e)) {
                e.consume();
                onAction();
                return;
            }
            // 补全弹层优先：↑↓ 移动、Enter/Tab 确认、Esc 仅关弹层
            if (popup.isShowing()) {
                switch (e.getCode()) {
                    case UP:    popup.move(-1); e.consume(); break;
                    case DOWN:  popup.move(1);  e.consume(); break;
                    case TAB:
                    case ENTER: confirmPopup(); e.consume(); break;
                    case ESCAPE: popup.hide(); lastToken = null; e.consume(); break;
                }
                return;
            }
            // Esc：终止当前运行（提问挂起时亦可终止）
            if (e.getCode() == KeyCode.ESCAPE && current != null && running) {
                e.consume();
                manager.stop(current);
            }
        });
```

改为（在 Ctrl+Enter 分支后插入粘贴拦截，弹层分支后插入 Backspace 删块；Esc 分支保留）：

```java
        input.addEventFilter(KeyEvent.KEY_PRESSED, e -> {
            if (new KeyCodeCombination(KeyCode.ENTER, KeyCombination.CONTROL_DOWN).match(e)) {
                e.consume();
                onAction();
                return;
            }
            // 长文本粘贴（Ctrl+V / Shift+Insert）→ 变块；短文本不拦截走默认粘贴
            if ((e.isControlDown() && e.getCode() == KeyCode.V)
                    || (e.isShiftDown() && e.getCode() == KeyCode.INSERT)) {
                String clip = Clipboard.getSystemClipboard().getString();
                if (InputChip.shouldChipPaste(clip)) {
                    addChip(InputChip.pasteChip(clip));
                    e.consume();
                    return;
                }
            }
            // 补全弹层优先：↑↓ 移动、Enter/Tab 确认、Esc 仅关弹层
            if (popup.isShowing()) {
                switch (e.getCode()) {
                    case UP:    popup.move(-1); e.consume(); break;
                    case DOWN:  popup.move(1);  e.consume(); break;
                    case TAB:
                    case ENTER: confirmPopup(); e.consume(); break;
                    case ESCAPE: popup.hide(); lastToken = null; e.consume(); break;
                }
                return;
            }
            // 空文本时 Backspace/Delete 删除最后一个块
            if ((e.getCode() == KeyCode.BACK_SPACE || e.getCode() == KeyCode.DELETE)
                    && !chips.isEmpty() && input.getText().isEmpty()) {
                removeLastChip();
                e.consume();
                return;
            }
            // Esc：终止当前运行（提问挂起时亦可终止）
            if (e.getCode() == KeyCode.ESCAPE && current != null && running) {
                e.consume();
                manager.stop(current);
            }
        });
```

import 区确认已有 `javafx.scene.input.Clipboard`（Task 2 Step 1 已加）。

- [ ] **Step 2: 编译验证**

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile`
Expected: BUILD SUCCESS

- [ ] **Step 3: 提交**

Write `.git/CHIP_T4_MSG.tmp`：

```
feat: 长文本粘贴变块、空输入 Backspace 删除最后一个块

Co-Authored-By: Claude <noreply@anthropic.com>
```

Run:

```bash
git add src/main/java/com/minion/gui/input/InputView.java && git commit -F .git/CHIP_T4_MSG.tmp && rm .git/CHIP_T4_MSG.tmp
```

---

### Task 5: 发送路径 compose 化 + 文档同步 + 全量验证

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`（hasContent/onSend/onAction）
- Modify: `README.md`（快捷操作节）
- Modify: `docs/ARCHITECTURE.md`（gui 表 InputView 行）

**Interfaces:**
- Consumes: Task 1 的 `compose`；Task 2 的 `chips`/`chipRow`/`refreshChipRow`
- Produces: `String composedText()`、`void clearComposer()`（后续无任务依赖，但保持与设计一致）

- [ ] **Step 1: 新增 composedText/clearComposer，改 hasContent**

```java
    /** 块 + 文本组装发送文本 */
    private String composedText() {
        return InputChip.compose(chips, input.getText());
    }

    /** 发送后清空：块 + 文本区 + 弹层（块行 unmanaged 自动归位） */
    private void clearComposer() {
        chips.clear();
        chipRow.getChildren().clear();
        refreshChipRow();
        popup.hide();
        input.clear();
    }

    private boolean hasContent() {
        return !chips.isEmpty() || (input.getText() != null && !input.getText().trim().isEmpty());
    }
```

（替换现 `hasContent()` 方法体；`composedText`/`clearComposer` 放在 `hasContent` 附近。）

- [ ] **Step 2: 改 onSend**

```java
    private void onSend() {
        String text = composedText();
        if (text == null || text.trim().isEmpty()) return;
        clearComposer();
        SessionHandle target = current;
        if (target == null) {
            target = manager.createSession(null);
            if (target == null) return;
            manager.activateSession(target);
        }
        manager.dispatchCommand(target, text); // 斜杠命令本地分发；普通消息走 send
    }
```

- [ ] **Step 3: 改 onAction 的 SUPPLEMENT / ANSWER 分支**

```java
            case SUPPLEMENT: {
                String text = composedText();
                if (text == null || text.trim().isEmpty()) return;
                clearComposer();
                if (current != null) manager.sendSupplement(current, text);
                break;
            }
            case ANSWER: {
                String text = composedText();
                if (text == null || text.trim().isEmpty()) return;
                clearComposer();
                if (current != null) manager.sendAnswer(current, text);
                break;
            }
```

- [ ] **Step 4: README 快捷操作节补一条**

在 `README.md` 的 `@` 引用文件一行（第 25 行）后新增：

```markdown
- 补全确认反显为输入块：弹层选中的 /命令、@文件 与粘贴的 ≥100 字符长文本变为输入框上方不可编辑块，✕ 或空输入时 Backspace 删除；发送时块与文本按顺序组合（/命令仍须在消息开头）
```

- [ ] **Step 5: ARCHITECTURE.md gui 表补 InputChip**

在 `docs/ARCHITECTURE.md` 第 40 行（`input/SuggestionPopup、CompletionParser、Slash/FileSuggester` 行）后新增一行：

```markdown
| input/InputChip | 输入块模型与纯逻辑（compose 组装发送文本、粘贴 ≥100 字符变块阈值、弹层模式→块类型映射） |
```

- [ ] **Step 6: 全量测试**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test`
Expected: 全部 PASS（含新增 InputChipTest 10 例）

- [ ] **Step 7: 手工验证清单（启动 `minion.bat`）**

1. 输入 `/hel` → 弹层选 `/help` → 输入框上方出现蓝色块 `/help`，文本区无文本；点击块区域不弹窗
2. 再确认一个 `@` 文件 → 绿色块出现，块行自动换行布局
3. 粘贴一段 ≥100 字符文本 → 灰色「粘贴内容，N 字符」块；粘贴短文本 → 直接入文本区
4. 文本区为空时 Backspace → 依次删除块；块上 ✕ 点击删除单个块
5. 发送（Ctrl+Enter）→ 聊天区收到组合文本（如 `/help @src/a.java 继续`），发送后块与文本清空
6. 有块时按钮变亮可发送；仅文本时行为与改动前一致

- [ ] **Step 8: 提交**

Write `.git/CHIP_T5_MSG.tmp`：

```
feat: 发送路径组合输入块（compose）+ README/架构文档同步

Co-Authored-By: Claude <noreply@anthropic.com>
```

Run:

```bash
git add src/main/java/com/minion/gui/input/InputView.java README.md docs/ARCHITECTURE.md && git commit -F .git/CHIP_T5_MSG.tmp && rm .git/CHIP_T5_MSG.tmp
```
