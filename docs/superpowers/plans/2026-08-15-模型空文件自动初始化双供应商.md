# 模型配置文件为空时自动初始化双供应商 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ModelManager 在 model.json 缺失/空无效/损坏三种重建路径下自动初始化 deepseek + 千问两套模型配置，除 apiKey 外全部参数预填；已有合法文件零改动。

**Architecture:** 仅改 `core/config/ModelManager.java`：`createDefault()` 改名 `createDeepseek()`（内容不变），新增 `createQwen()` 静态工厂，`load()` 重建分支依次直插两套默认；测试同步更新与新增回归保护；README 补一句说明。其余类零改动（DeepSeekClient 已内置 qwen 供应商适配）。

**Tech Stack:** Java 8 + Maven + JUnit4（测试目录 `src/test/java`）

## Global Constraints

- JDK 8 兼容：语法/API 不超 Java 8（无 var、无 switch 表达式）
- 已有合法 model.json（含自定义模型）不得改动——零迁移，只影响重建分支
- 列表顺序 deepseek 在前，重建后 current = 列表第一个（deepseek）
- 文档、注释、commit 中文；commit 用 conventional 格式
- git commit 消息必须经 `-F 文件` 提交（bash wrapper 遇非 ASCII 崩溃；先写消息到 `.git/COMMIT_MSG_TMP.txt`）
- 测试命令：`JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ModelManagerTest`
- 提交信息末尾附 `Co-Authored-By: Claude <noreply@anthropic.com>`

---

### Task 1: ModelManager 双默认模板 + 测试同步（TDD）

**Files:**
- Modify: `src/main/java/com/minion/core/config/ModelManager.java:46-51`（重建分支）、`57-70`（createDefault）
- Modify: `src/test/java/com/minion/core/config/ModelManagerTest.java`

**Interfaces:**
- Produces: `ModelManager.createDeepseek()`、`ModelManager.createQwen()`（均 `public static ModelConfig`，参数零）；`createDefault()` 删除（无外部调用者，已 grep 确认）

- [ ] **Step 1: 更新测试断言到新期望**

修改 `ModelManagerTest.java` 全部受影响用例：

① `load_createsDefaultModel` → `load_createsDefaultModels`（缺失文件 → 2 模型 + qwen 字段 + apiKey 为空）：

```java
/** 无文件时生成 deepseek + qwen 双默认并落盘 */
@Test
public void load_createsDefaultModels() throws IOException {
    Path dir = jarDir();
    ModelManager m = ModelManager.load(dir);
    assertEquals(2, m.list().size());
    ModelConfig c = m.current();
    assertEquals("deepseek-v4-flash", c.displayName);
    assertEquals(900000, c.maxContextTokens);
    assertEquals("", c.apiKey);
    ModelConfig q = m.get("qwen3-max");
    assertNotNull(q);
    assertEquals("https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions", q.url);
    assertEquals("qwen3-max", q.modelName);
    assertEquals("qwen", q.provider);
    assertTrue(q.thinking);
    assertEquals(131072, q.maxContextTokens);
    assertEquals(0.8, q.compressThreshold, 1e-9);
    assertEquals(10, q.keepRecentMessages);
    assertEquals("", q.apiKey);
    assertTrue(Files.exists(dir.resolve("model.json")));
}
```

② `addAndReload_restoresModels`：`assertEquals(2, m2.list().size())` → `assertEquals(3, m2.list().size())`（双默认 + 新增 qwen-test），其余断言不变。

③ `remove_lastModelRejected` 重写（双默认下需先删到只剩一个再验证最后一个不可删）：

```java
/** 拒绝删除最后一个模型 */
@Test
public void remove_lastModelRejected() throws IOException {
    Path dir = jarDir();
    ModelManager m = ModelManager.load(dir);
    assertTrue(m.remove("deepseek-v4-flash")); // 先删到只剩一个
    assertEquals(1, m.list().size());
    assertFalse(m.remove(m.currentName()));    // 最后一个不可删
    assertEquals(1, m.list().size());
}
```

④ `remove_otherModelOkAndCurrentFallsBack` 重写（切到千问后删当前 → 回退第一个）：

```java
/** 删除当前模型成功，current 回退到剩余第一个 */
@Test
public void remove_otherModelOkAndCurrentFallsBack() throws IOException {
    Path dir = jarDir();
    ModelManager m = ModelManager.load(dir);
    m.setCurrent("qwen3-max");
    assertTrue(m.remove("deepseek-v4-flash"));
    assertEquals(1, m.list().size());
    assertEquals("qwen3-max", m.current().displayName);
}
```

⑤ `load_corruptFileBacksUpAndRebuilds`：`assertEquals(1, ...)` → `assertEquals(2, m.list().size())`，.bak 断言不变。

⑥ `load_structureInvalidJsonBacksUpAndRebuilds`：`assertEquals(1, ...)` → `assertEquals(2, m.list().size())`，其余不变。

⑦ `load_nullDisplayNameEntryFiltered`：`assertEquals(1, ...)` → `assertEquals(2, m.list().size())`，其余不变。

⑧ `save_atomicWriteNoTmpAndReparsable`：`assertEquals(2, models.size())` → `assertEquals(3, models.size())`（双默认 + qwen-test）。

- [ ] **Step 2: 运行测试确认失败**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ModelManagerTest`
Expected: FAIL——重建路径用例断言 1 模型实际 2、qwen 断言失败、`remove_lastModelRejected` 因双默认下删除成功而失败

- [ ] **Step 3: 实现 ModelManager 双默认**

`ModelManager.java` 重建分支（46-51 行区域）改为：

```java
        if (!loaded) {
            if (Files.exists(m.file)) backupCorrupt(m.file); // 文件存在但不可用：先备份再重建，避免配置丢失
            m.models.add(createDeepseek());
            m.models.add(createQwen());
            m.currentName = m.models.get(0).displayName;
            m.save();
        }
```

`createDefault()` 改名为 `createDeepseek()`（内容不变），其后新增：

```java
    /** 千问（阿里百炼 DashScope OpenAI 兼容模式）；reasoningEffort 对 qwen 无效，客户端仅发 enable_thinking */
    public static ModelConfig createQwen() {
        ModelConfig c = new ModelConfig();
        c.displayName = "qwen3-max";
        c.url = "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions";
        c.apiKey = "";
        c.modelName = "qwen3-max";
        c.provider = "qwen";
        c.thinking = true;
        c.reasoningEffort = "max";
        c.maxContextTokens = 131072;
        c.compressThreshold = 0.8;
        c.keepRecentMessages = 10;
        return c;
    }
```

- [ ] **Step 4: 运行测试确认全绿**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ModelManagerTest`
Expected: PASS（10 个用例全过）

- [ ] **Step 5: 提交**

```bash
# 先写消息文件（避免 wrapper 中文崩溃）：
# 内容：
#   feat: model.json 为空时自动初始化 deepseek+千问双默认配置
#
#   Co-Authored-By: Claude <noreply@anthropic.com>
git add src/main/java/com/minion/core/config/ModelManager.java src/test/java/com/minion/core/config/ModelManagerTest.java
git commit -F .git/COMMIT_MSG_TMP.txt && rm .git/COMMIT_MSG_TMP.txt
```

---

### Task 2: 合法文件零迁移回归保护

**Files:**
- Modify: `src/test/java/com/minion/core/config/ModelManagerTest.java`

**Interfaces:**
- Consumes: `ModelManager.load(Path)`（Task 1 后行为：合法文件原样加载）

- [ ] **Step 1: 新增回归测试**

```java
/** 合法已有配置（仅 deepseek 自定义模型）：重载后原样保留，不追加千问、不备份（零迁移） */
@Test
public void load_validExistingFileUntouched() throws IOException {
    Path dir = jarDir();
    String json = "{\"models\":[{\"displayName\":\"custom-deepseek\",\"url\":\"http://custom\",\"apiKey\":\"sk-x\","
            + "\"modelName\":\"deepseek-custom\",\"provider\":\"deepseek\",\"thinking\":false,"
            + "\"maxContextTokens\":64000,\"compressThreshold\":0.6,\"keepRecentMessages\":5}],"
            + "\"currentModelName\":\"custom-deepseek\"}";
    Files.write(dir.resolve("model.json"), json.getBytes(StandardCharsets.UTF_8));
    ModelManager m = ModelManager.load(dir);
    assertEquals(1, m.list().size());
    ModelConfig c = m.current();
    assertEquals("custom-deepseek", c.displayName);
    assertEquals("http://custom", c.url);
    assertEquals("sk-x", c.apiKey);
    assertFalse(c.thinking);
    assertEquals(64000, c.maxContextTokens);
    assertFalse(Files.exists(dir.resolve("model.json.bak")));
}
```

- [ ] **Step 2: 运行确认通过（回归保护，行为本就不变）**

Run: `JAVA_HOME="E:/javame/jdk8" mvn test -Dtest=ModelManagerTest`
Expected: PASS（11 个用例全过）

- [ ] **Step 3: 提交**

```bash
# 消息文件内容：
#   test: 合法 model.json 零迁移回归保护
#
#   Co-Authored-By: Claude <noreply@anthropic.com>
git add src/test/java/com/minion/core/config/ModelManagerTest.java
git commit -F .git/COMMIT_MSG_TMP.txt && rm .git/COMMIT_MSG_TMP.txt
```

---

### Task 3: README 同步 + 全量验证

**Files:**
- Modify: `README.md`（「模型供应商配置（deepseek / qwen）」段 说明 列表）

- [ ] **Step 1: README 补充初始化说明**

在 `README.md` 第 82 行（`- 模板参考：...`）前插入一行：

```markdown
- `model.json` 缺失/为空/损坏时自动生成 deepseek + 千问两套配置（除 key 外按各自调用参数预填，key 留空待填）
```

- [ ] **Step 2: 全量验证**

Run: `JAVA_HOME="E:/javame/jdk8" mvn compile && JAVA_HOME="E:/javame/jdk8" mvn test`
Expected: 编译通过、全部测试通过（含 ModelManagerTest 11 用例）

- [ ] **Step 3: 提交**

```bash
# 消息文件内容：
#   docs: README 补充 model.json 为空时自动初始化双供应商说明
#
#   Co-Authored-By: Claude <noreply@anthropic.com>
git add README.md
git commit -F .git/COMMIT_MSG_TMP.txt && rm .git/COMMIT_MSG_TMP.txt
```
