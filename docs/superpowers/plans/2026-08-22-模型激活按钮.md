# 模型激活按钮 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 设置窗模型页取消"单击即激活"，改为底部"激活"按钮显式切换；选中已激活模型时按钮置灰。

**Architecture:** 纯判定逻辑提取为 `SettingsDialog.canActivate(selectedName, currentName)` 包可见静态方法（可单测）；`modelPane` 移除单击激活监听，新增激活按钮 + 选中项监听联动 disable 状态；激活动作沿用 `ModelManager.setCurrent + SessionManager.applyModelChanged`（需求 13 全量生效）。

**Tech Stack:** JDK 8 + JavaFX 8 + Maven；JUnit 4（无 JavaFX toolkit，GUI 测试延续项目惯例只测纯逻辑静态方法）。

## Global Constraints

- JDK 8 兼容（lambda 可用，不用更高版本语法）；不新增依赖、不新增配置项
- **工作目录**：所有命令在 worktree 内执行：`cd .claude/worktrees/feat-model-activate-button`（相对主仓库根 `E:\javame\code\code2\minion`）
- **构建/测试**：本机 `JAVA_HOME` 已指向 JDK 8（`E:\javame\jdk8`，含 JavaFX），直接用 `mvn test` / `mvn clean package`（CLAUDE.md 中 `D:/javame/jdk1.8` 为旧机器路径，勿用）
- 文档、注释、commit 用中文；commit 用 conventional 格式（如 `feat(gui): ...`）
- 基线：551 测试全部通过；完成后全量回归必须保持通过
- 设计文档：`docs/superpowers/specs/2026-08-22-模型激活按钮-design.md`

---

### Task 1: canActivate 判定方法（TDD）

**Files:**
- Modify: `src/main/java/com/minion/gui/dialog/SettingsDialog.java`（在 `refresh` 方法后，约 169 行后新增方法）
- Test: `src/test/java/com/minion/gui/dialog/SettingsDialogTest.java`（在类末尾 `}` 前追加）

**Interfaces:**
- Produces: `static boolean SettingsDialog.canActivate(String selectedName, String currentName)` —— 选中项非 null 且与当前模型名不同时返回 true。Task 2 的按钮状态与激活防护依赖此签名。

- [ ] **Step 1: 写失败测试**

在 `SettingsDialogTest.java` 类末尾 `}` 之前追加：

```java
    // ===== 模型页激活判定（纯逻辑，无 JavaFX） =====

    /** 无选中（null）或选中的就是当前模型 → 不可激活 */
    @Test
    public void canActivate_rejectsNullOrCurrent() {
        assertFalse(SettingsDialog.canActivate(null, "deepseek-v4-flash"));
        assertFalse(SettingsDialog.canActivate("deepseek-v4-flash", "deepseek-v4-flash"));
    }

    /** 选中了其它模型 → 可激活 */
    @Test
    public void canActivate_allowsDifferentModel() {
        assertTrue(SettingsDialog.canActivate("qwen3-max", "deepseek-v4-flash"));
    }
```

（文件已有 `import static org.junit.Assert.*;`，无需新 import。）

- [ ] **Step 2: 运行确认失败**

```bash
mvn test -Dtest=SettingsDialogTest
```

预期：编译失败，错误为 `找不到符号: 方法 canActivate(...)`。

- [ ] **Step 3: 最小实现**

在 `SettingsDialog.java` 的 `refresh` 方法之后追加：

```java
    /** 选中项可激活：非空且不同于当前模型（模型页「激活」按钮启用/置灰判定） */
    static boolean canActivate(String selectedName, String currentName) {
        return selectedName != null && !selectedName.equals(currentName);
    }
```

- [ ] **Step 4: 运行确认通过**

```bash
mvn test -Dtest=SettingsDialogTest
```

预期：`Tests run: 8, Failures: 0, Errors: 0`（原 6 + 新 2）。

- [ ] **Step 5: 提交**

```bash
git add src/main/java/com/minion/gui/dialog/SettingsDialog.java src/test/java/com/minion/gui/dialog/SettingsDialogTest.java
git commit -m "feat(gui): 模型页激活判定方法 canActivate（选中非当前模型才可激活）"
```

---

### Task 2: 模型页交互改造 + README 同步

**Files:**
- Modify: `src/main/java/com/minion/gui/dialog/SettingsDialog.java:103-120,154`（`modelPane` 内）
- Modify: `README.md:37`

**Interfaces:**
- Consumes: Task 1 的 `static boolean SettingsDialog.canActivate(String, String)`
- Consumes: `ModelManager.setCurrent(String)`、`ModelManager.currentName()`、`SessionManager.applyModelChanged()`、`refresh(list, models)`（均已有，签名不变）

- [ ] **Step 1: 移除单击激活监听、新增激活按钮**

将 `modelPane` 中以下原代码（103-120 行）：

```java
        list.setOnMouseClicked(e -> {
            int idx = list.getSelectionModel().getSelectedIndex();
            if (idx < 0 || e.getClickCount() != 1) return;
            String name = list.getItems().get(idx);
            if (!name.equals(models.currentName())) {
                models.setCurrent(name);
                manager.applyModelChanged(); // 需求 13：切换模型全量生效
            }
            refresh(list, models);
        });

        HBox actions = new HBox(8);
        Button add = new Button("新建");
        Button edit = new Button("修改");
        Button del = new Button("删除");
        add.getStyleClass().add("btn-ghost");
        edit.getStyleClass().add("btn-ghost");
        del.getStyleClass().add("btn-ghost");
```

替换为：

```java
        HBox actions = new HBox(8);
        Button activate = new Button("激活");
        Button add = new Button("新建");
        Button edit = new Button("修改");
        Button del = new Button("删除");
        activate.getStyleClass().add("btn-ghost");
        add.getStyleClass().add("btn-ghost");
        edit.getStyleClass().add("btn-ghost");
        del.getStyleClass().add("btn-ghost");
        activate.setDisable(true); // 初始置灰：首次 refresh 已选中当前模型（监听注册在后，首次选中不触发）
        activate.setOnAction(e -> {
            int idx = list.getSelectionModel().getSelectedIndex();
            if (idx < 0) return;
            String name = list.getItems().get(idx);
            if (!canActivate(name, models.currentName())) return; // 双保险：已激活不可重复激活
            models.setCurrent(name);
            manager.applyModelChanged(); // 需求 13：切换模型全量生效
            refresh(list, models);
        });
        // 选中项变化（鼠标点击/键盘导航/refresh 重选）联动按钮状态
        list.getSelectionModel().selectedItemProperty().addListener(
                (obs, o, n) -> activate.setDisable(!canActivate(n, models.currentName())));
```

- [ ] **Step 2: 按钮加入动作栏（激活放最前）**

将：

```java
        actions.getChildren().addAll(add, edit, del);
```

改为：

```java
        actions.getChildren().addAll(activate, add, edit, del);
```

- [ ] **Step 3: README 同步（37 行）**

将：

```
- ⚙ 设置（右上角）：左列导航（基础设置 / 模型 / MCP / 关于）；切换模型、修改参数即时生效（运行中会话下一轮生效）；基础设置页底部按钮栏「应用」（保存不关窗）与「关闭」
```

改为：

```
- ⚙ 设置（右上角）：左列导航（基础设置 / 模型 / MCP / 关于）；模型页单击仅选中模型（查看配置用「修改」），选中后点「激活」按钮切换，选中已激活模型时按钮置灰；切换/修改参数即时生效（运行中会话下一轮生效）；基础设置页底部按钮栏「应用」（保存不关窗）与「关闭」
```

- [ ] **Step 4: 全量回归**

```bash
mvn test
```

预期：`Tests run: 553, Failures: 0, Errors: 0`、`BUILD SUCCESS`。

- [ ] **Step 5: 手动 GUI 验证**

```bash
mvn -q clean package -DskipTests && start java -jar target/minion-0.1.0.jar
```

（`start` 后台启动，避免阻塞终端；GUI 交互需人工确认，此步骤的验证结论由用户给出。）

打开 ⚙ 设置 → 模型页，逐条确认：
1. 单击非当前模型行：仅选中高亮，标题栏模型名**不变**（未切换）
2. 选中非当前模型：底部「激活」按钮可点击（按钮顺序：激活|新建|修改|删除）
3. 点击「激活」：● 移到该行、标题栏模型名更新、「激活」按钮置灰
4. 选中已激活（带 ●）模型：「激活」按钮置灰
5. 键盘上下键在列表移动选中：按钮状态随选中项联动
6. 「新建/修改/删除」行为不变（修改表单正常打开，即"查看配置"路径）

验证完毕后关闭 GUI。

- [ ] **Step 6: 提交**

```bash
git add src/main/java/com/minion/gui/dialog/SettingsDialog.java README.md
git commit -m "feat(gui): 模型页取消单击即激活，改为底部激活按钮（已激活置灰）"
```
