# 运行状态指示器（齿轮 + 动态文案）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在正文区左下角悬浮转动的齿轮 + 动态文案（10 秒随机轮换"正在加载中.../可随时补充信息..."，上下文压缩时固定"上下文压缩中..."），仅当前激活会话运行时显示。

**Architecture:** 独立组件 RunningIndicator（齿轮 SVGPath 旋转 Timeline + 10s 文案轮换 Timeline）挂载在 MainWindow 的 chatScroll 外层 StackPane 左下角；压缩状态经新事件链路 AgentUi.onCompressingChanged → SessionController → SessionManager.Listener → MainWindow 传播。

**Tech Stack:** JavaFX 8（Timeline/KeyValue 动画，同 StatusDot 模式）、JUnit4、Maven。

## Global Constraints

- JDK 8 兼容（`JAVA_HOME="D:/javame/jdk1.8" mvn ...`）；不新增依赖
- 所有动画 Timeline 隐藏前必须 stop（StatusDot 防泄漏规约）
- AgentUi / SessionManager.Listener 新增方法必须为 `default` 空实现（不破坏现有实现）
- 压缩状态传播必须 try/finally 保证异常后复位
- 注释、commit 用中文，commit 用 conventional 格式
- 资源目录是 src/resource（非 src/main/resources）

---

### Task 1: core 层压缩事件（AgentUi + AgentLoop）

**Files:**
- Modify: `src/main/java/com/minion/core/agent/AgentUi.java`（接口末尾加 default 方法）
- Modify: `src/main/java/com/minion/core/agent/AgentLoop.java`（compactNow() 与 runUserTurn 自动压缩两处）
- Modify: `src/test/java/com/minion/core/agent/RecordingUi.java`（记录压缩事件）
- Test: `src/test/java/com/minion/core/agent/AgentLoopCompactTest.java`

**Interfaces:**
- Produces: `AgentUi.onCompressingChanged(boolean compressing)`（default 空实现）；Task 2 的 SessionController 将覆写它，Task 1 测试经 RecordingUi 断言

- [ ] **Step 1: 写失败测试** — 在 AgentLoopCompactTest.java 末尾追加两个测试，并给 RecordingUi 加记录字段

RecordingUi.java 追加（import java.util.ArrayList 已有）：
```java
    public final List<Boolean> compressing = new ArrayList<Boolean>();

    @Override public synchronized void onCompressingChanged(boolean compressing) {
        this.compressing.add(compressing);
    }
```

AgentLoopCompactTest.java 追加（import java.util.Arrays）：
```java
    /** 自动压缩：压缩前后发 onCompressingChanged，首事件 true、末事件 false */
    @Test
    public void autoCompress_firesCompressingEvents() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】被压缩的历史";
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(50, 0.8, 2, llm, 0);
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.roundLimit = 10;
        for (int i = 0; i < 3; i++) {
            llm.addTurn("回复" + i);
            loop.runUserTurn("问题" + i);
        }
        assertFalse("应触发自动压缩", ui.compressing.isEmpty());
        assertTrue("首事件=压缩开始", ui.compressing.get(0));
        assertFalse("末事件=压缩结束", ui.compressing.get(ui.compressing.size() - 1));
    }

    /** 手动 /compress：严格 true→false 成对 */
    @Test
    public void compactNow_firesCompressingEvents() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】手动压缩";
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(100000, 0.8, 1, llm, 0); // 大阈值不自动压缩
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        llm.addTurn("回复");
        loop.runUserTurn("问题");
        ui.compressing.clear();
        loop.compactNow();
        assertEquals(Arrays.asList(true, false), ui.compressing);
    }
```

- [ ] **Step 2: 运行测试确认失败**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=AgentLoopCompactTest
```
Expected: 编译失败（AgentUi 无 onCompressingChanged，RecordingUi 覆写报错）→ 确认测试先行。

- [ ] **Step 3: AgentUi 加 default 方法**

AgentUi.java 接口末尾（onWarning 之后）：
```java
    /** 上下文压缩进行中（true=开始，false=结束；同步阻塞压缩前后成对发出） */
    default void onCompressingChanged(boolean compressing) { }
```

- [ ] **Step 4: AgentLoop 两处压缩点 try/finally 包裹**

compactNow() 改为：
```java
    public void compactNow() {
        if (contextManager == null) {
            ui.onWarning("未启用上下文压缩");
            return;
        }
        ui.onCompressingChanged(true);
        try {
            int before = session.messages.size();
            session.messages = contextManager.compress(session.messages);
            if (session.messages.size() < before) {
                ui.onWarning("已压缩上下文（历史摘要已置前）");
            } else {
                ui.onWarning("暂无可压缩内容");
            }
        } finally {
            ui.onCompressingChanged(false);
        }
    }
```

runUserTurn 循环内自动压缩分支改为：
```java
                if (contextManager != null && contextManager.shouldCompress(session.messages)) {
                    ui.onCompressingChanged(true);
                    try {
                        int before = session.messages.size();
                        session.messages = contextManager.compress(session.messages);
                        if (session.messages.size() < before) {
                            int pct = (int) (contextManager.estimate(session.messages) * 100
                                    / contextManager.maxTokens());
                            ui.onWarning("上下文已达 " + pct + "%，已自动压缩历史（技能不受影响）");
                        }
                    } finally {
                        ui.onCompressingChanged(false);
                    }
                }
```

- [ ] **Step 5: 运行测试确认通过**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=AgentLoopCompactTest
```
Expected: 4 个测试全 PASS（原有 2 + 新增 2）。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/minion/core/agent/AgentUi.java src/main/java/com/minion/core/agent/AgentLoop.java src/test/java/com/minion/core/agent/RecordingUi.java src/test/java/com/minion/core/agent/AgentLoopCompactTest.java
git commit -m "feat: 上下文压缩状态事件（AgentUi.onCompressingChanged 链路起点）"
```

---

### Task 2: GUI 转发链路（SessionController + SessionManager）

**Files:**
- Modify: `src/main/java/com/minion/gui/session/SessionController.java`
- Modify: `src/main/java/com/minion/gui/session/SessionManager.java`（Listener 接口 + notifyCompressingChanged + createSession/恢复会话两处注入）
- Test: `src/test/java/com/minion/gui/session/SessionControllerTest.java`
- Test: `src/test/java/com/minion/gui/session/SessionManagerTest.java`

**Interfaces:**
- Consumes: Task 1 的 `AgentUi.onCompressingChanged(boolean)`
- Produces: `SessionManager.Listener.onCompressingChanged(SessionHandle h, boolean compressing)`（default 空实现）；`SessionController.setCompressingStateListener(Consumer<Boolean>)`；Task 4 的 MainWindow 实现该 Listener 方法

- [ ] **Step 1: 写失败测试**

SessionControllerTest.java 追加（仿现有测试风格，匿名 Consumer）：
```java
    /** 压缩状态回调：onCompressingChanged 转发到 compressingStateListener */
    @Test
    public void onCompressingChanged_forwardsToListener() {
        SessionController c = new SessionController();
        final java.util.List<Boolean> got = new java.util.ArrayList<Boolean>();
        c.setCompressingStateListener(new java.util.function.Consumer<Boolean>() {
            @Override public void accept(Boolean b) { got.add(b); }
        });
        c.onCompressingChanged(true);
        c.onCompressingChanged(false);
        assertEquals(java.util.Arrays.asList(true, false), got);
    }
```

SessionManagerTest.java 追加：
```java
    /** 压缩状态：会话控制器事件 → Listener.onCompressingChanged（携带会话句柄） */
    @Test
    public void compressingChanged_notifiesListener() throws Exception {
        SessionManager m = newManager();
        final java.util.List<Boolean> got = new java.util.ArrayList<Boolean>();
        final SessionHandle[] gotH = new SessionHandle[1];
        m.addListener(new SessionManager.Listener() {
            @Override public void onSessionRunningChanged(SessionHandle h, boolean running) { }
            @Override public void onSessionTitleChanged(SessionHandle h) { }
            @Override public void onCompressingChanged(SessionHandle h, boolean compressing) {
                gotH[0] = h;
                got.add(compressing);
            }
        });
        SessionHandle h = m.createSession(null);
        h.controller.onCompressingChanged(true);
        h.controller.onCompressingChanged(false);
        assertSame(h, gotH[0]);
        assertEquals(java.util.Arrays.asList(true, false), got);
    }
```

- [ ] **Step 2: 运行测试确认失败**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=SessionControllerTest,SessionManagerTest
```
Expected: 编译失败（setCompressingStateListener / onCompressingChanged(Listener) 不存在）。

- [ ] **Step 3: SessionController 加转发**

在 askStateListener 声明之后加：
```java
    /** 上下文压缩状态回调（true=开始，false=结束），SessionManager 注入 */
    private volatile java.util.function.Consumer<Boolean> compressingStateListener;

    public void setCompressingStateListener(java.util.function.Consumer<Boolean> l) { this.compressingStateListener = l; }
```

类内加覆写（onAskUserDone 之后）：
```java
    @Override public void onCompressingChanged(boolean compressing) {
        if (compressingStateListener != null) compressingStateListener.accept(compressing);
    }
```

- [ ] **Step 4: SessionManager 加 Listener 方法 + 转发 + 两处注入**

Listener 接口（onSessionAskChanged 之后）加：
```java
        /** 上下文压缩状态变化（true=压缩中；仅当前激活会话时 GUI 显示） */
        default void onCompressingChanged(SessionHandle h, boolean compressing) { }
```

notifyAskChanged 附近加：
```java
    private void notifyCompressingChanged(SessionHandle h, boolean compressing) {
        for (Listener l : listeners) l.onCompressingChanged(h, compressing);
    }
```

createSession 中 `controller.setAskStateListener(...)` 块之后加：
```java
        controller.setCompressingStateListener(new java.util.function.Consumer<Boolean>() {
            @Override public void accept(Boolean compressing) {
                notifyCompressingChanged(h, compressing);
            }
        });
```

恢复会话（restoreSessions 中 `controller.setAskStateListener(...)` 块之后）同样加一份（注释：恢复会话也接线压缩状态转发）。

- [ ] **Step 5: 运行测试确认通过**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=SessionControllerTest,SessionManagerTest
```
Expected: 全 PASS（含新增 2 个）。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/minion/gui/session/SessionController.java src/main/java/com/minion/gui/session/SessionManager.java src/test/java/com/minion/gui/session/SessionControllerTest.java src/test/java/com/minion/gui/session/SessionManagerTest.java
git commit -m "feat: 压缩状态 GUI 转发链路（SessionController→SessionManager.Listener）"
```

---

### Task 3: RunningIndicator 组件

**Files:**
- Create: `src/main/java/com/minion/gui/RunningIndicator.java`
- Test: `src/test/java/com/minion/gui/RunningIndicatorTest.java`

**Interfaces:**
- Consumes: 无（独立组件）
- Produces: `RunningIndicator.setRunning(boolean)`、`setCompressing(boolean)`；静态 `pickText(Random)`、`displayText(boolean, String)`、常量 `ROTATING_TEXTS` / `COMPRESSING_TEXT`。Task 4 挂载到 MainWindow

- [ ] **Step 1: 写失败测试** — 新建 RunningIndicatorTest.java（纯静态逻辑，无 JavaFX toolkit，同 InputViewButtonTest 风格）

```java
package com.minion.gui;

import org.junit.Test;

import java.util.HashSet;
import java.util.Random;
import java.util.Set;

import static org.junit.Assert.*;

/** RunningIndicator 纯逻辑测试：文案池随机、压缩态优先级（组件本体需 FX toolkit，动画不单测） */
public class RunningIndicatorTest {

    /** 轮换文案随机选择：多次取样全部落在池内 */
    @Test
    public void pickText_staysInPool() {
        Random rnd = new Random(42);
        Set<String> pool = new HashSet<String>();
        for (String s : RunningIndicator.ROTATING_TEXTS) pool.add(s);
        for (int i = 0; i < 200; i++) {
            assertTrue("随机文案必须在池内: " + RunningIndicator.pickText(rnd),
                    pool.contains(RunningIndicator.pickText(rnd)));
        }
    }

    /** 池内恰有两个轮换文案（需求：正在加载中/可随时补充信息） */
    @Test
    public void rotatingPool_hasTwoEntries() {
        assertEquals(2, RunningIndicator.ROTATING_TEXTS.length);
        assertEquals("正在加载中...", RunningIndicator.ROTATING_TEXTS[0]);
        assertEquals("可随时补充信息...", RunningIndicator.ROTATING_TEXTS[1]);
    }

    /** 压缩中固定显示压缩文案，不参与轮换 */
    @Test
    public void displayText_compressingOverrides() {
        assertEquals("上下文压缩中...", RunningIndicator.COMPRESSING_TEXT);
        assertEquals("上下文压缩中...", RunningIndicator.displayText(true, "正在加载中..."));
        assertEquals("正在加载中...", RunningIndicator.displayText(false, "正在加载中..."));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=RunningIndicatorTest
```
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 新建 RunningIndicator.java**

```java
package com.minion.gui;

import javafx.animation.Animation;
import javafx.animation.KeyFrame;
import javafx.animation.KeyValue;
import javafx.animation.Timeline;
import javafx.scene.control.Label;
import javafx.scene.layout.HBox;
import javafx.scene.shape.SVGPath;
import javafx.util.Duration;

import java.util.Random;

/**
 * 运行状态指示器：正文区左下角悬浮（齿轮旋转 + 文案轮换）。
 * 齿轮 2s/圈旋转；文案每 10s 随机轮换（正在加载中.../可随时补充信息...）；
 * 上下文压缩中固定显示「上下文压缩中...」（不参与轮换，压缩结束恢复）。
 * 动画规约同 StatusDot：隐藏前必须停全部 Timeline，防动画引用节点泄漏。
 * 挂载方式：放入 StackPane 并 setAlignment(BOTTOM_LEFT)（本组件不管理布局）。
 */
public class RunningIndicator extends HBox {

    /** 轮换文案池（随机选择，允许连续相同） */
    static final String[] ROTATING_TEXTS = {"正在加载中...", "可随时补充信息..."};
    /** 压缩固定文案（只在压缩时显示，不参与轮换） */
    static final String COMPRESSING_TEXT = "上下文压缩中...";
    /** 齿轮旋转周期（2s/圈） */
    static final double SPIN_MS = 2000;
    /** 文案轮换间隔（10s） */
    static final double ROTATE_INTERVAL_MS = 10000;

    /** Material settings 齿轮路径（24×24 viewport，缩放 0.7 显示） */
    private static final String GEAR_PATH = "M19.14,12.94c0.04,-0.3 0.06,-0.61 0.06,-0.94c0,-0.32 -0.02,-0.64 -0.07,-0.94"
            + "l2.03,-1.58c0.18,-0.14 0.23,-0.41 0.12,-0.61l-1.92,-3.32c-0.12,-0.22 -0.37,-0.29 -0.59,-0.22"
            + "l-2.39,0.96c-0.5,-0.38 -1.03,-0.7 -1.62,-0.94L14.4,2.81c-0.04,-0.24 -0.24,-0.41 -0.48,-0.41"
            + "h-3.84c-0.24,0 -0.43,0.17 -0.47,0.41L9.25,5.35C8.66,5.59 8.12,5.92 7.63,6.29L5.24,5.33"
            + "c-0.22,-0.08 -0.47,0 -0.59,0.22L2.74,8.87C2.62,9.08 2.66,9.34 2.86,9.48l2.03,1.58"
            + "C4.84,11.36 4.8,11.69 4.8,12s0.02,0.64 0.07,0.94l-2.03,1.58c-0.18,0.14 -0.23,0.41 -0.12,0.61"
            + "l1.92,3.32c0.12,0.22 0.37,0.29 0.59,0.22l2.39,-0.96c0.5,0.38 1.03,0.7 1.62,0.94l0.36,2.54"
            + "c0.05,0.24 0.24,0.41 0.48,0.41h3.84c0.24,0 0.44,-0.17 0.47,-0.41l0.36,-2.54"
            + "c0.59,-0.24 1.13,-0.56 1.62,-0.94l2.39,0.96c0.22,0.08 0.47,0 0.59,-0.22l1.92,-3.32"
            + "c0.12,-0.22 0.07,-0.47 -0.12,-0.61L19.14,12.94zM12,15.6c-1.98,0 -3.6,-1.62 -3.6,-3.6"
            + "s1.62,-3.6 3.6,-3.6s3.6,1.62 3.6,3.6S13.98,15.6 12,15.6z";

    private final SVGPath gear = new SVGPath();
    private final Label text = new Label();
    private final Random rnd = new Random();
    private Timeline spin;       // 齿轮旋转动画
    private Timeline rotateText; // 10s 文案轮换动画
    private boolean running;
    private boolean compressing;

    public RunningIndicator() {
        getStyleClass().add("running-indicator");
        gear.setContent(GEAR_PATH);
        gear.getStyleClass().add("running-indicator-gear");
        gear.setScaleX(0.7);
        gear.setScaleY(0.7);
        text.getStyleClass().add("running-indicator-text");
        getChildren().addAll(gear, text);
        setVisible(false); // 初始隐藏（空闲）；位置由挂载方 StackPane 对齐决定
    }

    /** 从轮换池随机取一个文案（纯静态可单测；允许连续相同，符合"随机"语义） */
    static String pickText(Random rnd) {
        return ROTATING_TEXTS[rnd.nextInt(ROTATING_TEXTS.length)];
    }

    /** 文案优先级：压缩中固定压缩文案，否则当前轮换文案（纯静态可单测） */
    static String displayText(boolean compressing, String current) {
        return compressing ? COMPRESSING_TEXT : current;
    }

    /** 运行状态：false → 整体隐藏 + 停止全部动画（防泄漏）+ 复位压缩态；true → 显示 + 启动动画 */
    public void setRunning(boolean running) {
        this.running = running;
        if (!running) {
            compressing = false;
            stopAnimations();
            setVisible(false);
            return;
        }
        setVisible(true);
        text.setText(displayText(compressing, pickText(rnd)));
        startAnimations();
    }

    /** 压缩状态：true → 固定压缩文案并暂停轮换；false → 恢复轮换（仅运行态生效） */
    public void setCompressing(boolean compressing) {
        this.compressing = compressing;
        if (!running) return;
        text.setText(displayText(compressing, pickText(rnd)));
        if (compressing) {
            if (rotateText != null) rotateText.stop();
        } else {
            startRotateText();
        }
    }

    private void startAnimations() {
        if (spin == null || spin.getStatus() != Animation.Status.RUNNING) {
            spin = new Timeline(
                    new KeyFrame(Duration.ZERO, new KeyValue(gear.rotateProperty(), 0)),
                    new KeyFrame(Duration.millis(SPIN_MS), new KeyValue(gear.rotateProperty(), 360)));
            spin.setCycleCount(Animation.INDEFINITE);
            spin.play();
        }
        startRotateText();
    }

    private void startRotateText() {
        if (compressing || rotateText != null && rotateText.getStatus() == Animation.Status.RUNNING) return;
        rotateText = new Timeline(new KeyFrame(Duration.millis(ROTATE_INTERVAL_MS),
                e -> text.setText(displayText(compressing, pickText(rnd)))));
        rotateText.setCycleCount(Animation.INDEFINITE);
        rotateText.play();
    }

    /** 停止全部动画并置空引用（组件隐藏前必须调用；动画强引用节点防泄漏） */
    private void stopAnimations() {
        if (spin != null) { spin.stop(); spin = null; }
        if (rotateText != null) { rotateText.stop(); rotateText = null; }
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn test -Dtest=RunningIndicatorTest
```
Expected: 3 个测试全 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/minion/gui/RunningIndicator.java src/test/java/com/minion/gui/RunningIndicatorTest.java
git commit -m "feat: RunningIndicator 组件（齿轮旋转动画 + 10s 文案轮换 + 压缩态）"
```

---

### Task 4: MainWindow 挂载 + 状态同步 + theme.css

**Files:**
- Modify: `src/main/java/com/minion/gui/MainWindow.java`
- Modify: `src/resource/theme/theme.css`
- 验证: 全量 mvn test + 启动冒烟

**Interfaces:**
- Consumes: Task 3 的 `RunningIndicator.setRunning/setCompressing`；Task 2 的 `SessionManager.Listener.onCompressingChanged`

- [ ] **Step 1: MainWindow 挂载 chatHost（StackPane 叠加）**

在字段区加 `private RunningIndicator runningIndicator;`（在 `private InputView inputView;` 附近）。

`show()` 中 `inputView = new InputView(...)` 之后，把 chatScroll 包进 StackPane：
```java
        // 运行状态指示器：正文区左下角悬浮（齿轮+动态文案），StackPane 叠加于消息区之上，不占布局
        runningIndicator = new RunningIndicator();
        StackPane chatHost = new StackPane(chatScroll, runningIndicator);
        StackPane.setAlignment(runningIndicator, Pos.BOTTOM_LEFT);
        StackPane.setMargin(runningIndicator, new Insets(0, 0, 10, 14));
        StackPane.setFillWidth(runningIndicator, false);  // 防 StackPane 默认拉伸铺满
        StackPane.setFillHeight(runningIndicator, false);
        VBox.setVgrow(chatHost, Priority.ALWAYS); // 原 chatScroll 的 vgrow 约束移交 chatHost
```
并将 `right.getChildren().setAll(tabsBar, chatScroll, inputView);` 改为 `right.getChildren().setAll(tabsBar, chatHost, inputView);`。

- [ ] **Step 2: 状态同步（三处）**

1. `clearChatPane()` 末尾加：
```java
        runningIndicator.setRunning(false); // 无活动会话：指示器隐藏并停动画
        runningIndicator.setCompressing(false);
```

2. `onSessionActivated` 的缓存命中分支（`inputView.bindSession(h); sessionList.refresh(); return;` 处）与新会话分支（末尾 `sessionList.refresh();` 处）各加：
```java
                    runningIndicator.setRunning(h.running); // 切会话：按新会话运行态刷新指示器
                    runningIndicator.setCompressing(false); // 切会话复位压缩态（压缩事件按会话隔离）
```

3. `onSessionRunningChanged` 回调改为：
```java
            @Override public void onSessionRunningChanged(SessionHandle h, boolean running) {
                Platform.runLater(() -> updateTab(h));
                if (inputView != null) inputView.onRunningChanged(h, running);
                if (chatView != null && chatView.handle() == h) { // 仅当前激活会话反映到指示器
                    runningIndicator.setRunning(running);
                    runningIndicator.setCompressing(false); // 运行结束复位压缩态
                }
            }
```

4. 新增 `onCompressingChanged` 回调（在 onSessionRunningChanged 之后）：
```java
            @Override public void onCompressingChanged(SessionHandle h, boolean compressing) {
                Platform.runLater(() -> {
                    if (chatView != null && chatView.handle() == h) { // 仅当前激活会话
                        runningIndicator.setCompressing(compressing);
                    }
                });
            }
```

- [ ] **Step 3: theme.css 加样式**

在 `.status-dot` 相关样式附近追加：
```css
/* ===== 运行状态指示器（正文区左下角齿轮+文案） ===== */
.running-indicator { -fx-spacing: 8; }
.running-indicator-gear { -fx-fill: #8a8f98; }
.running-indicator-text { -fx-text-fill: #8a8f98; -fx-font-size: 12px; }
```

- [ ] **Step 4: 编译 + 全量测试**

```bash
JAVA_HOME="D:/javame/jdk1.8" mvn clean package -DskipTests=false
```
Expected: BUILD SUCCESS（含全部测试）。

- [ ] **Step 5: 启动冒烟（手动）**

```bash
java -jar target/minion-0.1.0.jar
```
Expected: 启动后指示器不可见；发送消息会话运行后正文区左下角出现旋转齿轮 + 文案（10s 轮换）；空闲后消失；/compress 或自动压缩时显示"上下文压缩中..."。GUI 无法自动化，此步骤人工验证后确认。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/minion/gui/MainWindow.java src/resource/theme/theme.css
git commit -m "feat: 正文区左下角运行状态指示器挂载与状态同步"
```

---

### 收尾

- [ ] 更新 README.md（功能说明处提一句运行状态指示器）与设计文档（无变更则不动）
- [ ] `JAVA_HOME="D:/javame/jdk1.8" mvn clean package` 全绿后提交收尾 commit
