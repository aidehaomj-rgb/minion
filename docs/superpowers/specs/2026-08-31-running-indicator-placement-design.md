# 运行提示位置调整设计

## 问题

运行状态提示当前悬浮在消息滚动区左下角，流式思考和回复内容滚动到底部时可能与提示重叠。

## 方案

保持 RunningIndicator 的运行、重试、压缩及动画逻辑不变，只调整挂载位置：从消息区 StackPane 移出，挂到 InputView 三列布局的左侧空白列，靠近输入框左边并垂直居中。提示不再覆盖任何聊天消息，也不挤压输入框的 61.8% 固定宽度。

## 验收

- 运行提示显示在输入框左边。
- 思考、回复和工具输出不会与提示重叠。
- “最新对话”按钮仍位于消息区右下角。
- 输入框宽度和运行状态逻辑不变。
