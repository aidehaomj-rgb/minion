# 可插拔工具与只读数据库工具 设计文档

- 日期：2026-09-05
- 状态：待用户审查
- 影响范围：`core/tools/plugin`（新）、`core/tools/db`（新）、`core/tools/browser`、`core/tools/ToolRegistry`、`core/config/Config`、`gui/dialog/SettingsDialog`、`gui/plugin`（新）、`gui/session/SessionManager`、`Main`、`pom.xml`、`src/resource/config.properties`

## 1. 背景与目标

设置窗当前只有「基础设置 / 模型 / MCP / 关于」四页。工具集是硬编码的：`SessionManager.newRegistry()` 在建会话时把 13 个内置工具 + 4 个浏览器工具一次性注册进 `ToolRegistry`，浏览器工具是否加载取决于启动时 `config.properties` 的 `browser.path` 是否为空，运行期无法调整。

本次要做三件事：

1. **可插拔工具框架**：设置窗新增「工具」页（位于 MCP 之后），页内每个工具一行「显示名 + 状态文案 + 启用开关 + 该工具专属的配置入口」。启用/停用**实时生效**于全局所有会话（含正在运行的会话，其下一次请求即生效），生效方式就是「该工具的描述词是否随请求注入」。
2. **浏览器工具纳入框架**：4 个 CDP 工具（`Browser` / `BrowserEval` / `BrowserScreenshot` / `BrowserDebug`）作为第一个插件；浏览器的 5 项配置从 `config.properties` 迁到 `tools.json`，改配置**保存即重建**，不再需要重启。
3. **新增只读数据库工具**：MySQL / PostgreSQL / Oracle 各一个工具，只支持读，**禁用连接池，每次调用新建连接**；每种库有独立的数据源列表（下拉框选「当前数据源」+「数据源管理」弹窗增删改）。

配置统一落在新文件 `jarDir/tools.json`：文件不存在时自动创建，**四个工具全部默认不启用**。

## 2. 需求决策记录（用户已确认）

| # | 决策 |
|---|---|
| 1 | JDBC 驱动三个全部打进 fat jar（MySQL / PostgreSQL / Oracle），开箱即用 |
| 2 | MySQL、Oracle 支持 `query + schema + describe`；PostgreSQL 仅支持 `query`，其 `schema`/`describe` 不执行任何操作、返回固定提示（`describe` 提示用 `SELECT * LIMIT 1` 自行推断；`schema` 说明列出表名在当前环境会报错故禁用） |
| 3 | 查询不弹高危确认窗（`isHighRisk` 恒 false），只读靠三层防护 |
| 4 | 结果限制：`setMaxRows(100)`、字符预算 30000（超出全量落盘 + 返回头部）、`setQueryTimeout(300)` |
| 5 | 结果格式：Markdown 表格 + 元信息行（数据源 / 耗时 / 行数 / 是否截断） |
| 6 | 工具只用「当前选中数据源」，不向模型暴露 `datasource` 参数；当前数据源标识名动态写进工具 description |
| 7 | 数据源管理弹窗需要：测试连接、新建时按类型预填 URL 模板、删除当前选中项时自动回退；密码**明文**存 `tools.json`（与 `model.json` 的 apiKey 一致），不做 Base64 |
| 8 | 浏览器配置迁到 `tools.json` 并**完全实时重建**；`Config` 与 `config.properties` 去掉全部浏览器项；**不做旧配置迁移与兼容**，当全新的做 |
| 9 | 生效机制用**拉模式**：工具常驻 registry，靠 `PluginGate` 在 `schemas()` / `get()` 时按启用状态过滤；不启用的工具其 name/description/parameters 完全不出现在请求中 |
| 10 | 架构用**插件抽象**（`ToolPlugin` 接口 + `ToolPluginManager`），不做声明式字段描述符驱动的通用表单（对数据源管理这类复合交互是过度设计） |
| 11 | 测试不引入 H2；只做纯函数单测，JDBC 执行路径靠手工验证清单 |

## 3. 架构总览

### 3.1 关键前提：为什么必须改 registry 的取用逻辑

`AgentLoop` 每轮请求前都会重新执行 `promptBuilder.build(allSkills)`（系统提示实时拼接）与 `registry.schemas()`（工具表实时拼接）。但 **registry 的内容是会话创建时装配死的**：`newRegistry()` 只在建会话/恢复会话时跑一次，此后除 MCP 连接完成补注册外无人修改。

因此「实时生效」不能靠推模式遍历会话增删工具（要加监听器、要处理「变更时正在跑的那一轮」竞态、新建会话还得再装配一次），而用**拉模式**：工具总是注册进 registry 并打上所属插件标签，registry 持有一个 `PluginGate`（由 `ToolPluginManager` 实现，全局单例、所有会话共享），在 `schemas()` / `get()` 取用时按当前启用状态过滤。改开关 → 无需通知任何会话 → 下一轮取用自动按新状态生效。

### 3.2 组件与数据流

```
tools.json ──load──> ToolStore ──> ToolPluginManager（PluginGate + 配置 + 启用状态）
                                        │                    │
                     ┌──────────────────┼────────────────────┤
                     ▼                  ▼                    ▼
              BrowserPlugin      DbPlugin(mysql)     DbPlugin(postgresql/oracle)
                     │                  │                    │
        createTools(ctx) → Browser/Eval/Screenshot/Debug   DbTool×3
                     │                  │                    │
                     ▼                  ▼                    ▼
        SessionManager.newRegistry：register(pluginId, tool) + registry.setGate(plugins)
                                        │
                                        ▼
        AgentLoop 每轮 registry.schemas()（过滤禁用插件）→ 请求 tools 数组
        AgentLoop 每次 registry.get(name)（禁用返回 null）→ 失败 ToolResult 给模型自调

SettingsDialog「工具」页 ──setEnabled/setCurrent/save/reconfigure──> ToolPluginManager ──> tools.json
```

`BrowserManager` 独立于插件生效链路：它持有 `volatile BrowserSession`，浏览器工具持有 `BrowserManager` 引用（而非 `BrowserSession`），配置改动重建后工具对象无需替换。

### 3.3 提示词残留核查

工具描述的唯一注入通道是 `registry.schemas()`；`SystemPromptBuilder.BUILTIN` 全文不含任何浏览器/数据库工具字样；`SubAgentLoop` 的工具集同样来自 `registry.schemas()`（仅额外剔除 task）。故插件停用后无任何残留注入点。唯一不受本框架控制的是用户自维护的 `project.md` 与技能描述清单（均为项目文档/技能路由，非工具能力声明），本设计不动它们。

## 4. 详细设计

### 4.1 `core/tools/plugin/ToolPlugin`（接口）

```java
public interface ToolPlugin {
    String id();                          // browser / mysql / postgresql / oracle
    String displayName();                 // 浏览器操作 / mysql / postgreSQL / oracle
    String statusText();                  // 页面状态文案（见 4.4、4.5）
    boolean enabled();
    void setEnabled(boolean on);
    List<Tool> createTools(ToolContext ctx);   // 产出工具实例（无条件创建，启停由 gate 决定）
    default void onConfigChanged() { }         // 配置保存后钩子（浏览器插件重建 Chrome）
}
```

- `id()` 同时是 `tools.json` 的顶层键与 registry 里的插件标签，三者一致。
- `createTools` 由 `SessionManager.newRegistry` 每会话调用一次（工具对象会话级，因 `BrowserScreenshotTool` 等携带 `tmpDir`）。
- 插件实现类不依赖任何 JavaFX 类型（GUI 面板在 `gui/plugin`，通过 `ToolPluginManager` 读写同一批配置对象）。

### 4.2 `core/tools/plugin/ToolContext`

```java
public class ToolContext {
    public final Workspace workspace;
    public final String skillsDir;
    public final String tmpDir;              // jarDir/.session/tmp/<sessionId>
    public final ConfirmGate confirmGate;
    public final ToolPluginManager plugins;  // 工具动态读「当前数据源」等运行期状态
}
```

### 4.3 `core/tools/plugin/ToolPluginManager`

```java
public class ToolPluginManager implements ToolRegistry.PluginGate {
    public ToolPluginManager(ToolStore store);      // 内部按 store 内容建 4 个插件
    public List<ToolPlugin> plugins();              // 固定顺序：browser, mysql, postgresql, oracle
    public ToolPlugin plugin(String id);            // 未知 id 返回 null
    @Override public boolean enabled(String pluginId);   // 未知 id → true（不过滤内置/未知）
    public void setEnabled(String id, boolean on);  // 改内存 + save()
    public BrowserManager browserManager();
    public BrowserPlugin browserPlugin();
    public DbPlugin dbPlugin(String id);            // mysql/postgresql/oracle；非 db 插件 id 返回 null
    public void save();                             // 委托 ToolStore
    public void addListener(Runnable onChange);     // 仅供 GUI 面板刷新，不参与生效链路
    public void shutdown();                         // 退出钩子：browserManager.shutdown()
}
```

命名口径：`ToolPluginManager` 上取插件的方法一律 `xxxPlugin()`，`ToolStore` 上取配置对象的方法一律 `xxxConfig()`，避免同名不同返回类型造成混淆。

- `enabled(pluginId)`：`pluginId == null`（内置工具）→ 恒 `true`；插件不存在 → `true`（保守放行，避免拼写错误导致工具全灭）；否则取插件 `enabled()`。
- `setEnabled` 内调用 `plugin.setEnabled` 后 `save()`，再触发 `onChange` 监听（GUI 刷状态文案）。
- 浏览器插件的 `setEnabled(true)` 不需要额外动作（懒启动语义不变）；配置变更走 `onConfigChanged()`。

### 4.4 `core/tools/plugin/ToolStore` 与 `tools.json`

仿 `McpStore`：`jarDir/tools.json`，缺失即生成默认、损坏备份 `.bak` 后重建、原子写（`.tmp` + `Files.move REPLACE_EXISTING`，失败清理 tmp 并打印 `[minion] 写入 tools.json 失败: …`）。

```java
public class ToolStore {
    public static final String FILE_NAME = "tools.json";
    public static ToolStore load(Path jarDir);
    public BrowserConfig browserConfig();
    public DbConfig dbConfig(String id);  // mysql/postgresql/oracle；未知 id 返回 null
    public void save();
}
```

文件结构（gson 直接序列化 POJO，无包裹层）：

```json
{
  "browser": {
    "enabled": false,
    "path": "",
    "port": 9222,
    "userDataDir": "./.minion/browser-profile",
    "headless": false,
    "timeoutMs": 30000
  },
  "mysql":      { "enabled": false, "current": "", "dataSources": [] },
  "postgresql": { "enabled": false, "current": "", "dataSources": [] },
  "oracle":     { "enabled": false, "current": "", "dataSources": [] }
}
```

`dataSources` 元素：`{ "name": "标识名", "url": "jdbc:…", "user": "", "password": "" }`（密码明文）。

- 文件不存在 → 按上述默认值创建，**四项 `enabled` 全 false**、`path` 空、三个 `dataSources` 空数组、`current` 空串。
- 与 `config.properties` **零耦合**：不从 config 读浏览器项，也不把老值迁移过来；老文件里残留的 `browser.*` 行成为无人读取的死行，不清理。
- 反序列化容错：字段缺失取默认值（POJO 字段初始化即默认值）；`dataSources` 为 null 归一化为空列表；`port`/`timeoutMs` 非法（gson 解析异常已在损坏分支处理）。
- `userDataDir` 为相对路径时，按 `Config.jarDir()` 解析（沿用现有 `ChromeLauncher` 调用方口径：`Paths.get(cfg.userDataDir)`，与旧行为一致）。

### 4.5 `core/tools/ToolRegistry` 改造（拉模式核心）

```java
public interface PluginGate { boolean enabled(String pluginId); }

public void register(Tool tool);                       // 等价 register(null, tool)：内置工具永不过滤
public void register(String pluginId, Tool tool);      // 记录 tool → pluginId 归属
public void setGate(PluginGate gate);                  // 由 SessionManager.newRegistry 注入
public Tool get(String name);                          // 禁用插件的工具返回 null
public List<Tool> all();                               // 同样过滤
public List<JsonObject> schemas();                     // 同样过滤（不启用 → 描述词不注入）
```

实现要点：
- 内部保留 `LinkedHashMap<String, Tool> tools`（注册顺序不变）+ 新增 `Map<String, String> ownerOf`（工具名小写 → pluginId，可为 null）。
- 私有 `boolean visible(Tool t)`：`gate == null || ownerOf.get(key) == null || gate.enabled(owner)`。
- `gate == null` 时行为与改造前完全一致 → `ToolRegistryTest` 现有断言与所有既有调用点零改动。
- `all()` 一并过滤是安全的：全仓检索确认 `all()` 当前只被 `ToolRegistryTest` 调用，生产代码无依赖（工具执行走 `get()`，工具表走 `schemas()`）。
- 注册覆盖语义不变（同名后注册覆盖前者），覆盖时同步更新 `ownerOf`。

`AgentLoop` 的工具不存在分支文案由「未知工具」补充为「工具不存在或已停用」（`registry.get()` 返回 null 时），让模型知道该能力当前不可用而非名字写错。

### 4.6 浏览器插件

**`core/tools/plugin/BrowserConfig`**：`enabled/path/port/userDataDir/headless/timeoutMs`（POJO，字段默认值即 4.4 的默认值）。

**`core/tools/plugin/BrowserPlugin implements ToolPlugin`**
- `id()="browser"`、`displayName()="浏览器操作"`。
- `statusText()`：`path` 空 → `未配置浏览器路径`；否则 `<path 文件名> · 端口 <port> · <有头|无头>`（文件名取 `new File(path).getName()`，如 `chrome.exe · 端口 9222 · 有头`）。
- `createTools(ctx)`：4 个工具，构造参数首位由 `BrowserSession` 换成 `BrowserManager`；`BrowserScreenshotTool` 继续接 `ctx.workspace / ctx.skillsDir / ctx.tmpDir / ctx.confirmGate`。
- `onConfigChanged()` → `browserManager.reconfigure(config)`。

**`core/tools/plugin/BrowserManager`**
```java
public BrowserManager(BrowserConfig cfg);
public synchronized BrowserSession session();       // path 空 → null；否则懒建（不拉进程）
public synchronized void reconfigure(BrowserConfig cfg);  // stop 旧 launcher → 换 cfg → session 置 null
public synchronized void shutdown();                // 退出钩子
```
- `session()`：`path` 为空返回 null；否则双检懒建 `new BrowserSession(new ChromeLauncher(path, port, Paths.get(userDataDir), headless, timeoutMs), new CdpClient(10000, timeoutMs))`。构造不启动 Chrome（真正拉起在首次 CDP 调用 `launcher.pageEndpoint()`），故重建代价仅是对象替换 + 旧进程 `stop()`。
- `reconfigure`：若已有 launcher 则 `stop()`（`process == null` 时是空操作，复用外部 Chrome 实例的场景安全），置 `session=null`、`launcher=null`，保存新配置；下次 `session()` 按新配置懒建。
- **代价说明**：重建会关闭 minion 自启的 Chrome（当前已打开的页面丢失）；复用外部实例（非 minion 启动）时 `stop()` 无操作，但 `session` 仍会按新端口重建。

**4 个工具类的改造**：字段 `BrowserSession session` → `BrowserManager browser`；每个 `execute` 首行
```java
BrowserSession s = browser.session();
if (s == null) return ToolResult.error("浏览器未就绪：请在 设置 → 工具 → 浏览器操作 → 配置 中填写浏览器路径");
```
其余逻辑不变（原方法体内的 `session.xxx()` 改为 `s.xxx()`）。

**启用与 path 空的关系**：gate 只判 `enabled`。`enabled=true` 且 `path` 空时工具仍注入，调用返回上面那句明确指引，同时页面 `statusText()` 显示「未配置浏览器路径」——启用语义单一可预期。

**`ChromeLauncher` 文案**：两处提到 `config.properties 配置 browser.path` 的异常消息改为「未找到 Chrome，请在 设置 → 工具 → 浏览器操作 → 配置 中填写浏览器路径」/「Chrome 启动超时(端口 N)，请检查 设置 → 工具 → 浏览器操作 → 配置 中的浏览器路径」。

### 4.7 数据库插件

#### 4.7.1 `core/tools/db/DbType`（枚举）

| 常量 | `id` | `displayName` | 工具名 | 驱动类 | URL 模板（新建预填） | 动作 |
|---|---|---|---|---|---|---|
| `MYSQL` | `mysql` | `mysql` | `DbMysql` | `com.mysql.cj.jdbc.Driver` | `jdbc:mysql://localhost:3306/dbname?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai` | query, schema, describe |
| `POSTGRES` | `postgresql` | `postgreSQL` | `DbPostgres` | `org.postgresql.Driver` | `jdbc:postgresql://localhost:5432/dbname` | query |
| `ORACLE` | `oracle` | `oracle` | `DbOracle` | `oracle.jdbc.OracleDriver` | `jdbc:oracle:thin:@localhost:1521:ORCL` | query, schema, describe |

枚举另提供 `boolean supports(String action)`、`String driverClass()`、`String urlTemplate()`、`static DbType ofId(String id)`。

#### 4.7.2 `core/tools/db/DataSourceConfig` 与 `DbConfig`

```java
public class DataSourceConfig { public String name, url, user, password; }
public class DbConfig {
    public boolean enabled;
    public String current = "";
    public List<DataSourceConfig> dataSources = new ArrayList<>();
    public DataSourceConfig currentDataSource();   // current 空或找不到 → null
}
```
数据源增删改由 `DbPlugin` 提供方法（内部维护 `DbConfig` 并触发 `manager.save()`）：
`addDataSource / updateDataSource(旧名, 新对象) / removeDataSource(name) / setCurrent(name) / testConnection(name)`。
- `updateDataSource` 改名且旧名 == `current` → 同步 `current`。
- `removeDataSource` 删的是 `current` → 回退到列表第一个；列表空则 `current=""`。
- 标识名唯一性、URL 前缀校验由 `DataSourceValidator`（纯函数）负责，GUI 只弹提示。

#### 4.7.3 `core/tools/db/DataSourceValidator`（纯函数，单测覆盖）

```java
public static String validate(String name, String url, List<DataSourceConfig> all, String originalName);
// 返回 null = 通过；否则返回中文错误原因
```
规则：
- `name` trim 后为空 → 「标识名不能为空」
- `name` 与 `all` 中**其它**项重名（忽略大小写；等于 `originalName` 自身不算）→ 「标识名已存在：<name>」
- `url` trim 后为空 → 「URL 不能为空」
- `url` 不以 `jdbc:` 开头（忽略大小写）→ 「URL 必须以 jdbc: 开头」
- `name` 含 `"` `\` `/` 等文件路径非法字符不拦（标识名不落文件名），但长度 > 40 → 「标识名过长（≤40 字符）」

#### 4.7.4 `core/tools/db/SqlGuard`（纯函数，单测覆盖）

```java
public static String check(String sql);   // null = 放行；否则返回拒绝原因
```
先做一次**词法归一化扫描**（同一段文本按方言读法各扫一遍，产出**八份视图**），再跑七项检查。
round 1 评审指出原「正则剥前导注释 + 整段找分号/子串」有绕过与误判；round 2 评审指出
「两份文本按检查项分工」本身就是洞（分号看保守文本、短语只看剥注释文本 → 短语判定对
`#`/不接空白 `--` 之后的内容完全失明）；round 3 评审指出**首词判定只认一份视图**同样是洞
（`stripped` 会把可执行注释的内容内联 → 首词可以「从注释里借」）；round 4 评审指出**轴数不对**：
各家库的不一致处其实是**三条互相独立的轴**，round 1-3 只覆盖了两条，第三条轴（块注释配对）
整类漏掉。现规定如下。

**一条不变式**：MySQL / MariaDB / PostgreSQL / Oracle 对同一句 SQL 的文法不一致，不一致的地方正好
落在三条轴上，每条轴两种读法：

| 轴 | 一种读法 | 另一种读法 |
|---|---|---|
| 1 行内注释 | `#` 是注释、`-- ` 是注释（MySQL/MariaDB） | `#` 是运算符（PG 的 `#>`、异或）、`--1` 是两次取负；只有 `--` 后接 ASCII 空白/行尾时三库都认 |
| 2 可执行注释 | `/*!…*/`（含 `/*M!…*/`、任意 ASCII 字母前缀）的内容**被真执行**（MySQL/MariaDB） | 它就是普通块注释（PG/Oracle） |
| 3 块注释配对（round 4 新增） | 块注释**可嵌套**，收尾是与起手配对的那个星斜杠（PG/MariaDB） | 按**首个**星斜杠收尾（Oracle 文档口径、MySQL 同样不认嵌套） |

三条轴两两组合 = **八份视图**（视图下标就是三个读法的位标记 `AXIS_FLAT|AXIS_INLINE|AXIS_KEEP`），
**每一项检查都要在它该看的每份视图上各跑一遍，任一命中即拒**——单项检查不得只认一份文本、
也不得只认一种配对。理由：「命中」既不向上也不向下单调——分号/写动词要求文本**更多**，
而危险短语要求关键字之间**只剩空白**（注释剥得越干净才越拼得拢），所以每条轴的两种取值都必须各扫一遍。

八份视图按三个位标记命名（`stripped`/`conservative`/`plain` 是 round 1-3 就有的那三份子集）：

- `stripped`（轴 1 剥、轴 2 内联、轴 3 嵌套）：一切注释替换成**一个空格**，可执行注释内容内联。
  用途：取首词之一；危险短语匹配（`FOR--c\nUPDATE` → `FOR UPDATE`）。
- `conservative`（轴 1 留、轴 2 内联、轴 3 嵌套）：只有「三库都当注释」的片段才剥掉，`#` 与不接空白的
  `--` **按代码原样保留**（区内字面量照旧掩码）。用途：判分号、判危险短语、判 EXPLAIN 目标。
- `plain`（轴 1 剥、轴 2 剥、轴 3 嵌套，round 3 新增）：连可执行注释也整段剥掉、内容绝不内联，
  即「任何一库都至少会执行」的**最小文本**。
- **`flat` 系四份**（round 4 新增，轴 3 = 按首个星斜杠收尾）：`flat`（= plain 的不认嵌套孪生）、
  `flatInline`（= stripped 的不认嵌套孪生，即 MySQL 的真实读法）、以及两份行内注释按代码保留的
  `codeFlat`/`codeFlatInline`（Oracle 既把 `#` 当代码又不认嵌套）。
  它们专门用来看见「不认嵌套的库在第一个星斜杠就结束了注释、其后被真执行」的那段文本：
  `SELECT 1 /*a/*!*/ INTO OUTFILE '/tmp/x' */`、`SELECT * FROM t /*!FOR*/ /*a /*b*/ UPDATE */`
  （前者靠 `flat`，后者两个半边分属「内联」与「不认嵌套」两条轴，只能靠 `flatInline` 命中）。
- **首词不许从注释内容里借**（round 3 起、round 4 扩到四份）：`/*!SELECT*/DROP TABLE t` 的
  `stripped` 首词是注释里借来的 `SELECT`，而这句话在 PG/Oracle 里整段只是注释、真执行的首词是 `DROP`。
  故首词必须在**四份「行内注释按注释处理」的视图**（`stripped`/`plain`/`flatInline`/`flat`）上双双过白名单；
  某份视图整份为空（该读法下只剩注释、没有任何语句）时跳过这一项，不给 `/*!40000 SELECT * FROM t*/`
  这类合法写法添堵。命中时文案按塌掉的那条轴点破读法：「把可执行注释当普通注释看时，」/
  「把块注释按「不认嵌套」的方言（Oracle/MySQL）看时，」，免得模型以为首词是它自己写的那个而当成误拒。
- 首词仍**不看**行内注释按代码保留的那四份：以 `#` 或不接空白的 `--` 起手的语句，在非 MySQL 库里
  必然是语法错误（运算符/取负没有左操作数），藏不住可执行的写语句；而那份读法里行首注释是普通词，
  用它取首词会把 `# 注释\nSELECT 1` 这类合法写法打死。
- 不认嵌套的读法多暴露出来的**注释尾巴**（`中文 */` 一类起手不是词的残料）在任何库都执行不过去，
  所以只有 `flat` 系两份视图在「取不到首词」时跳过不判；认嵌套的视图仍照旧报「无法识别语句首关键词」。
- 「`--` 后接空白才算三库通用注释」只认 **ASCII 空白**（空格/制表/换行/回车/换页）：
  `Character.isWhitespace` 连 U+2000、全角空格都算空白，而 MySQL 不认，宽松判定等于替 MySQL 藏住整行。
- **行尾 = 换行或回车**（round 4）：MySQL/PG/Oracle 的 `--`、`#` 注释都在 CR 处结束。只认换行等于
  让 `SELECT 1-- x␍; DROP TABLE t` 里第二条语句整行隐身（␍ 是回车）。行内存疑区同样以 CR 为界。
- 注释替换成「一个空格」而不是删空，保证被注释切开的关键字仍能压平成短语。块注释收尾按轴 3 取两种配对。
- MySQL/MariaDB **可执行注释**：`/*!…`、`/*M!…` 以及**任意长度 ASCII 字母/数字前缀 + `!`**
  （round 1 把前缀限死三个字母，导致 `/*MARIADB!DROP TABLE t*/` 整类绕过——round 2 建议 1 去掉上限）。
  内容去掉前缀与版本号后递归归一化，按父视图**同一读法**内联进带 `AXIS_INLINE` 的四份
  （其余四份整段剥掉），同时记录该片段供复检（第 5 项）：**片段的首词在它的四份「首词视图」上都要判，
  片段自己的八份视图也要各跑一遍内容级检查**——「外层被真执行、内层只是普通注释」这种混合读法
  只有在这里才看得到（`SELECT 1 /*!FOR/*M!1c*/UPDATE*/` 在 MySQL 侧就是 `FOR UPDATE`）。
  登记每个片段只由「同一条配对轴 + 同一行内注释读法」的那一遍负责，同一片段不会被扫四遍。
- 字符串字面量与引号标识符（`'…'`、`"…"`、反引号）一律掩码成占位空串 `''`；只按「引号加倍」解转义、
  **不认反斜杠**（PG 标准模式反斜杠非转义，认了会把真实分号藏进假字符串；MySQL 侧最坏是误拒）。
- 字面量/块注释在**根级**不闭合、可执行注释嵌套超过 4 层 → 直接拒（「无法安全解析」）。
  但**行内存疑区**（`#` 与不接空白的 `--` 之后到行尾）以行尾为界：本行内配不上的引号/星斜杠
  不算未闭合（那一行在任何库都执行不过去），按普通字符留着 —— 既不会把
  `SELECT 1--don't` 换行 `; DROP TABLE t` 的真实分号当字符串吞掉，也不会误报
  `SELECT 1--2 /* x`、`SELECT 1 # don't` 这类合法语句「无法安全解析」。
- **片段级的词法失败只作废那一份读法**（round 4 随轴 3 一起补）：按首个星斜杠收尾切出来的可执行注释
  正文，往往自带配不上的斜杠星（`/*!/*M!1c*/…`），在认嵌套的那几份里必然「注释未闭合」。
  这只说明**那种读法**下这段文本是一句执行不过去的残料，不能牵连同句其余读法——该份视图留空、
  其余照旧复检（否则第 5 项的正确文案会被「无法安全解析」顶掉）。根级不闭合仍一律拒。

检查：
1. `sql` 为 null / trim 后为空 → 「SQL 不能为空」；`stripped` 视图归一化后为空同样返回该文案。
2. **首关键词（八份里看四份）**：取视图开头的 `[A-Za-z0-9_]+` 段并转大写，必须 ∈
   `SELECT, WITH, SHOW, DESC, DESCRIBE, EXPLAIN`，否则 → 「只读工具拒绝执行：语句以 <词> 开头（仅允许 …）」；
   取不到词 → 「无法识别语句首关键词」。这一项在 `stripped`/`plain`/`flatInline`/`flat` 上各判一次，
   整份为空的跳过；命中时文案按塌掉的那条轴加读法前缀（见上）。
   防 `/*c*/DROP TABLE t`、`SEL/**/ECT` 一类绕过，防 `/*!SELECT*/DROP TABLE t` 这种「首词从注释里借」，
   也防 `/*a /*!b*/ DROP TABLE t */ SELECT 1` 这种「首词从不认嵌套暴露的注释尾巴里冒出来」。
3. **多语句**（八份视图各判一次）：去掉末尾至多一个 `;` 后仍含 `;` → 「不允许多条语句」。
   故 `SELECT 'a;b'`、`SELECT 1 /* 说明; x */`、`SELECT 1; -- 说明` 放行，
   `SELECT x #> '{a}'; DROP TABLE t`、`SELECT 1--2; DROP TABLE t`、`SELECT 1--don't⏎; DROP TABLE t`、
   `SELECT 1-- x␍; DROP TABLE t` 拒绝。
4. **危险短语**（八份视图各判一次，空白压平后整词边界匹配、忽略大小写）：`INTO OUTFILE`、`INTO DUMPFILE`、
   `FOR UPDATE`、`FOR NO KEY UPDATE`、`FOR KEY SHARE`、`FOR SHARE`、`LOCK IN SHARE MODE`
   → 「只读工具拒绝执行：语句含 <短语>」。
   round 4 把锁语句按各家锁强度配齐：PG 有 `FOR UPDATE` / `FOR NO KEY UPDATE` / `FOR SHARE` /
   `FOR KEY SHARE` 四档，MySQL 8 除 `LOCK IN SHARE MODE` 外也认 `FOR SHARE` —— 只拦 `FOR UPDATE`
   等于把另外三档留给模型自由使用。方向互补才算闭环：`SELECT 1--1 INTO OUTFILE '/tmp/x'`（MySQL 里
   `--1` 是取负）靠保守视图命中，`SELECT * FROM t FOR--c⏎UPDATE`（PG 里 `--c` 是注释）靠 `stripped` 命中，
   `SELECT * FROM t FOR/*!x*/UPDATE`（PG/Oracle 里只是普通注释）靠 `plain` 命中，
   `SELECT * FROM t FOR/*a/*b*/NO KEY UPDATE */` 靠 `flat` 命中。
5. **可执行注释片段**：片段首词（在其四份「首词视图」上）不得是写动词
   （INSERT/UPDATE/DELETE/DROP/…/RESET 共 33 个），否则 → 「只读工具拒绝执行：可执行注释内含 <词> 语句」；
   片段自己的八份视图同样要过第 3、4、6、7 项，嵌套片段递归检查。
6. **EXPLAIN 的目标**（round 2 建议 2）：某份视图首词为 `EXPLAIN` 时，跳过解释选项词
   （ANALYZE/COSTS/BUFFERS/FORMAT/PLAN/FOR/…）后的第一个实词不得是写动词，
   否则 → 「只读工具拒绝执行：EXPLAIN 的目标含写动词 <词> …」。
   堵住 `EXPLAIN ANALYZE DELETE FROM t`、`EXPLAIN (ANALYZE, BUFFERS) UPDATE …`、`EXPLAIN DELETE FROM t`
   —— PG 的 `EXPLAIN ANALYZE` 会真执行 DML。`EXPLAIN ANALYZE SELECT …`、`EXPLAIN FORMAT=JSON SELECT …` 不受影响。
   round 3 起逐视图自查首词，round 4 起逐视图包含不认嵌套的读法，故
   `EXPLAIN /*!x*/ANALYZE DELETE FROM t`（`plain` 里才是真形）与
   `EXPLAIN /*a/*!*/ DROP TABLE t */`（`flat` 里才是真形）都拦得住。
7. **括号处的语句位**（round 4 新增）：任一视图里紧跟在 `(` 之后、或紧跟在 `)` 之后的第一个词，
   不得是 DML 动词（`INSERT/UPDATE/DELETE/MERGE/UPSERT/REPLACE` 六个），
   否则 → 「只读工具拒绝执行：括号后的语句位出现写动词 <词>（PG 的改写型 CTE、WITH 子句后接 DML 会真写数据）」。
   这两处正好是 `WITH` 这个白名单首词的两条写通道，两者都**先写数据再返回结果集**，
   第二层 `executeQuery` 拿得到 `ResultSet` 也就挡不住：
   `WITH x AS (DELETE FROM t RETURNING *) SELECT * FROM x`（改写型 CTE，动词在 `(` 后）、
   `WITH a AS (SELECT 1) DELETE FROM t WHERE id=1`（CTE 列表以 `)` 收尾，主语句动词紧跟其后）、
   `EXPLAIN WITH a AS (SELECT 1) DELETE FROM t`（第 6 项只看到 `WITH` 就停了，这条由第 7 项补）。
   动词表**只收这六个**：把 `SET/TABLE/BEGIN/START/COPY` 那批非保留词放进来会误伤
   PG 里完全合法的 `SELECT begin, end, start FROM t`。

`DESC`/`DESCRIBE` 在 Oracle 上是 SQL*Plus 命令、JDBC 执行会报错，但保留在白名单内（无害，错误由驱动消息透传，MySQL 上可用）。

**回归工件**：`SqlGuardTest`（43 项，按评审轮次分组，每条洞 + 对照组一起钉）之外，
round 4 另沉淀 `SqlGuardDialectTest`——**不再靠人肉想绕过用例**：测试里独立实现一套「参考读取器」，
按上表三条轴枚举出 7 种真实方言读法（mysql/mysql-flat/mariadb/mariadb-flat/postgresql/oracle-nest/oracle-flat），
算出每种读法**真正会执行的文本**，再用一份与 `SqlGuard` 互不共享代码的「危险定义」判它危不危险；
只要任一读法的文本危险而守卫放行，就算漏判。用例靠**结构原子枚举**撑开
（`/*`、`*/`、`/*!`、`#`、`--`、换行、回车、分号、括号、引号按长度组合再夹载荷），约 5 万条、1.2 秒。
同一份工件在 round 4 修复前的实现上报出 **2445 条漏判**（锁短语 1185 类、WITH 括号语句位每动词 138 条、
`mysql-flat` 的 INTO OUTFILE、`--␍` 行尾、EXPLAIN 目标…），修复后 0 条 —— 工件非空转，
下一轮再新增一条方言轴时它会自己现形。

已知取舍与残余缺口（前者是误拒方向，后者靠第二层 `setReadOnly(true)` + `executeQuery` 与数据源侧只读账号兜底）：
- 误拒：方言存疑注释里出现危险短语/写动词会被拒（`SELECT 1 # 说明 for update 会锁行`），与「这类注释里出现分号也拒」同一口径；
  `/*fix!DROP 影子表*/`、`/*TODO! …*/` 这类以 `!` 收尾前缀的普通注释会被当可执行注释内联复检；
  括号起手的只读查询 `(SELECT 1)` 报「无法识别语句首关键词」；
  以可执行注释起手、剥掉它之后首词就不在白名单的写法（`/*!SELECT*/1` 这种只在 MySQL 才成立的语句）一并拒掉——
  判不出目标方言，就不能让首词从注释内容里借。
- 误拒（round 4 随轴 3 新增）：**可嵌套块注释的尾巴**会被不认嵌套那份读法当代码看见，尾巴里正好有
  分号/危险短语/括号语句位写动词时整句被拒（`SELECT 1 /* a /* b */ ; c */` 会被判「不允许多条语句」）；
  子查询的表/列别名若恰好取名成 `insert/update/delete/merge/replace`（PG 里 `replace` 一类非保留字可以），
  会被第 7 项误拒。方向与其余各轴一致：判不出方言就宁误拒，这类写法在 MySQL/Oracle 侧本来就是两条语句或语法错误。
- 漏判兜底：Oracle `EXPLAIN PLAN FOR SELECT …`（写 plan_table）、
  PG 的 `SELECT … INTO t`（建表写入；与 MySQL 无副作用的 `SELECT … INTO @var` 无法在不引入误拒的前提下区分，
  故不进第 4 项的危险短语清单）、`LOAD_FILE()` 等外读面（要拦须先扩第 4 项的七条危险短语）。
- 不识别：PG 美元引用（`$$…$$`）与 Oracle `q'[…]'`，其内部分号按字面量外处理（误拒方向）。
- MySQL 优化器提示（`/*+ SET_VAR(…) */`）可以改会话变量（`general_log` 一类），不拦：
  按提示名拦会打死所有正常提示，且这类改动本就需要权限，交给只读账号兜底。
- 三条**已识别**的方言轴（行内注释、可执行注释、块注释配对）已两两组合出八份视图，这一层不再有
  「少一份读法」的空格；上面几条残余缺口都在这三条轴之外（引号语义、美元引用、提示与会话变量、
  `SELECT … INTO` 的方言歧义），多加视图解决不了，只能由第二层与只读账号兜底。

#### 4.7.5 `core/tools/db/DbExecutor`（每次新建连接，无池）

```java
public class DbExecutor {
    public DbExecutor(Path tmpDir);                       // 落盘目录（会话级）
    public String query(DataSourceConfig ds, DbType type, String sql);
    public String listTables(DataSourceConfig ds, DbType type);      // schema
    public String describe(DataSourceConfig ds, DbType type, String table);
    static Connection open(DataSourceConfig ds, DbType type) throws Exception;  // 包级可见，便于替换/测试
}
```
连接与执行口径（三库统一）：
1. `Class.forName(type.driverClass())` —— 显式加载，不依赖 shade 后的服务发现。
2. `DriverManager.setLoginTimeout(10)`；`Connection c = DriverManager.getConnection(url, user, password)`。
3. `c.setAutoCommit(true)`；`c.setReadOnly(true)`。
4. `Statement st = c.createStatement()`；`st.setMaxRows(101)`（多取 1 行用于判定截断）；`st.setQueryTimeout(300)`。
5. `ResultSet rs = st.executeQuery(sql)` —— 第三层防护。
6. try-with-resources 关 `rs/st/c`：**每次调用新建、用完即关，绝不缓存连接（禁用连接池）**。

结果渲染：
- 前 100 行入表；若取到第 101 行 → `truncated = true`。
- `MarkdownTable.render(columns, rows)`（纯函数）生成表格；元信息行由 `MarkdownTable.header(dsName, elapsedMs, rowCount, truncated, maxRows)` 生成：
  - 未截断：`数据源: prod-mysql · 耗时: 1.24s · 行数: 37`
  - 截断：`数据源: prod-mysql · 耗时: 1.24s · 行数: 100（已达上限 100 行，结果被截断，可加 LIMIT/WHERE 细化）`
- 0 行 → `查询成功，0 行结果`（仍带数据源与耗时）。

`schema` / `describe` 走 **JDBC `DatabaseMetaData`，不拼 SQL**（零注入风险、跨库一致）：
- `listTables`：`metaData.getTables(catalog, schemaPattern, "%", new String[]{"TABLE","VIEW"})`
  - MySQL：`catalog = c.getCatalog()`、`schemaPattern = null`
  - Oracle：`catalog = null`、`schemaPattern = metaData.getUserName()`（不限定会拉出 SYS/SYSTEM 海量对象）
  - 输出列：`表名 / 类型 / 注释`（`REMARKS`）；上限 500 行，超出截断并在末行提示「…（已达 500 行上限，可用 query 精确检索）」
- `describe`：`metaData.getColumns(catalog, schemaPattern, tablePattern, "%")`，`tablePattern` 直接取参数 `table`（含 `%`/`_` 时按模式匹配多表，结果带表名列）
  - 输出列：`表名 / 列名 / 类型 / 长度 / 可空 / 默认值 / 注释`；`长度` 取 `COLUMN_SIZE`（`DECIMAL_DIGITS` > 0 时附 `,精度`）；`可空` 由 `NULLABLE` 映射为 `是/否/未知`
  - 上限 100 行；`table` 空 → 「describe 需要 table 参数」；无匹配 → 「未找到表：<table>」

PostgreSQL 的 `schema` / `describe`：`DbTool` 在调用前直接返回固定提示，**不进 `DbExecutor`、不建连接**：
- `schema` → `postgreSQL 数据源不支持列出表名（当前环境查询系统表会报错），请直接使用 query，例如 SELECT * FROM 表名 LIMIT 1 推断结构`
- `describe` → `postgreSQL 数据源不支持表结构查询，请用 query 执行 SELECT * FROM 表名 LIMIT 1 自行推断字段`

字符预算与落盘（沿用项目现有 30000 口径，同 `GrepTool.DISPLAY_CHARS` / `BashTool` 总预算）：
- 渲染结果 ≤ 30000 字符 → 原样返回。
- 超出 → `OutputDump.write(tmpDir, "db", full)` 全量落盘，返回 `full.substring(0, 30000)` + `\n\n…（完整结果 <N> 字符已落盘：<绝对路径>，可用 Read 查看）`；表格式数据头部比尾部有用，故返回头部（不用 `OutputDump.tail`）。
- 落盘失败（返回 null）→ 降级为纯内存截断，追加 `…（结果过长已截断，共 <N> 字符）`。

#### 4.7.6 `core/tools/db/MarkdownTable`（纯函数，单测覆盖）

```java
public static String render(List<String> columns, List<List<String>> rows);
public static String header(String dsName, long elapsedMs, int rowCount, boolean truncated, int maxRows);
public static String cell(Object v);   // null → "NULL"；| → \|；\r\n → <br>；> 120 字符截断加 …
```

输出样例：
```
数据源: prod-mysql · 耗时: 1.24s · 行数: 100（已达上限 100 行，结果被截断，可加 LIMIT/WHERE 细化）

| id | name | created_at |
| --- | --- | --- |
| 1 | 张三 | 2026-01-01 10:00:00 |
| 2 | NULL | 2026-01-02 11:00:00 |
```

#### 4.7.7 `core/tools/db/DbTool implements Tool`

一个类按 `DbType` + `DbPlugin` 参数化，三处实例化（每会话各 3 个对象）。

- `name()` → `type.toolName()`（`DbMysql` / `DbPostgres` / `DbOracle`）。
- `description()` **动态拼接**（每轮实时取值，切数据源无需重建工具）：
  - 有当前数据源：`mysql 只读查询（当前数据源: prod-mysql，jdbc:mysql://10.0.0.5:3306/shop）。action=query 执行只读 SQL 返回 Markdown 表格（最多 100 行）；action=schema 列出表与视图；action=describe 查看表字段。仅允许 SELECT/WITH/SHOW/DESC/DESCRIBE/EXPLAIN，禁止多语句与写操作。`
  - 无当前数据源：`mysql 只读查询（当前未选择数据源，调用会返回提示，请在 设置 → 工具 中选择数据源）。…`
  - PostgreSQL 版本额外写明：`仅支持 action=query；schema/describe 在本类型下不可用，会返回提示，表结构请用 SELECT * FROM 表名 LIMIT 1 自行推断。`
- `schema()`：`SchemaGenerator.objectSchema(描述, new String[]{"action","sql","table"}, new String[]{"action"})`。
- `execute(args)`：
  1. `action` 缺失 → 「缺少 action 参数」；未知 action → 「未知 action: x（支持 …）」
  2. `DbConfig.currentDataSource() == null` → 「<类型> 插件未选择数据源，请在 设置 → 工具 → <显示名> 中选择」
  3. `query`：`sql` 缺失 → 「query 需要 sql 参数」；`SqlGuard.check(sql)` 非 null → 原样返回拒绝原因；否则 `DbExecutor.query`
  4. `schema` / `describe`：PostgreSQL → 固定提示；否则 `DbExecutor.listTables` / `describe`
- `isHighRisk(args)` → 恒 `false`（查询不弹确认窗）。

#### 4.7.8 `core/tools/plugin/DbPlugin implements ToolPlugin`

- `id()/displayName()` 取自 `DbType`。
- `statusText()` 恒空：当前选中/（无数据源）由行内下拉框与占位表达，状态列不重复占列（浏览器行仍有文案，见 4.6/4.8）；
- `createTools(ctx)` → `Collections.singletonList(new DbTool(type, this, new DbExecutor(Paths.get(ctx.tmpDir))))`。
- 数据源变更方法（4.7.2）统一在末尾 `manager.save()` + 触发 GUI 监听。
- `testConnection(name)`：后台线程执行（GUI 侧起线程，方法本身同步）：`DriverManager.setLoginTimeout(10)` → `open(ds, type)` → 立即关闭 → 返回耗时 ms；异常返回 message 首行（`SettingsDialog.shorten` 同口径，40 字符截断）。

### 4.8 GUI：设置窗「工具」页

**导航**：`SettingsDialog.show` 的 `nav.getItems()` 改为 `基础设置 / 模型 / MCP / 工具 / 关于`，内容切换分支加 `"工具".equals(item) ? toolsNode : …`；`show(...)` 签名增加 `ToolPluginManager plugins` 参数（`MainWindow` 调用处同步传入）。窗口尺寸不变（870×500）。

**新增 `gui/plugin/ToolsPane`**（`SettingsDialog` 已 770 行，工具页独立成类，`SettingsDialog` 只调 `ToolsPane.build(plugins, owner)`）：
- `VBox`，4 行固定（行数固定且每行控件不同，不用 ListView）；每行 `HBox`：`CheckBox「启用」(首列) + 显示名(100px) + 状态文案(msg-thinking 样式, HGrow) + 配置区`。
- 行布局（状态列口径：浏览器行显示配置文案；数据库行**恒空**——当前选中/（无数据源）
  均由行内下拉框表达，状态列不重复占位）：
  - `[ ] 启用   浏览器操作   未配置浏览器路径          [配置]`
  - `[ ] 启用   mysql        （状态列空）             [数据源 ▾]  [数据源管理]`
  - `[ ] 启用   postgreSQL   （状态列空）             [数据源 ▾]  [数据源管理]`
  - `[ ] 启用   oracle       （状态列空）             [数据源 ▾]  [数据源管理]`
- 启用 CheckBox：`plugins.setEnabled(id, v)`（内部 save + 通知）→ 刷新本行状态文案。实时生效由 gate 保证，无需其他动作。
- 数据源 `ComboBox<String>`：项为标识名列表，选中值为 `current`；无数据源时 `setDisable(true)` 且显示占位「（无数据源）」；选中即 `dbPlugin.setCurrent(name)` → save → 全局下一轮生效。
- 「数据源管理」/「配置」按钮统一 `btn-ghost` 样式（与 MCP 页、模型页一致）。
- 面板持有 `plugins.addListener(() -> Platform.runLater(this::refresh))`，使后台变更（如测试连接线程）也能刷新；监听器仅做 GUI 刷新。

**新增 `gui/plugin/DataSourceDialog`**（数据源管理弹窗，按 `DbType` 参数化）：
- 结构：左 `ListView<String>`（标识名列表，选中显示 url 于下方 `msg-thinking` 标签，密码不显示）+ 右 `VBox` 按钮：`新建 / 修改 / 删除 / 测试连接`（`btn-ghost`）；底部 `关闭`。尺寸约 620×380，`Theme.style(d)` 深色。
- 新建/修改表单（`Dialog<DataSourceConfig>`，`GridPane` + 150px 标签列，宽 600，与 MCP 表单同风格）：
  - `标识名:`（修改时**可编辑**——用户明确要求标识名可修改）
  - `URL:`（新建预填 `type.urlTemplate()`）
  - `用户名:` / `密码:`（`PasswordField`？→ 用 `TextField` 明文显示，与 `model.json` apiKey 处理一致，便于核对；不遮罩）
  - 校验：点 OK 时调 `DataSourceValidator.validate(...)`，非 null → **不关表单**，表单顶部红字标签显示原因（`-fx-text-fill: #ff6b6b`）。JavaFX 8 `Dialog` 拦截关闭用 `resultConverter` 返回 null 会直接关窗，故改用 OK 按钮 `addEventFilter(ActionEvent.ACTION, …)` 校验失败 `e.consume()`（与 `SettingsDialog` 里「应用」按钮同技巧）。
- 删除：`Alert CONFIRMATION`「删除数据源「x」？」（`Theme.style`），确认后 `dbPlugin.removeDataSource(x)`（内部处理 `current` 回退）→ 刷新列表与外层下拉框。
- 测试连接：选中项 → 后台 daemon 线程执行 `dbPlugin.testConnection(name)`，按钮期间 `setDisable(true)` 且文案改「测试中…」；回到 FX 线程弹 `Alert`：成功「连接成功，耗时 320ms」/ 失败「连接失败：<原因>」。

**新增 `gui/plugin/BrowserConfigDialog`**（浏览器配置弹窗）：
- 字段：`浏览器路径:`（`TextField` + 「浏览…」`FileChooser`，扩展过滤 `*.exe` / `*.*`，当前值为存在文件时定位其父目录——迁移自 `BasicPane` 原实现）、`调试端口:`、`用户数据目录:`、`无头模式:`（CheckBox）、`超时(ms):`。
- 顶部说明标签：`保存立即生效；已打开的浏览器页面会被关闭`（`msg-thinking` 样式）。
- OK 时校验 `port`/`timeoutMs` 为整数（复用 `SettingsDialog.setInt` 口径的纯逻辑，非法 → 红字提示不关窗）；通过则写回 `BrowserConfig` → `plugins.save()` → `browserPlugin.onConfigChanged()`（重建）→ 刷新行状态文案。

### 4.9 装配与生效链路

**`Main`**：
```java
ToolPluginManager plugins = new ToolPluginManager(ToolStore.load(jarDir));
// 删除 BrowserSession / ChromeLauncher 装配与 browserPath 判空分支
SessionManager manager = new SessionManager(confirmUi, config, jarDir,
        workspaces, models, skills, plugins, mcpManager);
Runtime.getRuntime().addShutdownHook(new Thread(() -> {
    manager.shutdown();
    mcpManager.shutdown();
    plugins.shutdown();          // 原 chromeToStop.stop()
}));
MinionApp.start(config, workspaces, models, manager);   // 签名不变
```
`plugins` **不走 `MinionApp` 静态注入**：现有 MCP 就是靠 `SessionManager.mcpManager()` getter 暴露给 `MainWindow` 的（`MainWindow.java:370-371`），照此办理 —— `SessionManager` 新增 `public ToolPluginManager plugins()`，`MainWindow` 调用改为
`SettingsDialog.show(stage, manager.models(), manager, MinionApp.config(), manager.mcpManager(), manager.plugins())`。`MinionApp.start` 与 `MinionApp` 的静态字段一个都不动。

**`SessionManager`**：
- 字段 `BrowserSession browserSession` → `ToolPluginManager plugins`；构造参数同步（`browser` 包 4 个 import 删除，改 import `plugin` 包）。
- `newRegistry(ctx, sessionId)`：内置 7 个工具照旧 `register(tool)`；随后
```java
ToolContext tc = new ToolContext(ctx.workspace, ctx.skillsDir, tmpDirOf(sessionId).toString(),
                                 ctx.confirmGate, plugins);
for (ToolPlugin p : plugins.plugins()) {
    for (Tool t : p.createTools(tc)) registry.register(p.id(), t);
}
registry.setGate(plugins);
```
- MCP 注册逻辑不变（`register(tool)` 无标签 → 不被 gate 过滤，MCP 的启停仍由 `McpManager` 自己的 enabled + 注册/断开控制）。

**生效验证点**：改开关后不需要任何会话遍历；`AgentLoop` 下一次 `registry.schemas()` 即反映新状态。运行中会话若正处于「已发出请求、等待响应」阶段，本轮 tools 数组已定型，变更从下一次请求开始生效（符合「实时生效，缓存代价忽略」）。

### 4.10 `Config` 与 `config.properties` 清理

| 文件 | 改动 |
|---|---|
| `core/config/Config.java` | 删除 `browserPath()` / `browserPort()` / `browserUserDataDir()` / `browserHeadless()` / `browserTimeoutMs()` 五个方法 |
| `src/resource/config.properties` | 删除「`# ===== 浏览器工具(CDP 驱动 Chrome,需 Chrome 109+) =====`」整段（注释 + 5 个 key）→ 首启生成的外部 config.properties 不含浏览器项 |
| `gui/dialog/SettingsDialog.java` | `BasicPane` 删 5 个字段、`browserNote` 标签、5 行 `row(...)`、`apply()` 中 5 处 `config.set("browser.*")` 与 2 处 `setInt` 分支、浏览器路径 `FileChooser` 逻辑（迁至 `BrowserConfigDialog`）；`show(...)` 加 `ToolPluginManager` 参数与「工具」页分支 |
| `Main.java` | 见 4.9 |
| `gui/session/SessionManager.java` | 见 4.9 |
| `core/tools/browser/ChromeLauncher.java` | 两处异常文案改指向设置页路径 |
| `core/tools/browser/Browser{,Eval,Screenshot,Debug}Tool.java` | 构造参数与字段类型换 `BrowserManager`，`execute` 首行取 session 并判空 |
| 老 `config.properties` | 残留 `browser.*` 行成为死行，不迁移不清理 |

### 4.11 依赖与构建

`pom.xml` 新增（全部 JDK8 兼容；若中央仓库缺该小版本，就近选同线 JDK8 兼容版本并在文档注明）：

```xml
<!-- JDBC 驱动（可插拔数据库工具，只读）；mysql-connector 排除 protobuf-java（仅 mysqlx 用，减体积） -->
<dependency>
  <groupId>com.mysql</groupId><artifactId>mysql-connector-j</artifactId><version>8.0.33</version>
  <exclusions><exclusion><groupId>com.google.protobuf</groupId><artifactId>protobuf-java</artifactId></exclusion></exclusions>
</dependency>
<dependency><groupId>org.postgresql</groupId><artifactId>postgresql</artifactId><version>42.7.4</version></dependency>
<dependency><groupId>com.oracle.database.jdbc</groupId><artifactId>ojdbc8</artifactId><version>21.9.0.0</version></dependency>
```

`maven-shade-plugin` 配置增加 transformer（**必需**：三个驱动都带 `META-INF/services/java.sql.Driver`，不合并会互相覆盖，只剩一个能被服务发现加载）：
```xml
<transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
```
代码侧另有 `Class.forName(驱动类)` 显式加载作为双保险。

预期 jar 体积增加约 8–10MB（mysql-connector 2.5 + postgresql 1 + ojdbc8 4.4）。

## 5. 错误处理与提示文案汇总

一律返回失败 `ToolResult` 给模型自调，不抛异常打断会话（符合项目规约 3）。

| 场景 | 文案 |
|---|---|
| 浏览器未配 path（工具被调用） | `浏览器未就绪：请在 设置 → 工具 → 浏览器操作 → 配置 中填写浏览器路径` |
| 调用已停用/不存在的工具 | `工具不存在或已停用: <name>` |
| 未选数据源 | `mysql 插件未选择数据源，请在 设置 → 工具 → mysql 中选择` |
| SQL 为空 | `SQL 不能为空` |
| SQL 非只读 | `只读工具拒绝执行：语句以 UPDATE 开头（仅允许 SELECT/WITH/SHOW/DESC/DESCRIBE/EXPLAIN）` |
| 多语句 | `只读工具拒绝执行：不允许多条语句` |
| 危险子串 | `只读工具拒绝执行：语句含 FOR UPDATE` |
| 括号语句位写动词（改写型 CTE / WITH 后接 DML） | `只读工具拒绝执行：括号后的语句位出现写动词 DELETE（PG 的改写型 CTE、WITH 子句后接 DML 会真写数据）` |
| 首词只在「不认嵌套」读法下越界 | `只读工具拒绝执行：把块注释按「不认嵌套」的方言（Oracle/MySQL）看时，语句以 DROP 开头（仅允许 SELECT/WITH/SHOW/DESC/DESCRIBE/EXPLAIN）` |
| query 缺 sql | `query 需要 sql 参数` |
| describe 缺 table | `describe 需要 table 参数` |
| describe 无匹配 | `未找到表: <table>` |
| 未知 action | `未知 action: xxx（支持 query/schema/describe）` |
| PostgreSQL 的 schema | `postgreSQL 数据源不支持列出表名（当前环境查询系统表会报错），请直接使用 query，例如 SELECT * FROM 表名 LIMIT 1 推断结构` |
| PostgreSQL 的 describe | `postgreSQL 数据源不支持表结构查询，请用 query 执行 SELECT * FROM 表名 LIMIT 1 自行推断字段` |
| 驱动加载失败 | `MySQL 驱动加载失败（驱动应已内置，请检查 jar 完整性）: <异常>` |
| 连接失败 | `连接数据源「prod-mysql」失败: <驱动 message>（请检查 URL/账号/网络/白名单）` |
| SQL 执行异常 | `查询失败 [SQLState 42000 / 错误码 1064]: <message>` |
| 查询超时 | `查询超时（>300s），已中断；请加 LIMIT 或缩小查询范围` |
| 结果落盘 | `…（完整结果 <N> 字符已落盘：<绝对路径>，可用 Read 查看）` |
| tools.json 写入失败 | `[minion] 写入 tools.json 失败: <原因>`（stderr，与 mcp.json 同口径） |

## 6. 测试计划

### 6.1 单元测试（纯函数/纯逻辑，不引入新测试依赖）

| 测试类 | 覆盖 |
|---|---|
| `ToolStoreTest`（新） | 文件缺失 → 生成默认（四项 enabled 全 false、path 空、dataSources 空、current 空）；损坏 JSON → 备份 `.bak` 后重建；原子写；`dbConfig(id)` 未知 id 返回 null |
| `DbConfigTest`（新） | `currentDataSource()` 命中/未命中/current 空；`removeDataSource` 删当前项回退第一个、删空后 `current=""`；`updateDataSource` 改名同步 `current` |
| `DataSourceValidatorTest`（新） | 标识名空 / 与其它项重名（忽略大小写）/ 改回自身原名放行 / URL 空 / URL 非 `jdbc:` 前缀 / 超长 |
| `SqlGuardTest`（新） | 6 个白名单首词放行；UPDATE/INSERT/DELETE/DROP/TRUNCATE/ALTER/CREATE/CALL/SET/GRANT 拒；前导 `--`、`#`、`/* */`、多重注释与空白混合剥离后判定；`SELECT 1;` 放行而 `SELECT 1; DROP TABLE t` 拒；`SELECT … INTO OUTFILE/DUMPFILE`、四条锁强度短语拒；大小写混合；空/null。按评审轮次分组的 43 项：round 1 注释切开短语与字面量掩码、round 2 每份视图都跑内容级检查、round 3 首词不许从可执行注释里借、round 4 三条轴组合后的同一载荷 + WITH 后接 DML 与改写型 CTE + 块注释配对轴 + 回车也算行尾（每条洞都配一组「合法只读不许误拒」的对照组） |
| `SqlGuardDialectTest`（新，round 4 回归工件） | 多方言差分：测试内独立实现「参考读取器」（7 种方言读法，覆盖行内注释/可执行注释/块注释配对三条轴）算出每种读法真执行的文本，再用另一份「危险定义」判定；结构原子枚举约 5 万条（`/*`、`*/`、`/*!`、`#`、`--`、换行、回车、分号、括号、引号按长度组合夹载荷），断言「任一读法危险 ⇒ 守卫必拒」；另附 40 条真实只读语料反向检查「不许误拒」。在 round 4 修复前的实现上报出 2445 条漏判、修复后 0 条 |
| `MarkdownTableTest`（新） | 表头与分隔行；`cell()` 的 NULL/竖线转义/换行转 `<br>`/120 字符截断；0 行文案；`header()` 截断与未截断两种；30000 字符预算触发落盘提示（`tmpDir=null` 时降级为纯截断） |
| `ToolRegistryTest`（扩充） | `gate` 禁用某 pluginId → `schemas()` 不含其工具、`get()` 返回 null、`all()` 不含；`gate` 放行 → 正常；`gate == null` → 全通过（保住既有断言）；`pluginId == null` 的内置工具永不被过滤；同名覆盖时 `ownerOf` 同步 |
| `BrowserPluginTest`（新） | `statusText()` 三分支（path 空 / 有 path 有头 / 无头）；`createTools` 产出 4 个工具且名字正确；`BrowserConfig` 默认值 |
| `DbPluginTest`（新） | `statusText()` 恒空（当前选中/未配置由行内下拉与占位表达）；`DbTool.description()` 动态反映当前数据源（有/无）与 PostgreSQL 的「仅 query」说明；`DbTool.execute` 在未选数据源、缺参数、SqlGuard 拒绝三种情况下的返回文案（不触达 JDBC） |
| `DbExecutorGuardTest`（新） | `DbTool` 对 PostgreSQL 的 `schema`/`describe` 返回固定提示且**不建连接**（用不可达 URL 断言不抛连接异常） |

### 6.2 手工验证清单（写入实施计划的验收步骤）

1. `mvn package` 通过，jar 体积增幅符合预期；启动后 `jarDir/tools.json` 自动生成且四项 `enabled` 全 false。
2. 设置窗出现「工具」页（MCP 之后），4 行布局正确，状态文案与默认配置一致。
3. 停用状态下发消息，抓请求确认 `tools` 数组不含 `Browser*` / `Db*`（可用现有 LLM 请求日志或临时打印验证）。
4. 浏览器：勾启用 → 配 path → 保存 → `Browser status` 可用；改 port/headless 保存 → 旧 Chrome 被关闭、新配置生效；取消勾选 → 下一轮 `tools` 数组不再含 4 个浏览器工具；模型若仍调 `Browser` → 得到「工具不存在或已停用」。
5. 三种数据库各配一个真实数据源：`query`（含 NULL 值、超长文本、100+ 行截断）、`schema`、`describe`；PostgreSQL 的 `schema`/`describe` 返回固定提示。
6. 只读防护：对真实库执行 `UPDATE`/`DELETE`/`DROP`/`SELECT 1; DROP TABLE t`/`SELECT … FOR UPDATE` 均被拒且库无变更。
7. 切换数据源下拉框 → 当前会话下一轮工具 description 中的「当前数据源」立即变化（无需新建会话）；运行中会话在下一次请求生效。
8. 数据源管理：新建重名被拦、修改标识名同步下拉框与 `current`、删除当前项自动回退、删空后下拉禁用、测试连接成功/失败两种提示。
9. 重启后 `tools.json` 配置与启用状态保持；手改 `tools.json` 为非法 JSON → 生成 `.bak` 并重建默认。
10. 退出软件 → 自启 Chrome 进程被清理（无残留 chrome.exe）。

## 7. 风险与缓解

| # | 风险 | 缓解 |
|---|---|---|
| 1 | jar 体积 +8–10MB | 已排除 `protobuf-java`；用户已确认接受 |
| 2 | shade 未合并 `META-INF/services/java.sql.Driver` → 驱动自动注册失效 | 加 `ServicesResourceTransformer` + 代码显式 `Class.forName` 双保险 |
| 3 | Oracle `getTables` 不限定 schema → 返回 SYS/SYSTEM 海量对象 | 用 `metaData.getUserName()` 限定 + 500 行上限 |
| 4 | `executeQuery` 在三大驱动上都是「先执行再报错」，单独用会漏写 | 三层防护（SqlGuard 白名单/多语句/危险子串 → `setReadOnly(true)` → `executeQuery`）；README 建议为数据源配只读数据库账号作为最终兜底 |
| 5 | 300s 长查询占住该会话工作线程；`Thread.interrupt()` 不保证中断 JDBC socket 读，点「停止」可能仍等到超时 | 每会话独立线程，其他会话不受影响；`setQueryTimeout(300)` 保证最终释放；文档说明该行为 |
| 6 | 密码明文存 `tools.json` | 用户已确认（与 `model.json` apiKey 一致）；README 提示注意文件权限 |
| 7 | 浏览器配置重建会关闭已打开页面 | 配置弹窗顶部明确标注；用户已确认接受 |
| 8 | MySQL 8 驱动连 5.x 服务端握手失败 | URL 模板预填 `useSSL=false&allowPublicKeyRetrieval=true`；连接失败时透传驱动 message 供排查 |
| 9 | `ToolRegistry` 改造影响既有 13 个内置工具与全部测试 | `register(Tool)` 语义不变（pluginId=null 永不过滤）；`gate == null` 时行为与改造前完全一致 |
| 10 | 三个 `dataSources` 列表可能同名标识跨类型混淆 | 唯一性校验限定在**同一类型内**（mysql 与 oracle 可各有 `prod`），下拉框与 `current` 也按类型独立 |

## 8. 非目标（YAGNI）

- 不做数据库写操作（INSERT/UPDATE/DDL）、不做事务控制、不做导出文件。
- 不做连接池、不做连接复用、不做查询缓存。
- 不做声明式字段描述符驱动的通用配置表单（方案 C，已否决）。
- 不做工具级细粒度权限（如「只允许查某几张表」）、不做 SQL 审计日志。
- 不做 `config.properties` → `tools.json` 的浏览器配置迁移。
- 不新增 SQL Server / SQLite / 达梦等其它数据库类型（框架已可插拔，后续按需加一个 `DbType` + 一个插件实例）。
- 不做数据源密码加密存储。

## 9. 文档更新清单

- `README.md`：设置窗「工具」页使用说明；三种数据库工具的动作与只读约束；数据源配置与 URL 示例（含 MySQL 8 连 5.x 参数）；建议为数据源配只读账号；密码明文存储的提示；长查询与「停止」按钮行为说明。
- `docs/ARCHITECTURE.md`：新增 `core/tools/plugin`、`core/tools/db`、`gui/plugin` 三个包的类清单与职责；拉模式过滤链路（`PluginGate`）说明；`tools.json` 文件位置与结构；`BrowserManager` 与 `BrowserSession` 的关系。
- `CLAUDE.md`：包结构补三个新包；依赖列表补 3 个 JDBC 驱动与 shade `ServicesResourceTransformer`；扩展点补「新增可插拔工具：实现 `ToolPlugin` + 一个 `gui/plugin` 面板类，无需改会话装配主干」；核心规约补「工具启停/配置存 `tools.json`，不进 `config.properties`」。
- 实施计划落 `docs/superpowers/plans/2026-09-05-可插拔工具与只读数据库工具-plan.md`。


## 实施期修正记录（计划与本文档的差异，以计划/代码为准）

1. `DbTool` 构造为 `DbTool(DbType, DbConfig, Path tmpDir)`（不再传 DbPlugin + executor）
2. `DbExecutor` 方法统一返回 `ToolResult` 而非 String（错误语义直接给模型）
3. MySQL URL 模板：`&useInformationSchema=true`（REMARKS 注释才有值）
4. `ToolContext` 去掉 plugins 字段（无工具需要它；`ToolPluginManager` 由 Main 装配，会话侧只经 gate 引用）
5. `DbExecutor.firstLine` 改为 `public`（DbPlugin 测试连接复用同一异常首行口径）
6. 「未知工具」→「工具不存在或已停用」同时改 `SubAgentLoop`（两处一致）
7. 数据源表单标签列宽统一 160px（PluginUi.LABEL_WIDTH），与基础设置页 row 同口径
8. 状态列口径定案：`DbPlugin.statusText()` 恒空（初稿「未配置/未选择/当前数据源」三分支）——
   当前选中/（无数据源）由行内下拉框表达，状态列不重复占位；启用开关置首列（§4.7.8/§4.8/§6.1 已同步）
